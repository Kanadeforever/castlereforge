> **当前权威状态（2026-08-29）：v0.3-refactor44 + Castle PadSupport Public API v1。**
> R44 SaveAction原生disabled导航已固化：游戏线程发布三位mask，焦点迁移、上下跳过、确认双检及鼠标清理不依赖插件名或槽号。
> 天书state7根槽位层B退出已形成代码候选：在共享SaveSlot消费前排入Shell既有退出队列；三项动作与二次Yes/No的B语义不变。
> Controller Public API扩展现已停止；v1继续冻结。未来公共能力改由项目级API/SDK统一设计，Controller只作为手柄服务提供者。
> 本轮没有新增Hook、原版地址、INI键或第三方依赖；R43剧情/鼠标/Battle Target稳定边界全部保持。
> 当前目录继续固定为 `src/Controller/{source,templete,tools}`、`docs/Controller` 和仓库根 `build/`；不得恢复旧目录。
> 下文R43及更早版本中与本段冲突的“当前状态”只按历史记录阅读。

# 天书根页面B退出实机清单

- [ ] state7根槽位列表按取消键：退出整个主Interface并回到地图。
- [ ] 主Interface动画未开放时按一次取消：意图进入Shell队列，动画门开放后只执行一次，不丢键、不重复。
- [ ] “存档/读档/取消”三项窗口按取消：只执行index0原版取消，返回槽位层，不退出整个主Interface。
- [ ] SaveAction直接确认框与二次Yes/No按取消：仍由SaveSlot/ConfirmDialog处理，不穿透Shell。
- [ ] 天书根层按LB/RB：继续切主大类；与B同tick时B优先。
- [ ] 标题读档与地图存档点根槽位B：保持各自原版取消路径，不改成主Interface退出。
- [ ] SwapConfirmCancel=1时，当前“取消语义键”执行上述行为。

说明：代码已严格编译，但按用户要求本阶段未更新/运行综合验证工具；以上均待实机。

# refactor44 SaveAction实机验收清单

## A. 按钮mask

- [ ] `111b`：默认index2存档；上边界保持，向下依次读档、取消。
- [ ] `011b`：首次打开直接高亮index1读档；向上保持读档，向下到取消。
- [ ] `101b`：默认index2；向下跳过index1直接到index0，向上可回index2。
- [ ] `010b`：只能保持index1；上/下均不丢焦点。
- [ ] `000b`：停止手柄强制HitTest，不伪造高亮或确认。

## B. 动态变化与确认

- [ ] 已聚焦按钮变disabled时，当帧迁到最近可用项，不需要额外按方向。
- [ ] 同距离两项都可用时选择较小index。
- [ ] 确定键不能向disabled项排队；排队后再disabled时Event Hook清除旧pending。
- [ ] disabled恢复后不得补发先前确认，必须重新按一次确定键。
- [ ] 从槽位按确定打开三项窗口，同一按下沿不能立即执行默认读档/存档。
- [ ] 取消键仅在index0可用时触发原版取消；index0 disabled时不注入。

## C. Owner与鼠标

- [ ] 标题读档、天书、地图存档点共享同一行为，不新增第二套Hook。
- [ ] 实体鼠标或Back/RT鼠标接管后停止强制HitTest并清pending，原版hover/click正常。
- [ ] 再按手柄方向时按最新mask恢复最近可用焦点。
- [ ] 二次Yes/No、分页、普通槽位、R43剧情/战斗/鼠标全部快速回归。

# refactor43 实机验收清单

## A. 周崇、朱浩战后 mode=2 剧情选项

- [ ] 到达用户截图的客房回答：“继续躺一会儿”与“起来走动”两项同时显示。
- [ ] 第一次按下或按上后只出现原版橙色高亮与右侧手形，不出现第二只插件鼠标指针。
- [ ] 下键移到第二项，上键回到第一项；边界继续按同方向时保持首/末项，不越界、不闪烁。
- [ ] 确定键提交当前高亮项，剧情进入对应分支；交换确定/取消布局后应改由新的确定物理键提交。
- [ ] 取消键在 mode=2 中不提交任何一项，也不推进底层文字；mode=3 中仍按旧规则选择“否”。
- [ ] 不先按方向直接按确定时，默认选中并提交第一项。

## B. 剧情 RT 临时鼠标

- [ ] 普通剧情文字、mode=2多选和mode=3是/否中，新按RT都能进入临时鼠标。
- [ ] 按住RT时左右摇杆均可移动鼠标；确定=左键、取消=右键继续遵守 `SwapConfirmCancel`。
- [ ] 松开RT回普通手柄；剧情仍开着时绝不误入地图调查。
- [ ] 标题、战斗、主Interface、商店等非剧情UI按RT仍执行页面旧业务，不被鼠标模式抢走。

## C. Back常驻鼠标与实体鼠标

- [ ] 任意界面按Back进入后，移动任一摇杆不会退出鼠标模式。
- [ ] 剧情结束、菜单关闭或回到地图后，Back模式继续保持；再次Back才正常退出并播放既有回手柄震动。
- [ ] 自由地图按RT仍可退出Back模式并锁住RT到松开。
- [ ] 真实移动实体鼠标仍会无震动结束手柄鼠标会话；手柄主动移动绝不能被误判成实体鼠标。

## D. 删除旧自动隐藏路径的回归

- [ ] 普通地图只移动角色不会触发通用“夺回指针所有权/自动隐藏”日志或副作用。
- [ ] 战斗、菜单、目标选择等明确手柄导航仍按各模块显式Claim显示/隐藏正确焦点。
- [ ] SDL3缺失后的每5秒低频重试仍存在；它不是鼠标计时，不应因本轮修改失效。

## E. Battle Target显式收尾

- [ ] 用手柄方向选择目标后按确定：提交目标并立即隐藏目标鼠标。
- [ ] 用手柄按取消返回父列表：立即隐藏目标鼠标，父层焦点保持旧验收结果。
- [ ] Target生命周期自行结束或战斗结束时，若仍是手柄导航，鼠标同样隐藏。
- [ ] 用实体鼠标接管Target时只清手柄指示状态，插件不得重新Claim或把鼠标藏掉。

> **历史R42记录：** 当前权威版本：v0.3-refactor42（自动最近目标 + 确定/取消布局候选，2026-08-24）。实机稳定基线为 v0.3-refactor41：默认确定键按住/松开互动/取消键退出与 LT 兼容模式均已确认正常；R40目标轮盘及更早基线继续冻结。R42 新增两个默认开关：`Investigation.AutoFocusNearest=1` 在每次建立调查会话时复用R40距离第0项、肩键锁和resolver/25点probe自动尝试最近目标；`Controls.SwapConfirmCancel=0` 为南键确定/东键取消，设为1切换成东键（PS O）确定/南键（PS X）取消。确定/取消交换覆盖所有语义页面、调查激活和Back/RT鼠标左右键（确定=左键、取消=右键），但RB+ABXY战斗快捷键、X/Y专属功能等固定物理位置操作不变。用户补回的INI中文注释全部保留，新选项同样使用中文。PadInput、Cursor、RPG地址和业务UI冻结。下文更早“当前/候选”表述与本段冲突时按历史记录处理。

# refactor42 实机验收清单

## A. AutoFocusNearest默认开启

- [ ] A模式和LT模式进入调查后，不动任何摇杆/肩键，应自动尝试距离角色最近目标；resolver确认后短震。
- [ ] 最近目标中心透明时继续25点probe；全部失败不点击、不空震、不每tick重新尝试。
- [ ] 进入前左杆有残留方向，自动最近目标应先保持；回中/明显转向后左杆接管。
- [ ] 自动目标后按RB选择距离下一项、LB上一项；右杆移动立即人工接管。
- [ ] 调查中RT覆盖，RT松开恢复调查时视为新会话，再按当前最近目标自动聚焦。
- [ ] 没有可互动目标时安全保持调查，松开确定键不互动。

## B. AutoFocusNearest关闭

- [ ] 设为0并重启：进入A/LT调查不自动warp/震动，等待左杆/LB/RB/右杆输入，行为等同R41。
- [ ] 缺键或写大于1使用1；写0关闭。

## C. SwapConfirmCancel=0默认布局

- [ ] 南键（Xbox A/PS X）确定，东键（Xbox B/PS O）取消；标题、确认框、对话、Battle、主Interface、商店、炼化、存档逐项抽查。
- [ ] ActivationMode=0按住南键进入、松开互动，东键取消并锁住南键到释放。
- [ ] Back/RT鼠标：南键左键、东键右键。

## D. SwapConfirmCancel=1 PS布局

- [ ] 东键（PS O/Xbox B）确定，南键（PS X/Xbox A）取消；所有语义页面与默认布局结果相反但业务相同。
- [ ] ActivationMode=0改为按住东/O进入、松开互动，南/X取消；B/A物理字样不能残留在状态机里。
- [ ] ActivationMode=1仍按住LT进入，但使用东/O确认、南/X取消页面业务。
- [ ] Back/RT鼠标：东/O为左键，南/X为右键；确定始终左键、取消始终右键。

## E. 固定物理功能不得交换

- [ ] 两种Swap下，RB+南键始终攻击、RB+东键始终道具、RB+西键技能、RB+北键防御。
- [ ] X/Y专属页面功能、LB/RB类别、LT/RT数值、方向键都不受Swap影响。
- [ ] battle.c保持冻结SHA；只允许InputRouter Chord使用fixed物理表。

## F. 中文INI与独立包

- [ ] 用户补回的Mouse/Investigation/Rumble/Movement/BattleNavigation中文注释全部存在；不得出现英文默认模板回退。
- [ ] 新增Controls和AutoFocus说明均为中文；默认Swap=0、Activation=0、AutoFocus=1。
- [ ] 源码INI与编译内容INI逐字节一致；15份文档与两份检查器镜像一致。

---

# refactor41 实机验收清单（历史，已通过）

用户已确认R41默认确定键调查、取消锁存、RT覆盖与LT兼容模式功能正常；以下原清单保留作双布局回归依据。

## A. 默认 ActivationMode=0

- [ ] 自由地图轻按并持续按住A：当帧进入调查、角色停止移动，左杆方向轮盘、LB/RB距离轮盘、右杆精调均与R40一致。
- [ ] 没有已确认目标时松开A：退出调查，不点击、不震动、不打开其它UI。
- [ ] 当前原版hover已确认且没有pending时松开A：只互动一次并退出；48ms左键必须完整按下/释放，不能被退出清理抹掉。
- [ ] 刚用左杆/LB/RB切新目标、25点probe尚未确认时立即松A：不得点击上一个目标，也不得在模式结束后晚点击。
- [ ] 游戏切到后台、resolver快照过期或协议能力关闭时松A：安全退出且无模拟输入。

## B. B取消与重新武装

- [ ] 按住A调查时按一下B：立刻退出，不与当前目标互动。
- [ ] B按下后先松B、继续一直按A：仍保持退出，不能重新进入调查。
- [ ] 只有A物理松开后，再次新按A才允许进入。
- [ ] A与B从普通手柄态同帧按下：不进入调查；整理按键后重新按A才能进入。
- [ ] B取消必须清除pending probe、pending click与鼠标脉冲；不能把B发送成地图右键。

## C. RT覆盖和恢复

- [ ] A调查中按RT：立即进入临时鼠标；左/右摇杆移动和A/B鼠标左右键保持既有行为。
- [ ] RT松开时A仍按住：恢复A调查，不能因临时confirm屏障立即退出。
- [ ] RT期间先松A再松RT：只回普通手柄，不补一次调查互动。
- [ ] 普通手柄中A与RT同帧按下：RT优先；RT松开而A仍按住时进入调查。
- [ ] RT鼠标中重新按住A：A仍执行鼠标左键；RT松开时若A保持按住，则按统一规则恢复调查。
- [ ] RT鼠标中的B仍是右键，不应触发A调查取消分支。

## D. ActivationMode=1兼容模式

- [ ] INI改为1并重启：自由地图按A不进入调查；按住LT进入。
- [ ] LT调查中A按下确认、松开LT退出；B保持R40既有无新增业务。
- [ ] LT调查中的左杆/LB/RB/右杆、RT覆盖和resolver震动全部保持R40实机结果。
- [ ] INI缺键或写0使用A模式；负数回到0，大于1夹到1。

## E. 冻结回归与包完整性

- [x] R40 LB/RB距离轮盘：用户已验收通过，现为冻结基线。
- [ ] R39左杆角色中心轮盘、R38隐藏鼠标静默、Back常驻鼠标、PadInput热插拔快速回归。
- [ ] 标题、对话、Battle、主Interface、商店、炼化、客栈、存档点/槽不因A模式产生穿透。
- [ ] 源码包和编译内容包均有15份现行中文文档；编译内容包带两份最新检查器和中文工具说明，镜像SHA一致。
- [ ] `源码/构建说明.md` 使用中文文件名；其它非代码文档也必须使用简体中文名。

---

# refactor40 实机验收清单（历史，已通过）

用户已确认 R40 的 LB/RB 正反循环、焦点锁/交还和 resolver 确认后震动验收通过；以下原始清单保留作冻结回归依据。

## A. 基础循环

- [ ] 只有一个目标时，LB/RB 都安全保持该目标；不得崩溃、跳空或重复点击。
- [ ] 多个目标时，松开左杆后连续按 RB，必须按角色距离从近到远逐项移动并从末项回到首项；连续按 LB 必须严格反向并回绕。
- [ ] 刚进入 LT 且尚无当前对象时，第一颗 RB 选择最近目标，第一颗 LB 选择最远目标；结果不得受隐藏鼠标起始位置影响。
- [ ] 左杆已选中任意中间目标后按 RB/LB，必须从该对象的排序位置继续，而不是重新从首/末开始。

## B. 排序与动态边界

- [ ] 两个目标世界距离相同时，按角色右侧0°起的屏幕顺时针顺序稳定；完全同角度时 resolver index 保持确定结果。
- [ ] 目标重叠、目标在角色背后、type>=5远目标均能进入肩键循环；LB/RB 不套用左杆的前方180°门。
- [ ] 循环期间一个目标消失后不得使用旧数组下标或旧指针；当前对象消失时，下一颗 RB 从最新最近项开始，LB 从最新最远项开始。
- [ ] 新目标出现或角色移动导致距离顺序变化时，当前对象只按 object 身份重映射；下一颗肩键从最新顺序继续。

## C. 左杆/右杆融合

- [ ] 左杆保持某方向选中 A 后按 RB 选择 B；下一帧残留左杆不能把焦点抢回 A。
- [ ] 肩键按下时左杆已经回中：保持回中仍可连续 LB/RB；随后重新推离死区，左杆立即接管。
- [ ] 肩键按下时左杆仍偏置：轻微噪声不能抢回；回中或相对按键方向明显转动约12°后，左杆重新接管。
- [ ] 右杆移动与肩键同 tick 时仍由右杆人工精调优先；右杆停下后按 LB/RB，应从真实 resolver hover（若有）继续循环。
- [ ] LB 与 RB 同帧一起按下不得随机选择任一方向。

## D. resolver、点击与震动

- [ ] LB/RB 选目标后仍最多尝试5×5共25点；透明中心、边缘像素与完整失败路径安全。
- [ ] LB/RB 按键沿本身没有目标时不空震；原版 resolver 真正确认新的互动 hover 后必须短震，不能遗漏肩键选择反馈。
- [ ] probe 尚未确认时按 A 只排 `pending_click`；确认后只提交一次48ms原版左键，转向/再按肩键时旧 pending click 必须取消。
- [ ] 同一目标未离开时不得每8ms连续震动；循环到新目标后应各自首次确认短震。
- [ ] 普通自由地图未按 LT/RT/Back 时，隐藏鼠标 hover 互动对象继续完全不震。

## E. R39 与其它冻结回归

- [x] R39 左杆角色中心连续方向轮盘：用户已验收通过，现为冻结基线。
- [ ] 快速回归左杆360°连续选择、约2～3°边界迟滞、同方向不扫穿和右杆 manual override。
- [ ] LT中RT覆盖、RT松开恢复LT、Back常驻鼠标、菜单/Battle/Shop/SaveSlot 与实体鼠标接管不变。
- [ ] PadInput热插拔、普通对话A、R38普通隐藏鼠标静默继续保持既有PASS。

---

# refactor39 实机验收结论（历史，已通过）

用户已确认 R39 角色中心连续方向轮盘验收通过；其原清单不再作为当前阻塞项。R38 普通隐藏鼠标误震修复同样继续 PASS/冻结。

---

# v0.3-refactor36 测试与回归清单

> **v0.3-refactor36 当前候选（2026-08-22）：** refactor33 的统一商店主体已经用户实机确认；refactor35 实机反馈为各商店主体可用，但上下到列表边界不会连续翻页、移动项目会自动打开道具信息且 Y 无法关闭。本轮只修 Shop：上下越界分别调用原版上一页/下一页并落到新页末项/首项；左右显式翻页尽量保留当前行；两侧列手形相对 refactor35 统一向左 10 像素、向下 5 像素。
>
> **Y 道具信息裁决：** `0x417829` 与公共 `CALL_CMD0_EVENT` 是同一条原版 close Event CALL，必须复用全局 Hook，不得再次安装；`info+0x580` 只表示打开/关闭动画，稳态必须比较 `info+0x56C` 与原版关闭/打开目标 `+0x588/+0x58C`。方向/X/翻页只更新原版项目链并抑制最后开窗步骤；仅 Y 放行打开，已打开时 Y 点击 `info+0x598` 的真实关闭按钮。信息窗及动画期间其它商店输入保持模态阻断。
>
> **边界与验证：** 不按装备店、道具店、药店名称分支，继续复用 opcode `0x36 -> 0x413FA0 -> 0x89FCD4` 的同一 Shop Adapter；价格、库存、页、行、数量与成交结果均不直接写。天书偶发跳焦点继续低优先级，Tome/SaveSlot/Cursor 六文件逐字节保持 refactor33。27/27 C 单元严格编译通过，两次无时间戳 PE32/i386 链接字节一致；无 EXE **73 PASS / 0 FAIL**，未修改 `RPG.exe.org`（SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`）**93 PASS / 0 FAIL**。ASI **153,600 bytes**，SHA-256 `05e7ef5e96f32d6a55a819a5cb590ac2302adb9441564495a3df62882af2ef0c`。机器 PASS 不冒充实机 PASS。
>
> **交付裁决：** 从本版起不再内置散装 `证据/` 树；源码、文档与检查器构成接档材料。若以后确有新增证据，只合并维护单一 `文档/验证汇总.md`。
>
> **历史阅读规则：** refactor32 的存档点完整路由与客栈原位返回、refactor33 的地图十字键仅步行和统一商店主体均已验收并冻结；refactor34 的全局 Shop 形状硬门、refactor35 对共享 close Event 的重复 Hook 与对动画 mode 的稳态误判均已作废。当前修正以本文件顶部 refactor36 段落为准。

## refactor36 当前实机验收清单

### A. 全商店与连续跨页（阻断项）

- [ ] 装备店、真正的道具店、药店，以及其它同结构商店均能使用 X/A/B、LB/RB、方向键与买卖数量窗。
- [ ] 左右列分别在多页类别中按 ↓ 越过末项：必须调用原版下一页并聚焦新页第一条有效项目。
- [ ] 左右列分别按 ↑ 越过首项：必须调用原版上一页并聚焦新页最后一条有效项目。
- [ ] 第一页向上、末页向下安全无动作；不得停到空行、串列或反复翻页。
- [ ] ←/→显式翻页仍可用；新页存在同一行时保留行号，否则落到最后有效行。

### B. Y 独占道具信息（阻断项）

- [ ] ↑/↓、X、←/→、LB/RB只更新项目/列/页/类别，任何移动都不得自动打开道具信息。
- [ ] 左买入列和右卖出列各按 Y，打开的必须是当前聚焦道具；再按 Y 必须关闭。
- [ ] 信息窗打开、关闭及动画期间，A/B/X/方向/LB/RB/LT/RT 都不得穿透；只有 Y 执行切换。
- [ ] 关闭后按 A 能正常进入买入/卖出数量窗，不残留说明窗遮挡。
- [ ] 实体鼠标右键仍走原版说明路径；键鼠接管后回手柄不产生残留自动开窗。

### C. 视觉与数量窗

- [ ] 两侧列手形均比 refactor35 向左 10 像素、向下 5 像素，落在用户图示的标题框内。
- [ ] X 往返切列时，手形、红色行、中央详情和 A 的交易侧一致。
- [ ] 数量窗确认/取消只显示原版方框，不显示手形；LT/RT、A、B 与既有规格不变。

### D. 冻结回归

- [ ] 地图十字键仍只行走；客栈诸态/炼化仍从哪里进入回哪里；存档点完整询问链和炼化业务保持已验收结果。
- [ ] 天书偶发跳到“存档”并出现手形的问题不因本轮 Shop 修改变得稳定复现；该项仍按低优先级单独记录。

## refactor35 实机反馈清单（历史；由 refactor36 接续）

### A. 全商店主体恢复（阻断项）

- [ ] 装备店：X/A/B、↑/↓、←/→、LB/RB、买入和卖出数量窗均响应；
- [ ] 道具店：同一整套操作均响应；
- [ ] 药店：同一整套操作均响应；
- [ ] 若发现其它名称但菜单结构相同的商店，也必须直接可用，不允许依赖店名特判；
- [ ] 每家店进入后第一颗手柄输入即可接管；退出重进、跨城市换店后仍可用。

### B. 商店列标记

- [ ] 进入商店并操作任一手柄菜单键后，手形位于当前列标题框内部；X 坐标与 refactor33 相同，只沿 Y 向下。
- [ ] X 往返切列时，手形、红色物品行、中央详情和下一颗 A 的买/卖侧仍一致。
- [ ] 不同商店、窗口/全屏与实际使用分辨率各抽查一次，手形不得回到框外或落入第一行命中区。

### C. Y 道具信息

- [ ] 只按 ↑/↓、X、←/→、LB/RB，确认当前行/说明正常更新，但道具信息绝不自动打开。
- [ ] 左买入列与右卖出列各按 Y：打开的信息必须对应当前聚焦道具；再按 Y 正常关闭。
- [ ] 信息窗打开及关闭动画期间尝试 A/B/X/方向/LB/RB/LT/RT，下面的商店和数量窗都不得动作；只有 Y 可关闭。
- [ ] 关闭信息后 A 当前物品能正常打开买入/卖出数量窗，不保留旧信息遮挡。
- [ ] 实体鼠标右键仍保留原版信息窗；键鼠接管后再回手柄不产生一次残留开窗。

### D. 数量窗无手形

- [ ] 左列买入窗、右列卖出窗初始都由原版方框聚焦“确定”，但画面不显示手形。
- [ ] ←/→在确定/取消间切换时，原版方框随之变化；A 结果与方框一致，仍不显示手形。
- [ ] LT 减、RT 加、B 取消、完成买入/卖出各一笔，业务保持 refactor33 已通过结果。
- [ ] 数量窗关闭后列标题手形恢复到原交易列。

### E. 天书低优先级抽查

- [ ] 从天书进入共享存读档动作页，松开 A/方向后不应被 Shop 拉到“存档”按钮，也不应出现商店列手形。
- [ ] 键鼠→手柄切换一次后再重复；若仍偶发，只记录当时页面、上一界面与日志，不在本轮扩大修改天书。

### F. 已验收基线快速回归

地图十字键仅步行、商店 X/A/B/LB/RB/上下/分页/数量、存档点完整链、客栈原位返回和炼化只需快速抽查；这些业务不应因本轮视觉/信息窗边界而改变。

## refactor33 历史验收清单（已通过）

### A. 地图十字键仅步行

1. 静止时分别按 ↑/↓/←/→，确认四方向都能连续行走；
2. 分别按 ↑+←、↑+→、↓+←、↓+→，确认四个斜向正确；
3. 先用左摇杆推满跑动，再按十字键，确认角色切到步行且方向由十字键决定；
4. 保持左摇杆推满并松开十字键，确认恢复左摇杆跑动；
5. 同时按相反方向，确认该轴抵消且不出现抖动/随机方向；
6. 打开菜单后确认十字键仍只操作菜单，不造成后台角色移动。

### B. 商店根层

1. 至少进入一种可以买也可以卖的商店；手柄进入时应看到一只手形位于当前列标题上方；
2. X 往返切左右列，手形、红色行焦点、中央物品详情与下一颗 A 的交易侧必须一致；
3. 两列分别测试 ↑/↓；空行不得被选中，边界不应乱跳；
4. 两列分别测试单页边界和多页 ←/→；第一页左翻、末页右翻应安全无动作；
5. LB/RB 逐类循环，确认 L 永远视觉向左、R 永远视觉向右；
6. B 必须点击“用器”右侧退出图标并正常退出，不能表现为鼠标右键；
7. 换另一种商店快速复测，确认同一套操作成立。

### C. 买入 / 卖出数量窗

1. 左列 A 打开买入窗、右列 A 打开卖出窗，初始手形均在确认；
2. LT 单次减少、RT 单次增加，检查最小/最大边界；按钮没有额外动画属于规格；
3. ← 聚焦确认、→ 聚焦取消，手形和 A 结果必须一致；
4. B 必须直接取消并回到原交易列；
5. 分别完成一笔买入和卖出，检查金钱、库存与数量由原版正确结算；
6. 实体鼠标/右摇杆接管后，原版 hover/点击必须可用；下一次手柄动作再恢复手柄焦点。

### D. refactor32 冻结快速回归

只需抽查存档点完整链、客栈诸态/炼化原位返回、炼化 LB/RB 与数量/询问、剧情 A、Battle、主 Interface、标题读档及 SDL3 热切换；若任一回归，视为阻断。

## refactor32 历史实机验收清单（已通过冻结）

### A. 地图存档点（本轮主修）

- [ ] 前置剧情“是否存档”：方向可切换是/否，A 提交当前项，B 等价选择“否”；确认“是”后页面打开即接受手柄。
- [ ] ↑/↓可选择所有画面显示槽位，空槽也可选；到边界行为与标题/天书共享 SaveSlot 一致。
- [ ] ←/→上一页/下一页，页码与槽位同步，不重复翻页、不穿透地图。
- [ ] 选槽按 A 后，三项动作页可用方向切换；逻辑项与视觉项一致，三项逐项按 A 都走原版业务。
- [ ] 三项动作触发二次“是/否”后，方向、A、B 均有效；A 确认当前项，B 取消并回动作页，不穿透槽位/地图。
- [ ] 如目标槽位会出现覆盖确认，覆盖“是/否”也逐项验证；取消后回到正确层，确认后由原版完成 I/O。
- [ ] 在二次确认、动作页、槽位页分别按 B，逐层返回行为正确；展开/收起动画中连按不会误触其它 Context。
- [ ] 关闭后地图、剧情与其它菜单输入恢复；再次打开不会持有上一次 wrapper/SaveSlot 旧指针。
- [ ] 用实体鼠标进入/操作时保留原版 hover；再按手柄导航后才取得焦点。

### B. 客栈子界面原位返回（本轮小修）

- [ ] 在客栈选“诸态”进入主 Interface，退出后逻辑与视觉均停在“诸态”；第一颗 A 再次进入诸态。
- [ ] 在客栈选“炼化”进入炼化，根层用 B 点击真实退出图标；完全关闭后逻辑与视觉均停在“炼化”。
- [ ] 从炼化返回后的第一颗 A 必须再次进入“炼化”，不得错误打开“诸态”。
- [ ] 两条路线各重复至少 5 次，来源项每次保持一致且没有多余 Event；不得固定归零。
- [ ] 子界面关闭期间移动实体鼠标/右摇杆：最终尊重真实鼠标位置，不强制抢回来源项视觉。
- [ ] 从炼化第二层 B 回第一层时不得触发客栈交接；只有根层真实退出才触发。

### C. 已验收快速回归

- [x] 用户已确认炼化主体、方向、分页、A/B 与合法/非法炼化路径通过。
- [ ] 快速扫客栈三项、剧情 mode=3、标题/读档/天书、Battle 与主 Interface，确认本轮最高模态 gate 没有 BIG 回归。

## refactor30 / refactor29 历史实机清单

以下未勾选项保留当时的逐项测试记录；用户现已确认此前完成内容通过，不能据此把旧模块重新标成待验。

### A. refactor28 冻结确认

- [x] refactor28 用户整版实机 PASS：治疗法术左右选顶部角色、A使用、B取消正常；机能手形微调正常。
- [ ] 快速回归一次治疗目标、天书、机能，确认新增客栈/炼化没有污染主 Interface。

### B. 客栈根层

- [ ] 进入客栈后默认第一项，↑/↓能在“诸态 / 炼化 / 歇息”三项移动，视觉与逻辑一致。
- [ ] A 诸态：正常打开主 Interface；进入后已 PASS 的各页功能仍正常。
- [ ] A 炼化：正常打开炼化；没有重复点击/输入锁。
- [ ] A 歇息：按原版进入对话/询问/恢复流程。
- [ ] 客栈根层按 B **没有任何客栈业务反应**，也不能退出/误触地图。
- [ ] 实体鼠标/右摇杆接管后原版鼠标可以自由点三项。

### C. 炼化第一层

- [ ] ↑/↓同页选物；空白行不会被选中。
- [ ] ←/→上一页/下一页，边界不越界。
- [ ] LB/RB 六类别循环，列表/说明由原版正确刷新。
- [ ] A 当前合法物品进入第二层。
- [ ] B 通过**“用器”右侧退出图标**关闭炼化；确认没有模拟鼠标右键。

### D. 炼化第二层

- [ ] ↑/↓、←/→、LB/RB 与第二层真实列表同步。
- [ ] 不可炼化项目按 A：**完全无反应，但手柄仍可继续移动/返回，不能假死。**
- [ ] 可炼化项目按 A：打开原版 Yes/No；按项目惯例选择、确认、取消正常。
- [ ] B：只收起第二层回到第一层，不退出整个炼化。
- [ ] 从第二层返回后第一层继续可控制，视觉/逻辑焦点不脱节。

### E. 客栈/存档点等剧情“是/否”

- [ ] 出现截图同类“是 / 否”时，方向键能切换**原版选择框**；←/↑=是，→/↓=否；默认隐藏的手形不应作为第二套焦点出现。
- [ ] A 按当前焦点按原版左键路径提交，并正常继续剧情。
- [ ] B 直接选择“否”并提交；不会同时推进底下普通文字。
- [ ] 客栈“歇息”与存档点至少各测一次，确认它们确实命中同一 mode=3 原版协议。
- [ ] 实体鼠标/右摇杆接管后仍能按原版鼠标方式选择。

### F. BIG 回归快速扫

- [ ] Battle 顶层确认后视觉不弹回第一项；返回后视觉与逻辑 selection 一致。
- [ ] Battle Target 原版目标光标/鼠标不泄漏到普通菜单。
- [ ] 标题/读档/SaveSlot、地图 Y、普通剧情 A、SDL3 热切换保持 PASS。
- [ ] state2~8 与治疗目标快速遍历，无跨 Context 按键穿透。


## refactor28 历史封存（当时当前）实机清单

### A. 治疗法术目标选择（本轮主验收）

- [ ] 在 state3 选择一个需要指定己方角色的治疗法术，正常打开“是否使用”并选确定。
- [ ] 确定后原版手形自动落到当前角色顶部头像/角色图标的几何正中心。
- [ ] D-Pad ←/→ 每按一次只移动一个角色；多人队伍从左到右顺序正确，首尾循环合理。
- [ ] A 对当前手形所在角色施放；实际加血/状态对象与视觉目标一致。
- [ ] B 不施放，等价鼠标右键取消，立即回普通技能列表且 A/B/分页/分类恢复正常。
- [ ] 目标选择中 ↑/↓、LB/RB、LT/RT、X/Y 不穿透到技能列表、主大类或角色切换。
- [ ] 进入目标选择后移动实体鼠标，可以继续按原版点击目标、右键取消；插件不能把鼠标持续拉回。
- [ ] 右摇杆接管后同样可以自由鼠标操作；再次用 D-Pad/A 时可恢复控制器目标选择。
- [ ] 连续对不同角色施放多次，不能残留旧目标焦点、pending 或彩色手形状态。
- [ ] 不需要目标角色的其它技能仍保持 refactor27/state3 已通过行为。

### B. state8 微调（只看视觉）

- [ ] 音乐、音效、空明流转三行手形相对 refactor27 稍向左、稍向下；不遮住文字/按钮。
- [ ] 音乐/音效行手形热点仍在 Button 外，不出现持续或额外 hover 动画。
- [ ] LT/RT 音乐音效、A 空明流转与二次 Yes/No 保持 refactor27 PASS 行为。

### C. BIG 回归快速确认

- [ ] state7 天书槽位、存档/读档/取消、二次 Yes/No 正常。
- [ ] state2～6 页面原有 A/B/X/Y、分页、角色切换与焦点正常。
- [ ] Battle 主菜单视觉与逻辑一致、Target、不可交互项 A 均正常。
- [ ] 标题/SaveSlot、地图 Y、剧情 A 两阶段、Start 跳动画正常。
- [ ] SDL3 拔插/控制器热切换和实体键鼠路径正常。

### D. 机器门槛

- [x] 修改前 refactor27 + `RPG.exe.org`：80 PASS / 0 FAIL。
- [x] 22/22 C 严格编译 PASS。
- [x] PE32/i386 链接 PASS。
- [x] state3 治疗目标短锚点 + 6/6 Event CALL 静态闭合。
- [ ] 用户实机通过后，才可把 refactor28 治疗目标标 PASS。

---

## 历史累计记录（只作因果证据）

> 以下内容保留各历史版本当时的设计、失败方案、地址和测试记录。里面出现的“当前/待验/下一步”不再代表 refactor28 现行状态。

## A. refactor27 新增实机验收

### A1. state7 天书

- [ ] 进入天书后，槽位层 ↑/↓连续选槽；到页边界继续按能按原版规则跨页。
- [ ] 槽位层 ←/→上一页/下一页；第一页/末页边界无锁死。
- [ ] 当前显示空槽与已有槽都能被手柄聚焦，不由插件私自过滤。
- [ ] A 选择槽位后进入原版“存档/读档/取消”三项窗口。
- [ ] 三项窗口 ↑/↓视觉与实际项目一致，不出现屏幕顺序与真实 index 颠倒。
- [ ] 三项窗口 B 始终走原版“取消”，立即退出该窗口。
- [ ] 选“存档”或“读档”后原版 Yes/No 正常出现；此时 A/B/方向只作用于 Yes/No，不泄漏到底层三项窗口、槽位或 Shell。
- [ ] Yes/No 取消后仍可继续操作天书，不产生输入锁。
- [ ] 在槽位根层 B、LB/RB 仍遵守原 Interface Shell；三项窗口/YesNo 活动时不得切大类。
- [ ] 实体鼠标、右摇杆精细鼠标与原版键盘路径没有回归。

### A2. state8 机能

- [ ] 进入页后以手柄操作时，手形焦点位于音乐行最右侧按钮的右方且垂直居中，不遮住文字/按钮。
- [ ] ↑/↓只在 音乐→音效→空明流转 三行移动，首尾不越界。
- [ ] 音乐行 LT/RT 每次只触发一次原版减/加；音量 0/9 边界按原版无副作用，不锁输入。
- [ ] 音效行 LT/RT 同上。
- [ ] 音乐/音效手形焦点本身不造成按钮 hover/持续动画。
- [ ] 空明流转行手形同样位于右侧；A 打开原版 Yes/No。
- [ ] Yes/No 存活期间页面输入与 Shell 被阻断；关闭后恢复机能页。
- [ ] 无弹窗时 B 返回、LB/RB 切大类正常。
- [ ] D-Pad ←/→、X、Y 在机能页不触发未知业务，也不泄漏成换角色/翻页。
- [ ] 实体鼠标或右摇杆接管后停止强制手形；下一次机能手柄导航能够重新取得焦点。

### A3. 防大回归快速检查

- [ ] state2 法宝/道具确认与分页快速回归。
- [ ] state3 绝学、state4 及身、state5 五内、state6 阵形各快速回归一次。
- [ ] Battle Target 与不可交互项 A 快速回归。
- [ ] 标题、标题读档、地图 Y、普通剧情 A 两阶段推进快速回归。
- [ ] SDL3 拔插重连与控制器热切换快速回归。

### A4. 本轮机器门槛（已完成）

- [x] 当前 `RPG.exe.org` 属支持 SHA 白名单。
- [x] state7/state8 构造链、Event CALL、短签名全部匹配。
- [x] 综合静态检查 80 PASS / 0 FAIL。
- [x] 22/22 C 单元 `/W4 /WX /utf-8` 严格编译 PASS。
- [x] PE32/i386 链接 PASS。

## refactor26e 历史新增必测

### 普通剧情 A 两阶段 —— refactor26e 历史清单（后续用户已 PASS）

- [ ] 逐字显示中按 A：立即补全当前句，但不能同时跳到下一句；
- [ ] 当前句完整后再按 A：推进下一句；
- [ ] 连续多句剧情中重复执行上述流程，不能出现 A 完全无反应；
- [ ] 询问框出现时 A 只确认当前询问框，不同时推进背后剧情；
- [ ] 询问框 B 取消后，下一次剧情 A 仍正常；
- [ ] 进入/退出主 Interface、Battle、标题/SaveSlot 后再次触发剧情，A 仍正常；
- [ ] 实体鼠标与键盘原版对话行为无回归。

### refactor24 安装/调度完整恢复 —— 静态必过，实机快速回归

- [x] 14/14 业务 Hook installer 能力全部保留且无重复，真实依赖顺序通过；
- [x] 14/14 worker 关键阶段完整，输入消费/Context 偏序通过；
- [x] `dialogue_input.c/.h` 与 refactor24 字节级一致；
- [ ] 快速确认标题、SaveSlot、地图 Y、Battle、主 Interface 不出现大回归。

### 已通过冻结项

- [x] 法宝/道具弃置/分发确定取消：PASS；
- [x] 五内整页：PASS，包含鼠标焦点/偏移和按钮瞬时动画；
- [x] Battle Target：PASS；
- [x] Battle 不可交互项 A：PASS；
- [x] SDL3 拔插重连：PASS。【历史】当时暂停裁决；其后用户已实机确认控制器热切换 PASS，现已冻结。

## 0. refactor26c 历史：五内按钮动画 + 法宝确定/取消

- [x] 进入五内后 X 切换焦点，原版手形鼠标移动到当前数值**右上方**且不遮字。**用户已通过，封版。**
- [ ] LT 单击一次，只让当前节点“减”按钮闪/响应一次，然后恢复静止。
- [ ] RT 单击一次，只让当前节点“加”按钮闪/响应一次，然后恢复静止。
- [ ] 连续 LT/RT 20 次，不能出现任意按钮持续闪、卡在按下态或历史动画累积。
- [x] X 连续切换的手形鼠标焦点位置/唯一视觉已达到用户要求；本轮禁止改焦点视觉源码。
- [ ] Y 蕴魂同样只产生一次正常按钮响应，不残留动画。
- [ ] 实体鼠标/右摇杆接管后，原版鼠标点击行为与无插件手柄焦点模式一致。
- [ ] 五内页面内左摇杆 50% 换人、LB/RB 大类、B 返回仍正常；本轮视觉修复不能破坏统一操控。

## 0.1 refactor26a 历史失败：物品/法宝弃置确认框

- [ ] X 打开原版允许的“弃置/分发”确认框。
- [ ] 弹窗内 D-Pad ←/→ 能稳定切 Yes/No。
- [ ] A 执行当前项，B 取消。
- [ ] 弹窗存活时 ←/→ **不能翻 state2 页面**。
- [ ] 关闭弹窗后 ←/→ 恢复上一页/下一页。
- [ ] 连续重复“打开→左右切换→B取消→重新打开”不得残留 focus 或锁死。
- [ ] LT/RT 子类型、↑/↓列表、A 与 X 其它已有行为没有回归。

## 1. 左摇杆 50% 角色切换

- [ ] state1～5 中左摇杆水平推量**未超过 50%**时绝不切角色。
- [ ] 向左超过 50% 只切上一角色一次；保持推住不能连续滚动。
- [ ] 回到中间死区后再次向左/右超过 50%，才允许下一次切换。
- [ ] state2/3/4/5 角色切换后页面仍处于同类业务，不能产生输入锁死。
- [ ] state6 阵形左摇杆不能切角色。

## 2. D-Pad 左右统一翻页

### state2 法宝
- [ ] ←上一页、→下一页；第一页/末页边界无副作用。
- [ ] LT/RT 仍只切 6 个子类型，不被左右翻页改坏。
- [ ] ↑/↓列表与上下越界自动翻页仍正常。
- [ ] X 分发/弃置、A、B、LB/RB 无回归。

### state3 绝学/法术
- [ ] ←上一页、→下一页；第一页/末页边界无副作用。
- [ ] LT/RT 子类型切换仍正常。
- [ ] ↑/↓列表、A 技能、使用确认框无回归。

### state4 及身
- [ ] ←上一页、→下一页。
- [ ] LT上一页、RT下一页**继续保留**，与 D-Pad 调用同一原版分页逻辑。
- [ ] X 兵刃/护甲/饰物、↑/↓候选、A装备无回归。

## 3. 五内 state5

- [x] X 严格循环：烈→神→魔→魂→迅；唯一原版手形鼠标焦点跟随。
- [x] LT 只减少当前节点；RT 只增加当前节点；Y 只执行蕴魂。
- [x] **每次 LT/RT/Y 的按钮动画只短暂响应一次，随后正常释放。**
- [x] 连续多次加减不同节点后，历史按钮不保留动画残影。
- [x] D-Pad 左右不加减、不换人；角色只由左摇杆 50% 切换。
- [ ] 实体鼠标/右摇杆接管与重新回到手柄焦点正常。

## 4. 已 PASS 冻结回归

- [x] Battle Target：继续使用用户接受的旧成熟屏幕方向方案；`battle.c` SHA 冻结。
- [x] SDL3 热插拔：用户已 PASS；`pad_input.c/.h` SHA 冻结。
- [x] Battle 不可交互项 A：按下无副作用、不会锁死。
- [ ] 快速确认上述三项没有 BIG 回归即可。

## 5. state6 阵形

- [ ] D-Pad 四方向只移动阵位，绝不切角色。
- [ ] 左摇杆超过 50% 也不能切角色。
- [ ] A 两阶段交换/B 取消或返回保持原版事务。

## 6. 判定

本轮若普通剧情 A 仍完全无反应、一次 A 同时补全文字又跳下一句、或询问框与 Dialogue 同时消费 A，均判 FAIL。其它已通过模块出现 BIG 回归同样判 FAIL。

---

## Public API v1 回归

- [ ] `GetProcAddress("CastlePad_GetApi")` 成功。
- [ ] 请求版本 1 返回 magic/size/version 正确的表。
- [ ] 未连接手柄时 `IsConnected()==0`。
- [ ] LB/D-Pad 物理状态正确。
- [ ] `SwapConfirmCancel=0/1` 时 `ACTION_CANCEL` 跟随语义切换。
- [ ] Back/RT/调查模式下 `AllowsExternalUiInput()==0`。
- [ ] 回到普通手柄模式后 `AllowsExternalUiInput()==1`。
- [ ] 不安装任何外部消费者时，refactor42 原有功能无回归。
