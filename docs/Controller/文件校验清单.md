> **当前权威状态（2026-08-29）：v0.3-refactor44 + Castle PadSupport Public API v1。**
> R44 SaveAction原生disabled导航已固化：游戏线程发布三位mask，焦点迁移、上下跳过、确认双检及鼠标清理不依赖插件名或槽号。
> 天书state7根槽位层B退出已形成代码候选：在共享SaveSlot消费前排入Shell既有退出队列；三项动作与二次Yes/No的B语义不变。
> Controller Public API扩展现已停止；v1继续冻结。未来公共能力改由项目级API/SDK统一设计，Controller只作为手柄服务提供者。
> 本轮没有新增Hook、原版地址、INI键或第三方依赖；R43剧情/鼠标/Battle Target稳定边界全部保持。
> 当前目录继续固定为 `src/Controller/{source,templete,tools}`、`docs/Controller` 和仓库根 `build/`；不得恢复旧目录。
> 下文R43及更早版本中与本段冲突的“当前状态”只按历史记录阅读。

# 天书B文档阶段文件状态

- 代码提交：`90054c6`；
- 修改代码：`interface_tome.c`、`interface_shell.c`、`interface_shell.h`；
- 严格编译：30/30通过；
- 候选ASI SHA-256：`0F5D39EBFC550CC2BD5A74268767DD24B7097BAFDEB8EB6FA51264B66CC9B3F2`；
- 文档：15/15已遍历同步；
- Public API实现/头/DEF：未修改；
- 验证工具：按用户指令保持旧基线，未增加天书B护栏、未运行；
- 实机：待验。

# v0.3-refactor44 文件校验清单

- [x] 业务改动仅在Controller `save_slot.c`；
- [x] `save_slot.h`、天书源码继续旧SHA冻结；
- [x] 低三位mask与最高valid位单u32发布；
- [x] Hit/Event两个现有Hook各安装一次，无新增地址；
- [x] 最近焦点、同距小index、上/下跳过、mask0停止强制焦点；
- [x] worker排队与Event消费双重校验；
- [x] Begin/End/PointerTakeover/离开Action清状态；
- [x] 源码无SaveEnhance名称和91~99槽号；
- [x] Public API v1头、实现和DEF未改；
- [ ] 最终构建大小、SHA与PASS计数待完成后写回；
- [ ] 实机验收待用户执行。

# v0.3-refactor43 文件校验清单

## 1. 当前目录

- `src/Controller/source`：61个要求代码/定义文件；
- `src/Controller/templete/Castle_PadSupport.ini`：20个公开键，完整中文注释；
- `src/Controller/tools`：两份Python检查器和《工具详细说明.md》；
- `src/Controller/build.bat`：30个C单元，x86、`/W4 /WX /utf-8 /nodefaultlib`；
- `docs/Controller`：15份简体中文现行文档；
- `build`：`Castle_PadSupport.asi` 与 `Castle_PadSupport.ini`，允许其它项目产物并存。

禁止项：旧 `src/手柄支持`、旧“源码/编译内容/文档”镜像、Controller私有 `_build` 残留、OBJ/EXP/PYC缓存。

## 2. R43代码护栏

- mode=2必须包含first/last行地址、分发CALL、选择器入口、结果CALL；
- SceneChoice必须同时保留mode=2与mode=3，mode=2使用上/下和隐藏命中中心；
- 禁止SceneChoice源码直接出现 `0x44B0B0` 调用或对mode/hover/result赋值；
- DialogueInput必须在SceneChoice活动时清pending；
- RT合法门必须包含 `GLOBAL_DIALOGUE_ID!=0`；恢复调查必须包含free-map；
- Back自动回切的三个字段与64ms常量必须不存在；
- warp成功后必须读取 `GetCursorPos` 实际落点；
- `PadInput_HasAnyActivity` 和Cursor普通活动自动夺权函数必须不存在；
- 实体鼠标观察与显式 `Cursor_ClaimForControllerNavigation` 必须保留；
- Battle手柄Target确认/取消/离层必须走显式finish；`Battle_OnPointerTakeover`必须只hide、禁止调用finish；
- Public API v1导出仍为未修饰 `CastlePad_GetApi`。

## 3. 当前机器验证

- 30/30 C严格编译及链接：PASS；
- 两次全新 `/Brepro` 构建逐字节一致；
- ASI：190,976 bytes，SHA-256 `2B62CFB8BD4BB485AD8FA9553BA995D04E23439988AC64718A97D5A4918EB043`；
- INI：3,113 bytes，SHA-256 `C71ACC9C961C9588EA19BD5AD05F3F96600E805A5755419BCD20ED71F9AE6E5B`；
- Public API源码接入与PE导出：2 PASS / 0 FAIL；
- R43仓库源码检查：77 PASS / 0 FAIL；
- 原版RPG.exe Oracle：98 PASS / 0 FAIL，其中剧情mode=2/mode=3为3/3签名、5/5 CALL；
- 统一build产物检查：8 PASS / 0 FAIL；
- Git diff whitespace：PASS；
- 实机：待用户验收。

# v0.3-refactor41（A/LT双调查激活方式）文件校验清单（历史）

> 当前权威版本：v0.3-refactor41（A/LT 双调查激活方式候选，2026-08-24）。实机稳定基线为 v0.3-refactor40：LB/RB距离轮盘已验收通过。R41默认 `ActivationMode=0` 按住A调查、松开A互动、B取消；设为1保留按住LT与A确认。RT覆盖、R40目标轮盘、resolver/25点probe和确认后震动冻结。

## 1. 编译产物

- `Castle_PadSupport.asi`：182,784 bytes；SHA-256 `9a21f4730f9e4b5467d3c2bc34b436ed9561ebf2bbeb1cdd504622c82311cc37`。
- `Castle_PadSupport.ini`：658 bytes；SHA-256 `b912dd415e1fc8aa21b4a4d3750bab169ec390daa5ca65db92aeef8b220916d8`。
- PE：Intel i386 / PE32 / DLL；EntryPoint RVA `0x1EC20`；ImageBase `0x10000000`；DynamicBase、NXCompat、NO_SEH、`.reloc` 保留；Import Directory为0，无静态DLL/CRT依赖。
- INI共18个公开键；`[Investigation] ActivationMode=0` 明确存在，源码与编译内容INI逐字节一致。

## 2. 构建结果

- 29/29 C单元使用 x86 clang-cl `/O2 /GS- /Zl /W4 /WX /utf-8 /TC --target=i686-pc-windows-msvc` PASS。
- 两次从空 `_build` 完整编译并以 MSVC Link `/Brepro /nodefaultlib /machine:x86` 链接，ASI SHA完全一致。
- `build.bat` 为UTF-8 BOM + CRLF；代码页使用系统 `chcp.com` 绝对路径；任一编译、链接、INI或文档/工具复制失败都会中止。

## 3. 综合检查

- 源码包、不带EXE：**77 PASS / 0 FAIL**。
- 源码包、带可信原版Oracle：**98 PASS / 0 FAIL**。
- 独立编译内容包 `--artifact-only`、不带EXE：**8 PASS / 0 FAIL**。
- 独立编译内容包、带可信原版Oracle：**29 PASS / 0 FAIL**。
- Oracle：462,848 bytes；SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`；调查resolver结构/距离/互动锚点6/6与CALL 1/1通过。

## 4. R41机械护栏

- Runtime必须以默认0、范围0..1读取ActivationMode；
- ControlModes必须集中提供调查键pressed/down/released/inhibited，禁止散落 `lt_pressed/lt_down`；
- A调查顺序必须为B取消→RT覆盖→A松开；
- B取消必须武装释放屏障并完整清probe/点击；持续A直到物理松开都不能重进；
- A松开确认必须先active、前台、无pending、新鲜快照、真实hover，最后才PulseLeftClick；
- A成功松开确认后只能EndSession，禁止ResetClicks抹掉已获准脉冲；
- 旧A按下确认必须由 `ActivationMode==1` 硬门保护；
- RT恢复必须共用当前调查键语义；A模式恢复时撤销临时confirm残留屏障；
- A/B按下松开本身不空震；仍只在resolver确认新hover后短震；
- R40左杆/LB/RB/右杆、25点probe、普通隐藏鼠标静默和RPG地址继续受旧护栏保护。

## 5. 初学者注释与独立接档

- runtime/control_modes/investigation/plugin修改文件均通过R41提高后的注释比例门；
- 状态机包含“值日班长”比喻、B锁存、RT三条件恢复、松开顺序、pending旧目标误点与点击清理原因；不是只有函数顶部摘要；
- 源码包 `src/手柄支持/文档` 与编译内容包 `编译内容/文档` 均有14/14现行简体中文文档；
- 编译内容包 `工具/` 含最新 `refactor_check.py` 与《工具详细说明.md》；
- 两套文档及检查器镜像与权威源逐字节一致；
- `源码/构建说明.md` 仅作为面向GitHub的构建说明保留英文名，文档目录不放重复构建说明。

## 6. 未授权冻结文件复核

- `pad_input.c`：`389762ef3c860789020d97d3a6a2b9e6dec6811cacf6b4f30218f7e60081918b`
- `pad_input.h`：`008362eb93dc9ef5a0ca71228b1f16fc4246f2516f2882bc3160b2393ee75ee9`
- `cursor.c`：`9b0bf860c5442873937ea699ce4a5c587f50e874a6548b01e6030d22ab871a5e`
- `cursor.h`：`7b744fd24b6d8e9cfeaeba80c45e4f55f4d2e855c224f7ee67ec9419c5e25827`

以上四文件与R40开工前SHA一致。ControlModes是R41授权修改文件，不伪称字节冻结；其旧优先级和释放屏障由结构护栏验证。

实机状态：R38隐藏鼠标静默、R39角色中心轮盘、R40 LB/RB距离轮盘均 **用户PASS/冻结**；R41 A/LT双激活状态机为 **机器候选，待实机验收**。静态PASS不等于实机PASS。

---

## Public API v1 新增文件

源码目录必须额外包含：
- `Castle_PadSupport_API.h`
- `pad_public_api.h`
- `pad_public_api.c`
- `Castle_PadSupport.def`

`build.bat` 必须编译/链接 `pad_public_api.obj`，并把公共头复制到
`编译内容/开发接口/Castle_PadSupport_API.h`。
