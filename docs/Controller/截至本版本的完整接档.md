> **2026-09-01 RuntimeSDK 当前状态：** Controller 已接入标准 Client、完整 Hook 大事务和
> Runtime Schedule 单 tick；StandaloneHost 仍使用本地 Patch/worker。`CastlePad_GetApi` v1
> 继续冻结。Runtime Schedule 等待 ModLoader 外层 Loader-ready 后才开始，工作快照已从
> worker 栈迁入 Runtime 静态区；日志会以“Controller Runtime Schedule 首次 tick”证明真实运行。
> 最终发行包隔离真实 RPG 已确认该首次 tick 出现并持续存活。
> 静态/构建通过，完整资源环境功能回归待验。
>
> **历史业务权威状态（2026-08-29）：v0.3-refactor44 + Castle PadSupport Public API v1。**
> R44 SaveAction原生disabled导航已固化：游戏线程发布三位mask，焦点迁移、上下跳过、确认双检及鼠标清理不依赖插件名或槽号。
> 天书state7根槽位层B退出已形成代码候选：在共享SaveSlot消费前排入Shell既有退出队列；三项动作与二次Yes/No的B语义不变。
> Controller Public API扩展现已停止；v1继续冻结。未来公共能力改由项目级API/SDK统一设计，Controller只作为手柄服务提供者。
> 本轮没有新增Hook、原版地址、INI键或第三方依赖；R43剧情/鼠标/Battle Target稳定边界全部保持。
> 当前目录继续固定为 `src/Controller/{source,templete,tools}`、`docs/Controller` 和仓库根 `build/`；不得恢复旧目录。
> 下文R43及更早版本中与本段冲突的“当前状态”只按历史记录阅读。

# R44 天书B退出与项目级SDK决策接档

## 当前代码状态

- SaveAction disabled通用导航：提交 `ac55836`，严格编译与当时源码检查已通过，待实机；
- 天书根层B退出整个主Interface：提交 `90054c6`，30个C单元严格编译通过，待实机；
- 工作区在文档补充前为干净状态；Public API实现文件未改。

## 天书B根因与成功方案

共享SaveSlot在Tome之前消费B，导致Shell退出永远不可达。成功方案是在Tome根槽位层先调用Shell窄接口：Shell清未提交导航、排EXIT、消费取消、Claim；SaveSlot随后不再看到同一按键；Shell按既有root_ready和原版退出Button提交。深层Action/Popup完全绕过该分支。

修改仅为 `interface_tome.c`、`interface_shell.c/.h`。没有新Hook/地址，没有直接关闭状态写入，没有修改共享SaveSlot、InputRouter或其它页面。

## 禁止方案

- 禁止在Tome复制 `INTERFACE_EXIT_BUTTON` 或直接发UiBridge Event；
- 禁止为了天书改SaveSlot根层B，否则标题/存档点会连带变化；
- 禁止在Action/Popup层把B解释成退出主Interface；
- 禁止立即调用关闭函数绕过Shell动画期队列。

## Public API/SDK决定

停止扩展PadSupport私有API。v1继续兼容；不创建Controller v2。未来另立项目级SDK统一模块注册、版本/capability、线程生命周期、输入/UI所有权和服务调用，Controller只提供手柄能力。SaveAction原生disabled协作不需要API。

## 当前验证边界与下一步

按用户要求，本阶段只补文档，没有更新或运行验证工具。天书代码已有严格编译结果与ASI候选；下一步只需用户测试根层B、三项取消、二次Yes/No和其它SaveSlot owner。用户再次说“继续”后，才补专项检查器/最终验证或进入项目级SDK设计。

# v0.3-refactor44 截至本版本的完整接档

## 1. 当前结论

R44把SaveAction的disabled处理收回Controller通用导航层。外部插件只需在原版Update窗口修改真实Button状态，Controller会自动获得三位mask；双方不共享私有变量、槽号或加载顺序。Public API v1不变。

## 2. 修改范围

- 业务：仅 `src/Controller/source/save_slot.c`；
- 版本字符串：`plugin.c`、`runtime.c`、INI首行；
- 工具：综合检查器与Public API检查器基线文字；
- 文档：`docs/Controller`全15份、GitHub `readme.md`、工具中文说明；
- 未改：SaveEnhance、SaveSlot公开头、地址表、InputRouter、Cursor、Public API头/实现、DEF、build.bat。

## 3. 已确认架构

SaveAction三个按钮index0/1/2对应取消/读档/存档。游戏线程现有Hit/Event Hook调用 `save_action_collect_allowed_mask()`，按原版 `+0x45/+0x04` 判据扫描并用一个u32发布 `VALID|mask`。worker只读取该值进行焦点和输入裁决。

焦点不可用时按index距离找最近项；循环从0开始且只接受严格更短距离，因此同距选较小index。上为+1、下为-1，逐项跳过disabled且不循环。mask为0时撤销强制焦点。

确定键在worker排pending前检查mask和真实Button；Event Hook在disabled短窗口内重新发布mask，并再次检查mask、Button身份和原版条件。任一失败立即清pending。

## 4. 成功方案

- 单u32发布valid+mask，避免两字段跨线程半更新；
- 在游戏线程Hook立即归一化，首次绘制就能从禁用存档迁到读档；
- worker仍用mask导航，不在Update外猜短暂disabled；
- 现有两个动作Hook不变，所有owner共用；
- 鼠标接管、Begin、End和离开动作层都清理状态；
- 状态变化才记录日志，不逐Hit刷屏。

## 5. 禁止/失败方案

- 禁止检查外部ASI、插件变量或固定槽号；
- 禁止公开Button指针或把本需求塞进API v1；
- 禁止新增第二套SaveAction Hook；
- 禁止worker只读Button当前值代替游戏线程快照，外部插件可能已在Update返回后恢复disabled；
- 禁止只在视觉HitTest跳过而不复核Event，否则陈旧pending会在恢复后补发；
- 禁止mask=0时伪造默认焦点。

## 6. 线程与生命周期

`action_allowed_state`是volatile对齐32位，最高位为valid、低三位为mask。没有新增Button指针字段。SaveSlot_Begin/End、实体鼠标接管、回到槽位层均清状态；二次Yes/No继续由ConfirmDialog管理。

## 7. 当前验证与阻塞

严格编译、综合检查、原版Oracle、Public API和统一build产物检查必须全部通过后才交付。由于本机无法运行游戏，保留槽011b、动态disabled、鼠标接管和所有owner仍需用户实机验收；机器PASS不能冒充实机PASS。

## 8. 下一步

先按《测试与回归清单》验证011b与111b，再测101b/010b/000b及动态变化。通过后将R44转为稳定基线；未来Public API扩展另开API v2设计，不与本轮混合。

# v0.3-refactor43 截至本版本的完整接档

## 1. 当前状态

- R42及更早手柄功能由用户确认总体正常，作为本轮基线；
- R43源码已完成静态实现，等待用户实机验收；
- Castle PadSupport Public API仍为v1，联动插件可独立运行；
- 仓库在 `main`，开工前工作区干净；新路径是用户主动调整，禁止恢复旧结构。

## 2. 本轮用户问题与结论

### 周崇、朱浩战后选项

用户截图文字位于 `Mp0610a.msg`：

```text
（我的回答是：）
我就不要拂逆她的一片好意，再躺一会儿好了。
男子汉怎可這般軟弱慵懶？該起來走動走動了。
```

资源控制码是三条 `/t` 后接 `/q`。RPG.exe把 `/q` 转成 `GLOBAL_DIALOGUE_MODE=2`，由 `0x4044F0` 处理；旧插件只支持 `/z -> mode=3 -> 0x404600`，因此完全没有读取该选项的方向/确定输入。这个缺陷已由源码与游戏本体双证据确认。

### 剧情中RT无效

旧 `ControlModes` 只在 `free_map` 时允许RT，任何 `GLOBAL_DIALOGUE_ID!=0` 都会把 `ui_active` 置1并令free-map失败。因此这是代码硬门导致，不是控制器或用户配置问题。

### Back后摇杆即退出

手柄摇杆调用 `SetCursorPos`，旧代码把“请求坐标”当下一次基线。DPI、窗口边界或ddraw包装若让Windows实际落点产生细小差异，下一tick就被识别为实体鼠标，`ControlModes_OnPhysicalMouseTakeover` 随即退出Back/RT。

### “5秒隐藏鼠标”

现行源码没有5秒鼠标隐藏计时。唯一5秒逻辑在 `pad_input.c`，用于SDL3初始化失败后的低频重试，必须保留。真正过时的是普通活动自动夺权/隐藏路径和Back的UI结束自动回切；两者已删除。

## 3. 成功方案

### mode=2

1. `game_addresses.h` 增加mode=2行范围、分发、函数和结果CALL地址；
2. `Runtime_SceneChoiceProtocolOk` 同时验证mode=2/3；
3. `SceneChoice_IsActive` 按mode分别校验；
4. mode=2读取first/last，焦点只存相对项目编号；
5. 上/下夹在边界，隐藏命中坐标为 `545, 370+22*line`；
6. 确定发送64ms左键；取消只吞键；
7. RPG.exe绘制橙色高亮/手形并写VAR999；插件不直接写结果；
8. 普通对话pending在选项活动时清零。

### 鼠标模式

1. `control_rt_mouse_allowed` 放行free-map或活动剧情消息；
2. RT保持分支使用同一门；恢复调查额外要求free-map；
3. 删除Back的 `back_mouse_saw_ui/ui_exit_tracking/ui_exit_start_tick` 和64ms自动回切；
4. `Cursor_HookGameSetCursorPos` 成功后读取实际落点；
5. 删除 `PadInput_HasAnyActivity` 和Cursor普通活动自动夺权；
6. 保留显式Claim、显式模式和实体鼠标优先接管。

Battle Target原先只清 `target_indicator_active`，隐含依赖通用自动夺权把controller owner留在1。删除通用路径后，这个依赖会造成目标提交后鼠标仍可见。R43新增 `target_cursor_finish_controller_navigation()`：手柄确认/取消/离层显式Claim后隐藏；实体鼠标接管仍走不Claim的旧清理函数。

## 4. 明确失败/禁止方案

- 禁止把mode=2当mode=3固定两项硬写；游戏现有七个MSG文件包含 `/q`，其中有两项和三项选择；
- 禁止直接写VAR999或调用0x44B0B0；剧情结果必须由原版左键命中提交；
- 禁止为截图额外显示插件鼠标；原版已经有高亮和手形；
- 禁止在所有菜单放行RT，会抢商店数量、Interface翻页等既有语义；只放行剧情消息；
- 禁止用固定像素容差掩盖自warp问题；应记录Windows实际落点；
- 禁止删除SDL3五秒重试，它与鼠标无关；
- 禁止恢复旧目录或让检查器要求旧“源码/编译内容/文档”镜像。

## 5. 文件与路径

- 代码：`src/Controller/source`；
- INI模板：`src/Controller/templete/Castle_PadSupport.ini`；
- 构建：`src/Controller/build.bat`；
- 工具：`src/Controller/tools`；
- 文档：`docs/Controller`；
- 输出：`build/Castle_PadSupport.asi`、`build/Castle_PadSupport.ini`；
- 原版参考：`参考资料/Castle`，忽略其 `mods`；
- 逆向固化包：`参考资料/研究固化包_v0.7D阶段中_固化68`。

## 6. 验证与当前阻塞

已完成：30/30 C单元 `/W4 /WX` 严格编译并成功链接；两次构建逐字节一致；Public API 2/0；仓库源码77/0；原版RPG.exe Oracle98/0；统一build产物8/0。最终ASI为190,976 bytes，SHA-256 `2B62CFB8BD4BB485AD8FA9553BA995D04E23439988AC64718A97D5A4918EB043`；INI SHA-256 `C71ACC9C961C9588EA19BD5AD05F3F96600E805A5755419BCD20ED71F9AE6E5B`。

仍需实机：mode=2上下/确定、剧情RT、Back摇杆稳定、实体鼠标接管、Battle Target结束隐藏、普通活动不自动隐藏、R42调查/菜单/战斗快速回归。没有实机结果前，R43状态必须标为“机器候选”。

## 7. 下一步

用户安装 `build/` 中ASI+INI后按《测试与回归清单》验收。若mode=2焦点Y出现偏差，先采集窗口模式/DPI和原版鼠标实际命中，不能直接写剧情变量绕过。若Back仍退出，优先看日志究竟是“实体鼠标接管”、再次Back、自由地图RT还是失前台，不猜测。

> **历史R42记录：** 当前权威版本：v0.3-refactor42（自动最近目标 + 确定/取消布局候选，2026-08-24）。实机稳定基线为 v0.3-refactor41：默认确定键按住/松开互动/取消键退出与 LT 兼容模式均已确认正常；R40目标轮盘及更早基线继续冻结。R42 新增两个默认开关：`Investigation.AutoFocusNearest=1` 在每次建立调查会话时复用R40距离第0项、肩键锁和resolver/25点probe自动尝试最近目标；`Controls.SwapConfirmCancel=0` 为南键确定/东键取消，设为1切换成东键（PS O）确定/南键（PS X）取消。确定/取消交换覆盖所有语义页面、调查激活和Back/RT鼠标左右键（确定=左键、取消=右键），但RB+ABXY战斗快捷键、X/Y专属功能等固定物理位置操作不变。用户补回的INI中文注释全部保留，新选项同样使用中文。PadInput、Cursor、RPG地址和业务UI冻结。下文更早“当前/候选”表述与本段冲突时按历史记录处理。

# v0.3-refactor42 截至本版本的完整接档

## 1. 当前结论

- R41 A按住/松开互动/B取消、LT兼容和RT恢复已经用户实机确认正常，转为稳定基线。
- R42新增 `[Investigation] AutoFocusNearest`，默认1；每次真正建立调查会话时，自动选择R40距离列表第0项。设为0完全关闭，回到R41进入后等待手动选择。
- 自动聚焦同时适用于ActivationMode=0的A调查和ActivationMode=1的LT调查；RT覆盖后恢复会重新建立会话，因此也重新按当前开关选择最近目标。
- 选择只调用既有 `inv_select_shoulder_target(snapshot, +1)`：会话刚重置时没有当前对象，因此+1按R40规则取得最近项，保存object身份、取得肩键焦点锁并启动同一25点probe。
- 它不直接写hover、不点击、不调用震动；只有原版resolver确认后才短震，A松开/按下确认仍按R41五门/兼容规则提交。
- 左杆、LB/RB、右杆均能按R40规则接管；自动聚焦只运行一次，禁止每tick把玩家吸回最近目标。
- R42同版新增 `[Controls] SwapConfirmCancel`，默认0：南键（Xbox A/PS X）确定、东键（Xbox B/PS O）取消；设为1：东键/PS O确定、南键/PS X取消。
- 交换只作用于INPUT_CONFIRM/INPUT_CANCEL语义。所有菜单、对话、调查激活/取消和鼠标左右键跟随；确定始终=左键，取消始终=右键。
- RB+ABXY战斗快捷键明确按固定物理位置：RB+南键攻击、RB+东键道具、RB+西/北键技能/防御；X/Y专属功能也不交换。

## 2. 代码与配置

- `RuntimeConfig`新增 `investigation_auto_focus_nearest`；读取 `AutoFocusNearest` 默认1、范围0..1。
- `RuntimeConfig`新增 `swap_confirm_cancel`；读取 `[Controls] SwapConfirmCancel` 默认0、范围0..1。
- `InputRouter`把固定物理表与普通语义表分开；普通确认/取消可交换，新增Raw语义接口供模式层忽略consumed读取。
- `InputRouter_ChordPressed()`当前只服务RB+ABXY，刻意使用固定物理表，从而无需修改已冻结的battle.c。
- `Investigation_UpdateActive()`新增局部 `session_started`。只有 `active` 从0切到1时置1；Cursor会话建立后、右杆/肩键/左杆本tick处理前执行自动候选。
- 复用 `INVESTIGATION_FOCUS_SHOULDER` 是有意设计：自动最近目标与手动肩键享有相同的残留左杆保护和对象失效处理，不维护第二套焦点状态。
- `ControlModes`原直接南/东键读取改为Raw确定/取消语义；调查和Back/RT鼠标都会跟随布局。PadInput、Cursor、RPG地址和业务UI不改。

## 3. INI用户内容保护

用户在R41后补回了完整中文注释。R42以该文件为基线，只做有计划的局部变化：

1. 顶部版本文字更新到R42；
2. 新增 `[Controls]` 的五行中文布局说明与 `SwapConfirmCancel=0`；
3. 把ActivationMode的A/B字样改成“确定键/取消键”，并说明物理位置由布局决定；
4. 在ActivationMode后追加两行中文自动聚焦说明；
5. 追加 `AutoFocusNearest=1`。

没有删除或改回英文任何其它中文说明；原Mouse/Rumble/Movement/BattleNavigation注释保持。检查器将这些中文句子纳入硬护栏，并拒绝英文模板回退。

## 4. 成功方案与禁止路线

### 采用

- 利用会话第一帧标记，一次性自动选择；
- 利用既有距离排序第0项，不重复实现“最近”；
- 利用肩键焦点锁，防止进入前残留左杆立即覆盖；
- 继续由resolver/25点probe确认，成功后沿统一hover震动；
- 关闭配置时不改变R41任何状态字段。
- 确定/取消在InputRouter中心交换，避免逐页复制条件；
- 鼠标模式通过Raw语义实现确定=左键、取消=右键；
- 战斗物理快捷继续走fixed表，保持RB+ABXY肌肉记忆。

### 禁止/踩坑

- 每tick重选最近：会让左杆/肩键/右杆无法接管；
- 直接把目标矩形中心当hover：绕过原版像素/互动真值；
- 自动聚焦后立即点击或震动：几何候选尚未由resolver确认；
- 根据普通隐藏鼠标hover决定“最近”：会让初始位置影响结果；
- 新写第二套距离排序：以后会与R40 LB/RB顺序漂移；
- 为追加键重建INI：会删除用户中文注释；本轮明确禁止。
- 直接交换PAD_SOUTH/PAD_EAST枚举：会连带破坏RB+ABXY、X/Y或设备层；
- 让Battle快捷走可交换语义表：会造成RB+攻击/道具互换；
- 鼠标仍硬读南/东键：会违反“确定始终左键、取消始终右键”。

## 5. 当前阻塞项与下一步

- 新增实机阻塞：自动最近目标开/关；两种确定布局下所有菜单、调查和鼠标左右键；RB+ABXY/X/Y固定物理功能不变。
- R41及更早全部实机PASS继续冻结。
- 最终构建、哈希、检查计数和中文INI哈希以本轮《文件校验清单》为准。

---

# v0.3-refactor41 截至本版本的完整接档（历史，已实机验收通过）

## 1. 当前结论

- R40 的 LB/RB 距离轮盘已由用户实机验收通过，连同 R39 角色中心连续方向轮盘、R38 普通隐藏鼠标静默共同成为冻结基线。
- R41 增加第二种调查激活方式，并通过 `[Investigation] ActivationMode` 切换：`0` 默认按住A，`1` 保留按住LT。
- `ActivationMode=0`：自由地图 A 新按沿进入调查；A 持续按住时左杆/LB/RB/右杆完全复用R40；松开A时只有原版 resolver 已确认当前真实 hover、没有未完成probe、快照仍新鲜且游戏在前台，才提交一次48ms左键互动，然后退出。
- A模式中按B优先于RT和A松开：立即取消调查、清probe/点击，并用现有 `confirm_inhibit_until_release` 锁住仍按着的A。即使B先松开，只要A未松，调查不能重建。
- `ActivationMode=1`：自由地图LT新按沿进入；调查中A按下按R40旧方式确认，松开LT只退出，B不新增业务。
- 两种模式中RT都优先覆盖调查；RT松开时当前配置的调查键仍按住且未被屏障锁住才恢复，否则回普通手柄。Back常驻鼠标优先级仍最高。

## 2. 模块修改与冻结边界

- `Runtime`：只新增 `investigation_activation_mode` 配置值，并把 `ActivationMode` 安全裁到0..1；不读取物理按钮。
- `ControlModes`：把A/LT统一翻译成“调查键按下/按住/松开/被锁住”四个语义；独占进入、B取消、A松开、RT覆盖/恢复和UI中断顺序。
- `Investigation`：新增 `Investigation_ConfirmCurrentHover()`；默认A模式不再把A按下沿当确认，LT模式的旧A确认由 `activation_mode==1` 硬门保护。
- `PadInput`、`Cursor`、RPG.exe地址、Hook、左杆/LB/RB排序、右杆吸附、震动真值、菜单/Battle/Shop/SaveSlot等全部冻结。

## 3. A模式状态机

```text
普通手柄 + 自由地图 + A新按下 + 无释放屏障
    → 调查 active / 阻断地图移动 / 捕获本tick全部语义

调查中持续A
    → R40 左杆方向轮盘、LB/RB距离轮盘、右杆精调、resolver震动

调查中按B
    → B取消优先
    → 清probe与点击
    → confirm_inhibit=1
    → 即使B松开、A仍按住也保持普通手柄且不能重进

调查中松A
    → pending_object必须为0
    → 读取新鲜resolver快照
    → hovered_object非0才排一次左键
    → 结束调查，但不ResetClicks抹掉已获准脉冲
```

如果 A 与 B 同时从普通手柄态按下，不建立调查；必须整理按键后重新按A。A模式中的B只在调查本体是取消；进入RT鼠标后，B继续按原设计作为鼠标右键。

## 4. RT覆盖与恢复

1. 调查中RT新按下：立即结束调查对象状态并进入RT鼠标，记录“调查键当时仍按住”。
2. RT期间新按当前调查键：记录恢复意图；A模式中这颗A同时仍是鼠标左键，保持RT鼠标旧语义。
3. RT松开：恢复意图、调查键仍Down、没有对应释放屏障三项同时成立才恢复调查。
4. A模式恢复前必须撤销RT退出临时武装的confirm屏障，因为这颗A已被明确裁决为合法模式保持键，不是残留鼠标点击。
5. A/LT在RT期间已经松开：RT结束只回普通手柄，不补调查互动。

## 5. 松开确认的五道安全门

`Investigation_ConfirmCurrentHover()` 按固定顺序检查：

1. Investigation能力已安装且会话仍active；
2. RPG.exe仍是前台进程；
3. `pending_object==0`，避免新目标尚未确认时误点旧hover；
4. sequence快照完整且未超过新鲜度门；
5. `snapshot.hovered_object!=0`，只认原版resolver真值。

任何一项失败都返回“未互动”，ControlModes仍安全退出调查。它不把A松开排成 `pending_click`，因为模式已经结束，等待未来resolver再补点会产生跨Context晚点击。

## 6. 配置与默认文件

新增公开键：

```ini
[Investigation]
ActivationMode=0
```

- `0`：按住A调查、松开A互动、B取消；默认；
- `1`：按住LT调查、调查中A确认；R40兼容方式；
- 缺失/负数回到0，大于1夹到1；运行中不热重载，修改后重启游戏。

源码包携带完整默认 `Castle_PadSupport.ini`，构建脚本必须复制到编译内容。源码包和编译内容包都同步全部15份现行简体中文文档；编译内容包另带最新 `refactor_check.py` 与《工具详细说明.md》。`源码/构建说明.md` 为中文构建说明；所有非代码文档文件名统一使用简体中文名。

## 7. 成功方案、失败方案与踩坑

### 采用

- 模式选择只在 ControlModes；Investigation不自行决定A/LT进入；
- A松开在会话仍active时先确认hover，再EndSession；
- B取消复用confirm释放屏障，满足“B先松、A未松仍不重进”；
- 已获准A松开点击只EndSession，不调用会立即清脉冲的 `control_clear_pointer_sessions()`；
- RT恢复全部使用通用 `investigation_pressed/down/inhibited`，不复制一套A专用RT状态机。

### 禁止/失败路线

- 在 Investigation 中直接用 `PadInput_Down(A)`决定进入：会绕过UI/free-map/RT优先级；
- A按下沿既进入又确认：会在进入第一帧误点隐藏鼠标旧hover；
- A松开时 pending仍存在却点击 `hovered_object`：它可能还是上一个目标；
- 先EndSession再确认：active安全门必然拒绝，造成松开A永远不互动；
- B取消只清mode不设confirm屏障：B松开后持续A可能在空白帧重新进入；
- A松开确认后调用 `Cursor_ResetClicks()`：会把刚排的48ms脉冲立即抹掉；
- 在RT鼠标模式把B解释为调查取消：会破坏既有鼠标右键。

## 8. 当前阻塞项与下一步

- 机器完成后唯一新增阻塞是两种模式的实机验收，尤其是A松开互动、B取消锁存和RT覆盖/恢复。
- R40及更早实机PASS项目不得降级；只做快速回归。
- 最终机器结果：29/29严格编译；两次 `/Brepro` 产物逐字节一致；ASI 182,784 bytes，SHA-256 `9a21f4730f9e4b5467d3c2bc34b436ed9561ebf2bbeb1cdd504622c82311cc37`；INI 658 bytes，SHA-256 `b912dd415e1fc8aa21b4a4d3750bab169ec390daa5ca65db92aeef8b220916d8`。源码包77/0、Oracle98/0，独立编译内容包8/0、Oracle29/0；PadInput/Cursor冻结SHA未变。

---

# v0.3-refactor40 截至本版本的完整接档（历史，已实机验收通过）

## 1. 当前结论

- R39 角色中心连续方向轮盘已由用户实机验收通过，正式成为稳定基线；R38 普通隐藏鼠标误震修复继续冻结。
- R40 只在 LT 调查内部增加 LB/RB 目标循环：RB=下一个，LB=上一个；目标顺序为角色世界距离近→远、同距时从角色右侧 0°起按屏幕顺时针角度、仍相同则按原 resolver index。
- 第一次没有当前对象时，RB 选择最近项，LB 选择最远项；已有左杆或肩键对象时从该对象继续循环。右杆人工精调后若已有真实 hover，则肩键从该 hover 继续。
- 肩键选中后由 `INVESTIGATION_FOCUS_SHOULDER` 持有焦点；按键时左杆已回中则连续肩键保持有效、下一次推杆立即接管；按键时左杆偏置则回中或相对原方向变化约 12°后交还左杆。
- LB/RB 仍只选择候选并启动既有 25 点 probe。只有 `0x409B50` 真正把对象发布为 `hovered_object` 后才算选中、触发统一调查 hover 短震并允许 A；按键沿本身不空震、不直接点击。
- `PadInput`、`Cursor`、`ControlModes`、R39 左杆算法、右杆精调、Back/RT、震动资格与所有菜单/Battle/Shop/SaveSlot 路径全部冻结。

## 2. R40 数据与线程边界

- game thread 继续是唯一游戏对象读取者。它在现有 `InvestigationTarget` 快照中新增 `world_distance_squared` 与 `resolver_index` 两个纯值字段；worker 不保存或解引用 object/actor/scene/collision/frame 指针。
- 世界距离由 `object+0x10/+0x14` 与 `actor+0x10/+0x14` 计算，使用饱和 `u32` 平方避免 type>=5 远目标产生 signed overflow；它只决定肩键顺序，不改变原版 type1..4 `<10000` 与 type>=5 绕过资格门。
- 每颗 LB/RB 新按沿都从最新完整快照重建最多 96 项的整数插入排序。状态只保存当前原版 object 身份；快照重排后按 object 重新映射，目标消失时清除身份和对应 pending probe。
- 顺时针 tie-break 只用屏幕半平面与叉积；禁止 `float`、`atan2`、`sqrt`、`qsort` 和新的数学/CRT依赖。

## 3. 输入融合与安全顺序

1. 右杆本 tick 有实际移动时继续优先，取消自动 pending 并进入既有 manual override；同 tick 肩键不抢右杆。
2. 无右杆移动时，先处理目标是否消失，再处理 LB/RB 新按沿；LB+RB 同帧同时按下视为无方向，不依赖读取顺序随机选边。
3. 肩键持有焦点期间只推进肩键对象自己的 probe，残留左杆不参与 R39 方向裁决。
4. 肩键锁释放后才重新运行 `inv_left_stick_target()`；左杆新目标裁决仍必须先于旧 `inv_progress_probe()`，旧 pending click 竞态不能回归。
5. A 若肩键目标仍在 probe，只设置 `pending_click`；原 resolver 确认后才由既有 48ms 左键桥提交。
6. B 本轮不新增业务，继续在 LT 模态中消费；地图没有可复用的原版“返回/取消”事务，禁止凭空发送右键或脚本结果。

## 4. 已确认原版依据

- 固化68原版证据继续确认 `0x4098CB -> 0x409B50`、scene `+0x1C/+0x20/+0x2C/+0x30`、互动类型 `record+0x73` 以及角色/目标 `+0x10/+0x14` 距离链。
- `0x409B50` 和后续原版命中/距离/脚本链仍是唯一业务 Oracle；R40 排序不修改其返回值、对象字段、事件参数或资格判定。
- 可信原版 `RPG.exe` 仍为 462,848 bytes，SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`。

## 5. 当前阻塞项与下一步

- 代码、工具和机器构建完成后，R40 唯一新增阻塞是 LB/RB 实机循环、焦点锁/交还手感与确认后震动验收。
- R39 已通过行为不得重新降级为待验；只做快速回归。
- 最终机器结果：29/29严格编译，两次 `/Brepro` 构建逐字节一致；ASI 181,248 bytes，SHA-256 `0fc2c0027a46595bb5c4368f2d981798d6fbd125f46c478970aa6b5623f6e939`；仓库开发模式无 EXE 73/0、可信原版 Oracle 94/0。冻结的 PadInput/Cursor/ControlModes 六文件与开工前 SHA 一致。

---

# v0.3-refactor39 截至本版本的完整接档（历史，已实机验收通过）

## 1. 当前结论

- 当前开发版本：**v0.3-refactor39**。
- R37 是功能完整基线；R38 首次把 LT 调查从“一次推杆一次选择”改成不回中的连续方向状态机，同时修复隐藏鼠标 hover 误震。
- R38 用户实机结论：**隐藏鼠标误震已修复并冻结；连续旋转明显改善，但仍不够跟手，且摇杆转到某些目标方向时不能稳定选中该目标。** 因此 R38 的连续选择算法只作为过渡版，不能封为最终手感基线。
- R39 只修 LT 左杆方向几何：轮盘中心改为当前角色屏幕位置；取消 ±63.4° 硬候选锥；角度评分改成 `cross²/distance²`；迟滞缩为约 2～3°。震动、Back/RT、菜单、Battle、Shop、SaveSlot 等均冻结。
- 总优先级仍是：**Back 常驻鼠标 > 自由地图 RT 临时鼠标 > 自由地图 LT 调查 > R36/R37 原手柄操作**。

## 2. 已确认架构与地址

- `ControlModes`：Back/RT/LT 唯一模式裁决者；Investigation 不自行解释 Trigger 优先级。
- `PadInput`：只采 SDL3 输入并执行底层震动。
- `Cursor`：坐标、显隐、48ms 左右键可靠脉冲、实体鼠标/手柄指针所有权。
- `Investigation`：只旁路观察原版互动 resolver，不创造互动结果。
- 唯一调查 Hook：`0x4098CB -> 0x409B50`；原函数先执行且原返回值必须保持。
- scene/object/collision/frame 协议：`scene+0x1C/+0x20/+0x2C/+0x30`、object `+0x6C/+0x70`、record `+0x73`、collision `+0x48`、frame `+0x54`。
- type 1..4 原版距离门：平方距离 `<10000`；type>=5 绕过。
- 角色坐标：`actor+0x10` 世界 X、`actor+0x14` 世界 Y；固化68也确认这两个 DWORD 是当前受控实体坐标。
- 地图相机：`GLOBAL_MAP_CAMERA_X=0x00978514` 与既有 Y 全局；角色和目标都用相同 camera 基准转成 640×480 客户区。
- 可信原版 `RPG.exe.org`：462,848 bytes；SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`。

## 3. R39 连续轮盘算法

1. 左摇杆物理死区仍沿用 `PAD_STICK_DEADZONE=7000`；回中只清方向意图，不再承担“重新武装才能切一次”的职责。
2. game thread 每次原版 resolver 执行后，除目标快照外再发布 `wheel_origin_x/y`：当前角色世界坐标减 camera，并裁到 640×480。
3. worker 不保存/解引用 actor 指针，只读取这两个整数快照，因此跨线程安全边界不变。
4. 每个 8ms worker tick 都以角色位置为中心计算目标方向；隐藏鼠标或 warp 后鼠标坐标不再参与左杆轮盘几何。
5. 摇杆先除以 `1024` 缩到约 -32..31，随后计算 dot/cross；`dot<=0` 只排除摇杆背后的 180° 目标。
6. R38 的 `abs(cross)<=dot*2`（±63.4°）硬候选锥已删除，因此摇杆前方不再存在人工空白扇区。
7. 角度误差改为 `cross²/distance² = |stick|²*sin²(angle)`。同一帧 `|stick|²` 对所有目标相同，所以数值越小越贴近摇杆；水平、垂直、对角线权重一致。
8. 角度相同时才用屏幕距离平方 tie-break；同一射线优先较近目标。
9. `left_selected_object` 继续保证同一方向只维持当前对象，不会每 8ms 沿目标链扫穿。
10. 迟滞改为 axis energy 的千分之二，满幅约 2～3°；只处理扇区边界抖动，不阻碍明确转向。
11. 新方向裁决仍先于 `inv_progress_probe()`；A→B 时先取消 A 的旧 probe/pending click，再允许 B 验证。
12. 右杆 `left_manual_override` 保留；右杆精调后不会因左杆仍停在旧方向自动吸回。

## 4. R38 已实机确认并冻结的震动边界

- `Investigation_UpdateRumble()` 仍以原版 resolver 的 `hovered_object` 为唯一互动 hover 真值。
- LT 调查和 Back/RT 鼠标模式：指针**首次真正 hover 到新的可互动对象**时短震；离开后重新武装。
- 普通隐藏鼠标：即使其坐标碰到互动对象也**完全不震**。用户已在 R38 明确确认问题解决。
- 禁止用 `Cursor_ControllerOwnsPointer()` 作为 hover 震动资格；显式资格只来自 `Cursor_MouseModeActive()` / `Cursor_InvestigationSessionActive()`。
- 真正回到普通手柄模式的高优先级“激活手柄”反馈仍归 `ControlModes`；RT/LT 临时手柄子态不触发该模式切换反馈。
- 业务层 `PadInput_Rumble()` 调用源继续限定为 `investigation.c` 与 `control_modes.c`。

## 5. 成功方案、失败方案与踩坑

### 已确认成功
- R37/R38 模式优先级、resolver 真值、25 点透明像素验证、pending click 延迟提交、右杆精调链继续成立。
- `left_selected_object` 防同方向扫穿、新方向先于旧 probe 的竞态修复继续保留。
- R38 震动资格门：**实机 PASS，冻结。**

### R38 实机证明仍不足的方案
- **固定鼠标锚点**：连续轮盘的视觉中心应该是角色，不是隐藏鼠标；已作废。
- **±63.4° 硬候选锥**：会制造人工空白方向；已作废。
- **`abs(cross)/(abs(dx)+abs(dy))`**：L1 分母存在方向权重偏差；已作废。
- **8% 迟滞**：虽能防抖，但实机反馈不够跟手；已作废。

### 仍明确禁止
- 简单删除当前目标记忆：会在 8ms worker 中连续扫穿。
- 把 warp 后鼠标位置当轮盘中心：会退化成高速走格子。
- 绕过原版 `0x409B50`，把几何候选直接当互动真值。
- 先推进旧 probe 再读取新方向：会留下旧目标晚确认/误点击竞态。

## 6. 构建、工具与交付规则

- 源码仍为 **29 个独立 C 单元**；Win32/x86/PE32 ASI；无新增 DLL、INI 键或第三方依赖。
- `源码/build.bat` 保持 UTF-8 BOM + CRLF、`chcp 65001`，x86 clang-cl/MSVC ABI，`/W4 /WX /utf-8 /GS- /Zl /nodefaultlib`。
- `工具/refactor_check.py` 已加入 R39 机械护栏：角色中心必须存在；R38 鼠标 anchor 与硬 cone 必须不存在；震动资格门继续锁定。
- `源码/构建说明.md` 是源码构建入口；所有非代码文档均使用简体中文文件名。
- 发布/源码包都必须携带本完整接档；发布内容只部署 ASI+INI，不包含 RPG.exe.org 或第三方 SDL3 二进制。
- 最终构建结果：29/29 严格编译 PASS；两套全新对象目录 `/timestamp:0` 链接逐字节一致；ASI **181,248 bytes**，SHA-256 `a49a89c20fea24f2fe53ed048341e23734718e67b1a6ef7fde7b85803b1dc160`；INI SHA-256 `cb1999f3951a4584a350e5eaeedf0819d5b3ec3673a7bd8db164987a4af82121`；综合检查无 EXE **75 PASS / 0 FAIL**、可信原版 Oracle **96 PASS / 0 FAIL**。机器 PASS 不冒充实机 PASS。

## 7. 当前阻塞项与下一步

- **唯一 R39 主阻塞：实机确认角色中心轮盘是否真正跟手。**
- 建议测试：按住 LT 不松杆，慢速绕完整一圈，特别观察角色右、右上、上、左上、左、左下、下、右下附近各有目标时，焦点是否按摇杆方向自然接续；明确朝某目标时应快速落到该目标，不能留在旧目标。
- 同方向保持仍不能扫穿；两个目标边界附近只允许轻微迟滞，不能出现高频 A↔B 抖动。
- R38 已通过的普通隐藏鼠标静默只做快速回归；若无回归，不再改震动代码。
- 其它延期问题（天书极偶发跳焦点/显示鼠标、列表末页偶发确认等）继续独立排队，不得借本轮改造扩散。

---

# 《幽城幻剑录》手柄操控模组截至 v0.3-refactor36 的完整接档

> **v0.3-refactor36 当前候选（2026-08-22）：** refactor33 的统一商店主体已经用户实机确认；refactor35 实机反馈为各商店主体可用，但上下到列表边界不会连续翻页、移动项目会自动打开道具信息且 Y 无法关闭。本轮只修 Shop：上下越界分别调用原版上一页/下一页并落到新页末项/首项；左右显式翻页尽量保留当前行；两侧列手形相对 refactor35 统一向左 10 像素、向下 5 像素。
>
> **Y 道具信息裁决：** `0x417829` 与公共 `CALL_CMD0_EVENT` 是同一条原版 close Event CALL，必须复用全局 Hook，不得再次安装；`info+0x580` 只表示打开/关闭动画，稳态必须比较 `info+0x56C` 与原版关闭/打开目标 `+0x588/+0x58C`。方向/X/翻页只更新原版项目链并抑制最后开窗步骤；仅 Y 放行打开，已打开时 Y 点击 `info+0x598` 的真实关闭按钮。信息窗及动画期间其它商店输入保持模态阻断。
>
> **边界与验证：** 不按装备店、道具店、药店名称分支，继续复用 opcode `0x36 -> 0x413FA0 -> 0x89FCD4` 的同一 Shop Adapter；价格、库存、页、行、数量与成交结果均不直接写。天书偶发跳焦点继续低优先级，Tome/SaveSlot/Cursor 六文件逐字节保持 refactor33。27/27 C 单元严格编译通过，两次无时间戳 PE32/i386 链接字节一致；无 EXE **73 PASS / 0 FAIL**，未修改 `RPG.exe.org`（SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`）**93 PASS / 0 FAIL**。ASI **153,600 bytes**，SHA-256 `05e7ef5e96f32d6a55a819a5cb590ac2302adb9441564495a3df62882af2ef0c`。机器 PASS 不冒充实机 PASS。
>
> **交付裁决：** 从本版起不再内置散装 `证据/` 树；源码、文档与检查器构成接档材料。若以后确有新增证据，只合并维护单一 `文档/验证汇总.md`。
>
> **历史阅读规则：** refactor32 的存档点完整路由与客栈原位返回、refactor33 的地图十字键仅步行和统一商店主体均已验收并冻结；refactor34 的全局 Shop 形状硬门、refactor35 对共享 close Event 的重复 Hook 与对动画 mode 的稳态误判均已作废。当前修正以本文件顶部 refactor36 段落为准。

## 0. refactor36 独立接档摘要（当前权威）

- 当前候选只修商店：上下边界连续翻页、Y 独占道具说明、说明窗稳态、列手形左10/下5。
- 所有店型继续共用 refactor33 已验收的唯一 Shop Adapter；r34 的全局形状硬门保持作废。
- r35 主体实机可用，但其说明能力因 `0x417829` 公共 CALL 重复 Hook 而被关闭，且把动画 mode 当稳态；这两项已按原版协议修正。
- 分页使用原版 Prev/Next Event 和真实行 Event，不直接写页、行或业务数据。
- 冻结边界：地图十字键、存档点、客栈原位返回、炼化及更早已验收功能不改；天书偶发问题继续低优先级。
- 交付不含散装证据目录；源码、文档、检查器与编译内容足够接档，必要证据以后只合并成一个文档。
- 机器结果：27/27 编译、确定性双链接、73/0 无 EXE、93/0 Oracle；ASI 153,600 bytes，SHA-256 `05e7ef5e96f32d6a55a819a5cb590ac2302adb9441564495a3df62882af2ef0c`。仍待实机。

## 0.1 refactor35 接档摘要（历史）

### 0.1 项目身份、路线与最终目标

本工程的最终目标不变：为《幽城幻剑录》原版提供现代手柄完整操控，使玩家不用键盘鼠标即可完成标题/读档、地图、剧情、Battle、主 Interface、客栈、炼化、存档点、商店及后续全部原版交互；实体键鼠与右摇杆鼠标仍可随时接管，RPG.exe 始终拥有业务状态、资格判断和结算。

固定路线是“原版 Oracle 先闭合 owner/层级/active/真实 Button 与返回码 → 页面独立 Adapter → capability 预检 fail-closed → 严格编译/PE/自动护栏 → 用户实机验收 → 冻结进入下一页”。Godot 逆向固化包、补充 JSON、`RPG.exe.org` 与 multimedia 都只作只读证据，不进入运行包。

### 0.2 当前冻结边界与本轮范围

- refactor33 的地图十字键仅步行，以及装备店、道具店、药店和同结构商店的 X/A/B/LB/RB/上下/分页/买卖数量，连同 refactor32 和更早内容均已用户验收；
- refactor34 的列手形 Y、Y 道具信息、数量窗无手形方案继续保留，但其 vtable+五子对象全局硬门造成所有商店失效，已判失败并撤销；
- refactor35 只恢复 r33 主体入口并隔离 Y 信息能力。天书偶发跳焦点继续低优先级，本轮不修改天书/SaveSlot/Cursor。

### 0.3 当前商店键位

| 层级 | 手柄 | 现行行为 |
|---|---|---|
| 根层 | X | 切左买入 / 右卖出；列手形在标题框内 |
| 根层 | Y | 用当前行原版右键信息链打开道具信息；再次 Y 点真实关闭 Button |
| 根层 | ↑ / ↓ | 原版 `code=1` 同步当前物品，但抑制最后的信息开窗 transition |
| 根层 | ← / → | 当前列上一页 / 下一页 |
| 根层 | LB / RB | 六类别视觉左 / 右 |
| 根层 | A / B | A 买入/卖出；B 点“用器”右侧真实退出图标 |
| 信息窗 | Y | 唯一手柄动作；其余键模态阻断 |
| 数量窗 | LT / RT | 减少 / 增加数量 |
| 数量窗 | ← / → | 原版方框选确定 / 取消，不显示手形 |
| 数量窗 | A / B | A 执行当前方框；B 取消 |

### 0.4 当前实现与安全门

- 列手形坐标：`x=right-width/6`、`y=top-height`；
- 信息窗：`owner+0x5B4`，`info+0x580` mode，`info+0x598` close Button；
- 合成行事件只带私有 `NAVIGATION/ITEM_INFO` intent，不写原版 row；
- Shop active 只要求有效 `0x89FCD4 owner` 与根 active；顶栏、当前列、数量窗和信息窗在各自动作点局部验证；
- 11 个主体 ButtonEvent 是硬能力；Y 的 1 个 close Event + 2 个 transition 是独立可选能力，失败只关闭 Y；
- Shop 只清自己的 pending 与自己显示过的手形；物理鼠标仍完整走原版。

### 0.5 当前机器结果与下一步

- 27/27 独立 C 严格编译 PASS；两次 PE 链接字节一致；
- 无 EXE 74 PASS / 0 FAIL；未修改 `RPG.exe.org` 94 PASS / 0 FAIL；
- ASI：143,360 bytes；SHA-256 `d98fe6dd526ee9f40e67953711cbb8df3a2bf11973fcb43b63efca2bc129c24e`。

下一步先逐一抽查装备店、道具店、药店的主体恢复，再验三项商店反馈；天书只作低优先级观察，其它已通过业务不得借机重构。

## 0A. refactor33 独立接档摘要（历史，已验收）

### 0.1 项目身份、路线与最终目标

本工程只负责 **《幽城幻剑录》原版程序的现代手柄完整操控模组**。最终目标是玩家不碰键盘和鼠标，也能自然完成标题、读档、地图移动、剧情、Battle、主 Interface、客栈、炼化、存档点、商店以及后续发现的全部原版交互；同时实体键鼠和右摇杆鼠标必须随时无缝接管，RPG.exe 始终拥有真正业务状态与结算。

固定路线仍是“原版 Oracle/EXE 先闭合 owner、层级、active、真实 Button 和返回码 → 页面独立 Adapter → 严格协议预检/fail-closed → 静态与 PE 校验 → 用户实机验收 → 冻结为下一轮基线”。Godot 固化包、补充 JSON、`RPG.exe.org` 和 `multimedia` 只作只读取证，不进入运行包。

### 0.2 当前冻结边界与本轮范围

- 用户已明确确认 refactor32 验收通过，因此存档点完整深层链、客栈原位返回、炼化以及更早全部 PASS 项正式冻结。
- refactor33 只新增“地图十字键八方向且仅步行”和“所有原版商店的一套统一操控”。
- 商店看似种类很多，但 opcode `0x36` 的不同 mode 都进入同一个 `0x413FA0` 主类；不能按画面名字复制多套菜单状态机。
- 新增能力仍是实机候选；静态 PASS 不能替代用户实机观察。

### 0.3 地图十字键协议

`Exploration_Update()` 先读取四个导航语义：相邻方向组成斜向，相反方向在该轴抵消，并映射到原版 `0x21..0x28`。只要十字键给出方向，便优先于左摇杆且锁存 `g_move_force_walk=1`；`Exploration_HookMouseAction()` 仍先调用原版 resolver，仅在需要改变角色当前走跑状态时返回原版 `0x52`。十字键松开后 `force_walk=0`，既有左摇杆径向阈值立即恢复。

### 0.4 统一商店协议与键位

| 层级 | 手柄 | 原版路径 |
|---|---|---|
| 商店根层 | X | 左买入列 / 右卖出列切换；目标列当前行 `code=1` 同步说明与交易侧 |
| 商店根层 | ↑ / ↓ | 当前列八行物品 `code=1` |
| 商店根层 | ← / → | 当前列真实 Prev / Next Button `code=2` |
| 商店根层 | LB / RB | 顶部六类视觉左 / 右；反向 native index 只在 Adapter 换算 |
| 商店根层 | A | 当前行 `code=2`，合法时由原版打开买入/卖出数量窗 |
| 商店根层 | B | topbar index0、“用器”右侧真实退出图标 `code=2` |
| 数量窗 | LT / RT | 真实减少 / 增加 Button `code=2`；不额外伪造动画 |
| 数量窗 | ← / → | 插件私有确认 / 取消焦点；手形放到真实按钮中心 |
| 数量窗 | A / B | A 点击聚焦按钮；B 直接点击真实取消 Button |

左右列视觉手形不是硬编码 854×480 坐标，而是从该列 row0 的原版 Button 屏幕矩形计算：`x=right-width/6`、`y=top-height*2`。实体鼠标或右摇杆接管时撤掉手形、清理尚未消费的 Shop owner 事件，并持续旁听原版最后 hover 的交易列。

### 0.5 当前机器结果与下一步

- 27/27 独立 C 严格编译 PASS，且可重复链接得到同一 ASI；
- ASI：PE32/i386 DLL，动态基址、NX、NO_SEH、重定位存在、无 CRT 导入，139,776 bytes；
- 无 EXE：73 PASS / 0 FAIL；未修改 `RPG.exe.org`：92 PASS / 0 FAIL；
- ASI SHA-256：`5c56706471d71b921145151159591d804b5e1c85ea68aac4277c65eabe932ee8`。

下一步只做实机验收：先验证地图十字键直向/斜向均只走路，再验证至少一种可买可卖商店、空列/单页/多页边界、X 双列手形、六类循环、买入和卖出数量窗、B 逐层退出，以及实体鼠标/右摇杆接管。若发现问题，只修 `exploration.c` 或 Shop capability，不动 refactor32 冻结基线。

## 0B. refactor32 独立接档摘要（历史，已验收冻结）

### 0.1 项目身份与最终目标

本工程只负责 **《幽城幻剑录》原版程序的现代手柄完整操控模组**。最终目标是玩家不碰键盘和鼠标，也能自然完成标题、读档、探索、剧情、Battle、主 Interface、客栈、炼化、存档点、商店以及后续发现的全部原版交互；同时实体键鼠必须随时无缝接管，RPG.exe 始终拥有真正业务状态与结算。

Godot 兼容引擎固化包、原始逆向 JSON、`RPG.exe.org` 与 `multimedia` 只作为只读 Oracle/取证资料，不能混入手柄模组源码或交付运行目录。本轮使用补充的原始固化包复核缺失 JSON 边界，但实际地址结论仍以原版 EXE 机器码和调用链为最终依据。

### 0.2 用户裁决与本轮范围

- 用户确认此前已完成内容全部验收通过；炼化两层业务也已通过。
- refactor31 让地图存档点基础槽位页得到输入，但漏掉三项动作和深层 Yes/No；同时把客栈返回错误固定到第一项。refactor32 只修正这两条状态链。
- 已 PASS 的 Battle、标题/读档、主 Interface state2～8、治疗目标、剧情 A、SDL3 热切换、客栈/炼化主体与 L/R 方向规则继续冻结，禁止借修 BUG 扩大改动面。

### 0.3 存档点根因与修复

refactor30 把“页面里是同一种 SaveSlot”错误等同成“owner 也是同一个 Interface state7”。原版 Oracle 已闭合真实路径：

```text
地图事件 opcode 0x3E
  -> 0x413B00 存档点包装层
  -> [0x89FCD0] = wrapper
  -> wrapper+0x580 = 0x4245B0 标准 SaveSlot
```

`save_point.c/.h` 继续只管理 wrapper 的发布、`+0x584` 动画 phase、子对象替换和销毁；稳定期把 `+0x580` 交给唯一 `save_slot.c`。refactor32 将 `SaveSlot+0x5A4` 三项动作 Hook 从天书能力中拆为全局共享子能力，并在 wrapper 模态分支继续调用最深层 `ConfirmDialog`。现行链为“前置 mode=3 → 槽位/分页 → 三项动作 → `action+0x5B4` Yes/No → wrapper 退出”；文件 I/O 仍全部归 RPG.exe。

### 0.4 客栈原位返回根因与修复

refactor31 把“逻辑第一项、视觉第二项”误修成两者都固定第一项，方向与用户期望相反。refactor32 由父 Inn 在点击真实诸态/炼化入口前记录 `return_focus`，只在同一 owner 经历 inactive→active 后恢复：诸态→诸态、炼化→炼化。Inn 通过既有 `inn_claim_navigation()` 与 `Inn_FilterButtonHit()` 同步逻辑/视觉；Synthesis 不再知道 Inn，真实鼠标或右摇杆仍可接管。

### 0.5 当前机器结果

- 26/26 独立 C 严格编译 PASS；
- ASI：PE32/i386 DLL，无 CRT 导入，131,584 bytes；
- 无 EXE 综合检查：71 PASS / 0 FAIL；
- 完全未修改 `RPG.exe.org`：89 PASS / 0 FAIL；
- ASI SHA-256：`1eac7488d4088f8a8fe6e71ce9feb45147fdaa6d2daed2380f90de95617dedd8`。

### 0.6 下一步路线

1. 先实机验收地图存档点从前置询问到深层 Yes/No 的完整输入，以及客栈诸态/炼化两条原位返回路径。
2. 两项通过后将 refactor32 封存为新 Oracle；若失败，只修各自 Adapter/路由，不动已经 PASS 的共享 SaveSlot、炼化业务和其它冻结能力。
3. 再沿既定覆盖路线进入尚未完成的原版交互（优先商店及与炼化相似但协议独立的页面），每次坚持“先逆向闭合 owner/状态机/真实 Button，再接手柄语义”。
4. 全部原版交互覆盖后进入最终回归、键鼠接管、热插拔、安装部署与文档收口，达成全程现代手柄目标。

## 0A. refactor29 独立接档摘要（历史）

### 0.1 项目身份、边界与最终目标

本工程只负责 **《幽城幻剑录》现代手柄完整操控**。它不是总 Remaster，也不承担 BugFix、宽屏、随时存档、免 CD、Mod Loader 或 Godot 兼容引擎。最终目标是让玩家只使用现代手柄即可自然完成标题、读档、探索、剧情、战斗、主 Interface、客栈、炼化、商店以及后续发现的所有原版交互，同时保留键盘/鼠标原版路径。

长期输入架构继续锁定为：

```text
SDL3 物理输入
  -> PadInput 统一 Press / Release / Hold / Axis
  -> InputRouter 通用语义
  -> Context 的 PASS / MERGE / OVERRIDE / CONSUME
  -> 当前页面 / 区域 Adapter
  -> UiBridge / 原版 ButtonEvent、HitTest、鼠标命中、状态机与生命周期
```

通用层只提供“确认 / 取消 / 导航 / 大类 / 子类 / 分页”等能力；页面 Adapter 决定是否继承、合并、覆盖或吞掉。任何页面只要原版协议特殊，就必须保留自己的 Adapter，不能为了形式统一而改写原版业务事务。

### 0.2 当前用户实机 PASS / 冻结基线

用户已经明确将 **refactor28 整版验收为 PASS**。因此下列能力全部属于当前冻结基线：

- 标题菜单、标题读档与共享 SaveSlot；
- 地图 Y 打开主 Interface、Interface Shell 大类/角色/返回生命周期；
- state2 法宝/道具及确认/弃置路径；
- state3 绝学/法术基础页面，以及治疗法术“确定使用后选择顶部角色目标”：←/→选人、A使用、B取消；
- state4 及身；
- state5 五内完整页面；
- state6 阵形；
- state7 天书；
- state8 机能以及用户要求的手形“向左约10px、向下约5px”微调；
- Battle 顶层/子菜单、视觉锁存、Target、不可交互项 A 不锁死；
- 普通剧情 A 两阶段推进；
- R3/Back 精细鼠标、电影跳过；
- SDL3 拔插重连和控制器热切换。

refactor29 **只新增三块候选能力**：客栈根菜单、炼化两层菜单、公共剧情 `mode=3` 两项“是/否”。这三块还没有用户实机裁决，不能写成 PASS。

### 0.3 refactor29：客栈根菜单

原版对象与协议：

- `GLOBAL_INN_UI = 0x0089FCCC`；
- Update 锚点 `0x00413850`；
- `+0x579` 为 active；
- `+0x588 / +0x58C / +0x590` 对应“诸态 / 炼化 / 歇息”三个真实 Button；
- `0x00413909 -> 0x00431310` 为 HitTest；
- `0x00413933 -> 0x00431380` 为 ButtonEvent。

手柄实现：↑/↓在三项中移动焦点，A 只向当前真实 Button 注入原版事件。根层原版没有 B 取消入口，因此 B 只在当前 Context 被消费以防输入穿透，**不制造任何业务事件**。诸态打开主 Interface、歇息退出并恢复 HP/MP 都完全交给 RPG.exe。

### 0.4 refactor29：炼化两层菜单

原版主对象：`GLOBAL_SYNTHESIS_UI = 0x0089FCBC`；ctor `0x0040F6D0`，Update `0x0040FBE0`。对象字段：

- `owner+0x588`：顶部类别/退出栏；
- `owner+0x594`：第一层列表；
- `owner+0x598`：第二层列表；
- `owner+0x5C8`：原版最终 Yes/No；
- 第一层类别/行/总页/当前页：`+0x5A0/+0x5A4/+0x5A8/+0x5AC`；
- 第二层类别/行/总页/当前页：`+0x5B4/+0x5B8/+0x5BC/+0x5C0`；
- 顶栏 `+0x57C` 子 Button：index0 是“用器”右侧**退出图标**，index1..6 是六类；
- 第一层翻页 Button：`+0x5EC/+0x5F0`；
- 第二层翻页 Button：`+0x634/+0x638`；
- 第二层取消 Button：`+0x63C`。

八处真实 Event CALL 均指向 `0x431380`：`0x40FE04 / 0x40FF2A / 0x40FF94 / 0x40FFF7 / 0x410061 / 0x4100E4 / 0x41011B / 0x410212`。

现行手柄语义：

- 第一层：↑/↓选物，←/→翻页，LB/RB切六类别，A选择当前物品进入第二层；B **只能触发顶部“用器”右侧退出图标**；
- 第二层：↑/↓选物，←/→翻页，LB/RB切类别，A把当前真实行 Button 交给 RPG.exe；B **只能触发 `+0x63C` 原版取消 Button**返回第一层；
- 插件不复制“能否炼化”规则。不能炼化的物品按 A 时由 RPG.exe 自己拒绝，因此应表现为**完全无反应且不锁输入**；能炼化时由 RPG.exe 自己弹 Yes/No；
- **炼化和以后商店都禁止用 RMB / 鼠标右键冒充退出**。根层退出必须走真实退出图标。

### 0.5 refactor29：公共剧情 mode=3“是 / 否”

这类对话选择常见于客栈住宿确认、存档点等。它不是菜单 `ConfirmDialog`。原版协议已经闭合：

- 公共消息分发器 `0x00403E30`；
- `mode=3` 时 `0x00403E80 -> 0x00404600`；
- `0x404600` 读取真实鼠标坐标，命中矩形为 `500 < x < 590`、`300 < y < 375`；
- 上半区把原版视觉/hover 状态设为 10（第一项“是”），下半区设为 9（第二项“否”），外部为 8；
- 原版提交 CALL：`0x404697 -> 0x44B0B0` 与 `0x4046AF -> 0x44B0B0`。

用户补充截图已经确认：**该界面本来就会绘制自己的选择框**。因此 `scene_choice.c` 不再显示第二只手形鼠标，而是调用 `Cursor_MoveHiddenSelectionAt()` 把隐藏的真实鼠标移动到原版命中区，让 `0x404600` 自己驱动选择框：

- ←/↑：选择“是”；
- →/↓：选择“否”；
- A：对当前项发送一次短左键脉冲；
- B：先选择“否”，再发送一次短左键脉冲；
- 实体鼠标或右摇杆接管后立即停止强制隐藏坐标；
- 插件不写剧情结果、不直接写 `0x46F678/0x46F679`、不直接调用 `0x44B0B0`。

这样视觉和业务仍完全属于原版，手柄只负责把“选择意图”送进原版鼠标协议。

### 0.6 refactor29 当前源码架构

当前为 **25 个独立 C 编译单元**。在 refactor28 的 22 单元基础上新增：

- `inn.c/.h`：客栈根菜单 Adapter；
- `synthesis.c/.h`：炼化两层 Adapter；
- `scene_choice.c/.h`：公共剧情 mode=3 两项选择 Adapter。

既有关键模块继续保持原职责：`runtime` 负责地址/配置/日志/patch，`pad_input` 负责 SDL3，`input_router` 负责语义与 Context，`cursor` 负责所有权和指针视觉，`ui_bridge` 负责 owner-aware 原版 Event/HitTest，`battle`、`frontend/save_slot`、`interface_*` 等分别保持已通过业务边界，`plugin.c` 仍是唯一生命周期和调度入口。

SceneChoice 在调度优先级上属于 modal：`ConfirmDialog_Update()` 后处理 SceneChoice，再处理 Inn/Synthesis/Dialogue，避免剧情选择把输入漏给场景或菜单。

### 0.7 当前支持 EXE 与 fail-closed

当前支持两个已验证协议兼容的原版 SHA：

- `b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf`：历史精确目标；
- `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`：用户确认完全未修改的 `RPG.exe.org`。

二者均属于支持白名单，但仍逐 capability 检查 CALL/机器码；未知样本或局部协议失配必须局部 fail-closed，不能靠跳过校验“强行兼容”。

### 0.8 refactor29 构建与机器验证

最终源码已经按 25 个单元逐个使用 x86 `clang-cl --target=i686-pc-windows-msvc /O2 /GS- /Zl /W4 /WX /utf-8 /TC` 严格编译，并使用 `lld-link /dll /nodefaultlib /machine:x86 /entry:DllMain@12` 链接为 PE32/i386 ASI。

当前机器结果：

- 25/25 C 编译 PASS；
- PE32/i386 链接 PASS；
- 无 EXE 包结构/源码护栏：**69 PASS / 0 FAIL**；
- 完全未修改 `RPG.exe.org` 全协议：**86 PASS / 0 FAIL**；
- ASI SHA-256：`0006c1b8a8c3bb03acd9b1052688a35f5c703e877192fa3ebdfa5b5f79cf6f41`。

这些是机器/静态验证，不等于 refactor29 三块新功能已经实机 PASS。

### 0.9 本轮成功方案、禁止方案与踩坑

已确认成功/应继续沿用：

- 客栈、炼化始终触发真实 ButtonEvent，不 new 页面、不直写 HP/MP、类别、页、行、炼化结果；
- 炼化资格由原版自己裁决，不复制业务判断；
- 炼化根层 B 走真实退出图标，第二层 B 走真实取消 Button；
- 剧情 Yes/No 复用原版 mode=3 鼠标命中与选择框，不把外观相似的 UI 强行套入 `ConfirmDialog`；
- 新页面只增加自己的 Adapter，不修改 refactor28 已 PASS 的冻结模块。

明确禁止：

- 炼化/商店用 RMB 作为退出兜底；
- 为 SceneChoice 再显示第二套手形焦点；
- 直接写剧情选择结果、炼化资格/结果或业务 state；
- 不可炼化项 A 后自己建立 pending/锁状态；
- 为了公共化而修改已 PASS Battle/Interface/SaveSlot 的特殊事务。

### 0.10 当前待验、阻塞项与下一步

refactor29 当前唯一阻塞是**用户实机验收**，重点检查：

1. 客栈：↑/↓焦点、A 的“诸态/炼化/歇息”三条原版业务，以及 B 不产生业务；
2. 炼化第一层：↑/↓、←/→、LB/RB、A进入第二层，B 通过右上退出图标正确离开；
3. 炼化第二层：同类导航、B 返回第一层；不可炼化项 A 完全无反应且控制不锁；可炼化项 A 正常弹原版 Yes/No；
4. 剧情 mode=3：方向只移动**原版选择框**，不出现第二只手；A确认当前项；B等价选择“否”；客栈住宿和存档点等至少各测一个场景；
5. 回归冻结项：主 Interface、治疗目标、Battle、Title/Save、剧情 A、SDL3 热切换至少做快速 smoke test。

在收到实机裁决前，不得把 refactor29 写为稳定 PASS，也不要擅自继续商店或其它新页面。下一步应先根据实机结果只修 refactor29 自己的 Adapter；只有三块通过后才进入下一项覆盖。

---

## 1. refactor28 历史独立接档摘要（当时现行）

### 1.1 项目边界与最终目标

本工程只负责 **《幽城幻剑录》现代手柄完整操控**。BugFix、宽屏、随时存档、免 CD、Mod Loader、Godot 兼容引擎等由其它独立模块完成；不要把这些功能塞进本 ASI。最终手柄模组目标是让玩家仅使用现代手柄就能完成标题、探索、剧情、战斗、主 Interface、商店/炼化等原版全部交互，同时保持键鼠原版路径可用。

### 1.2 当前稳定基线

refactor27 已由用户实机确认 **state7 天书 + state8 机能业务 PASS**。此前已 PASS 的 state2 法宝/道具、state3 绝学/法术基础页面、state4 及身、state5 五内、state6 阵形、Battle、标题/SaveSlot、地图 Y、剧情 A、SDL3 拔插与热切换等继续冻结。refactor28 不是重做这些模块，而是补一个此前未注意到的 **state3 治疗法术角色目标阶段**，并微调 state8 手形位置。

### 1.3 refactor28 新功能：治疗法术目标选择

原版 state3 在玩家于使用确认框选择“确定”后，并不总是立刻结算。需要指定己方对象的治疗技能会：

1. `0x43C2DD` 将 `page+0x768` 置 1；
2. `0x43C2E4..0x43C2EC` 调 `0x43DFD0(2)` 切换原版鼠标模式/颜色；
3. 下个 Update 在 `0x43C19E` 发现 `+0x768 != 0`；
4. `0x43C1AA -> 0x43C920` 扫描 `page+0x770..+0x780` 五个顶部角色 Button；
5. `0x43C933 -> 0x431380` 返回 `code=2` 时，原版自己解析真实角色 ID并执行治疗；
6. `page+0x784` 是全屏取消 Button，`0x43C1F7 -> 0x431380` 返回 `code=1` 时原版清 `+0x768`；
7. 目标选择结束后原版调 `0x43DFD0(0)` 恢复鼠标模式。

插件实现严格叠在这条链上：

- 初始焦点取 `Interface+0x5C4` 当前角色；真实队伍人数取 `Interface+0x5EC`，最大 5；
- 控制器拥有指针时用 `UiBridge_GetButtonScreenCenter()` 将手形放到真实顶部角色 Button 几何中心；
- D-Pad ←/→ 只改插件私有 `target_focus`，在真实可用 Button 中循环；
- A 排 `UI_EVENT_OWNER_INTERFACE` pending，目标 Button 在 `0x43C933` 返回一次 `code=2`；
- B 排取消 Button pending，在 `0x43C1F7` 返回一次 `code=1`；
- 目标状态声明为 Interface modal，阻断 Shell 的 B/LB/RB/左摇杆换角色及其它列表输入；
- 实体鼠标/右摇杆接管时只隐藏插件私有焦点，不清 `+0x768`，原版鼠标仍可自由选择；
- 插件禁止直接写 `+0x58C/+0x588/+0x788/+0x78C` 等业务目标字段。

### 1.4 state8 机能微调

refactor27 的五个原版 Event、LT/RT/A 业务全部冻结。本版仅把 `OPTIONS_FOCUS_GAP_X` 从 12 改 2，把 `OPTIONS_FOCUS_OFFSET_Y` 设为 5。也就是手形约向左 10px、向下 5px；X 仍在 Button 右缘外，避免音乐/音效出现 hover 动画。

### 1.5 当前源码架构

22 个独立 C 编译单元：

- `runtime.c`：Win32 API、配置、中文日志、地址预检、CALL patch；
- `pad_input.c`：SDL3 枚举/采样/拔插热切换；
- `input_router.c`：统一物理输入到语义、Context、PASS/MERGE/OVERRIDE/CONSUME；
- `cursor.c`：实体鼠标/右摇杆/控制器焦点所有权；
- `ui_bridge.c`：owner-aware ButtonEvent/HitTest 一次性桥；
- `battle.c`：已通过的 Battle 独立业务；
- `frontend.c` / `save_slot.c`：标题与共享 SaveSlot；
- `exploration.c`：地图移动与 Y 入口安全点；
- `dialogue_input.c` / `confirm_dialog.c` / `movie_skip.c`：剧情、询问框、电影；
- `interface_shell.c`：主 Interface 根层分类/角色/退出；
- `interface_items.c`：state2；
- `interface_skills.c`：state3，refactor28 新目标 modal 也在这里；
- `interface_equipment.c`：state4；
- `interface_inner_stats.c`：state5；
- `spatial_neighbor.c` + `interface_formation.c`：state6；
- `interface_tome.c`：state7；
- `interface_options.c`：state8；
- `plugin.c`：唯一生命周期、Hook 安装和 worker 调度。

所有源文件名保持英文；源码内使用详细简体中文注释。

### 1.6 地址/能力边界

支持 EXE SHA：

- `b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf`：历史精确目标；
- `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`：用户确认完全未修改的 `RPG.exe.org`，协议兼容 PASS。

state3 refactor28 新增地址：

- `SIG_INTERFACE_SKILLS_TARGET_UPDATE = 0x0043C19E`；
- `CALL_INTERFACE_SKILLS_TARGET_ROW = 0x0043C933 -> 0x00431380`；
- `CALL_INTERFACE_SKILLS_TARGET_CANCEL = 0x0043C1F7 -> 0x00431380`；
- `page+0x768` target active；`page+0x770` 起 5 个 target Button；`page+0x784` cancel Button。

state3 capability 现要求 **治疗目标状态锚点 1/1 + ButtonEvent 6/6**。不匹配时只禁用 state3 Adapter，不能连坐其它已通过模块。

### 1.7 构建与机器验证

开发前先复核 refactor27：`RPG.exe.org` 综合检查 **80 PASS / 0 FAIL**。refactor28 最终源码：

- 22/22 C：x86 `clang-cl /W4 /WX /utf-8 /GS- /Zl` PASS；
- `lld-link /machine:x86 /dll /nodefaultlib /entry:DllMain@12` PASS；
- ASI：PE32 / Intel i386，140,800 bytes；
- ASI SHA-256：`82da61da9aeb3edce7c1e6797949034e93e5a26d8fcfcf0439e9472fba8a6c6e`。

Windows 正式构建仍运行 `源码/build.bat`。不要把容器静态/结构验证写成用户实机 PASS。

### 1.8 当前实机待验与阻塞项

只新增两项：

1. **state3 治疗目标选择**：完整验证初始中心、左右、A、B、键鼠/右摇杆接管、实际目标一致与 modal 不穿透；
2. **state8 焦点微调**：只看位置是否合适、hover 动画是否仍不出现。

如果 refactor28 实机通过，state3 治疗目标即可冻结，之后再处理主 Interface 其它遗留手柄缺口。不要在未收到用户裁决前擅自继续下一页面。

### 1.9 已知失败路线/踩坑（必须继续记住）

- 不做通用“最近两个 HitTest”选择框：refactor8 已证明会灾难性误吞 Title/Battle/Save；
- 不直接写游戏业务 state 代替原版 ButtonEvent；Battle、SaveSlot、五内历史均证明会造成视觉/逻辑错位；
- 不把外观相似的页面强行共用业务 Controller；只抽真正相同的低层语义/算法；
- Pointer takeover 只能撤视觉/导航所有权，不能随便清已排给游戏线程的一次性业务事务；
- 已实机 PASS 模块默认冻结，新页面先修自己的 Adapter；没有共性证据不得扩大修改面。

### 1.10 下一步

当前立即下一步不是继续写新功能，而是等待用户验收 refactor28。若 PASS，更新文档把治疗目标冻结，再根据用户指出的下一个主 Interface 遗留交互逐项逆向。

---

## 历史累计记录（只作因果证据）

> 以下内容保留各历史版本当时的设计、失败方案、地址和测试记录。里面出现的“当前/待验/下一步”不再代表 refactor28 现行状态。

## refactor27 历史独立接档摘要（当时现行）

### 0.1 项目身份与最终目标

本工程只做《幽城幻剑录》**现代手柄操控模组**。目标是在台湾第三版原版 RPG.exe 上，让玩家只使用现代手柄即可自然完成标题、读档、探索、剧情、战斗、主 Interface 及后续商店/炼化等全部交互。BugFix、随时存档、免 CD、宽屏、Mod Loader、Godot 兼容引擎等全部属于其它独立模块；不要再把它们并进本 ASI。

内部架构的长期原则：

```text
SDL3 物理输入
  -> PadInput 统一 Press/Release/Hold/Axis
  -> InputRouter 通用语义
  -> Context 的 PASS / MERGE / OVERRIDE / CONSUME
  -> 当前页面/区域 Adapter
  -> UiBridge / 原版 ButtonEvent、HitTest、状态机与生命周期
```

通用层提供“确认/取消/导航/大类/子类”等能力；页面专属层可以合并或覆盖通用行为。不能为了架构漂亮而用通用实现取代原版特殊协议。

### 0.2 当前用户实机 PASS / 冻结基线

截至本版，用户已明确验收通过、后续不得无证据改写的内容包括：

- 标题菜单、标题读档与共享 SaveSlot 稳定槽位导航；
- 地图 Y 打开主 Interface、Interface Shell 大类/返回生命周期；
- state2 法宝/道具（含弃置/分发确认）；
- state3 绝学/法术；
- state4 及身；
- state5 五内（含 X/LT/RT/Y、原版手形焦点、瞬时按钮动画）；
- state6 阵形；
- Battle 顶层/子菜单视觉、Target、不可交互项 A 不锁死；
- 普通剧情 A 两阶段：逐字阶段补全、整句阶段推进；
- R3/Back 精细鼠标、Start=ESC 电影跳过等此前已封存能力；
- SDL3 拔插重连与控制器热切换。

【refactor27 当时状态】本轮新增的 state7/state8 **尚未用户实机验收**，只能称“首版候选”；后续已经由用户实机 PASS。

### 0.3 refactor27 本轮改动

#### state7 天书

原版静态链证明 `0x434E93 -> 0x4245B0`，结果写 `Interface+0x654`，所以天书槽位就是现有 SaveSlot。实现：

- 新 `interface_tome.c/.h` 只做 state7 页面适配、modal 判定和 pointer takeover；
- 槽位层复用 `save_slot.c` 已通过的 ↑↓连续/跨页、←→翻页、A/B；
- 原版三项窗口 Update=`0x4262C0`，三个按钮位于 `+0x58C/+0x590/+0x594`，当前项 `+0x598`，嵌套 Yes/No=`+0x5B4`；
- Hook `0x426365 -> 0x431310`（HitTest）和 `0x426387 -> 0x431380`（Event）；
- 屏幕上→下对应真实 index 2→1→0；index0 是取消，index1/2 会打开原版 Yes/No；
- Yes/No 直接交给已通过的 ConfirmDialog；
- 不实现 Save 文件 I/O、不直接写 SaveSlot 或游戏状态。

为了保护标题读档，历史五个 SaveSlot Hook 仍由 `SaveSlot_InstallHooks()` 安装；新增三项窗口只由 `SaveSlot_InstallActionHooks()` 安装。state7 协议失败时按页面 capability fail-closed，不应拖垮标题 SaveSlot。

#### state8 机能

原版构造 `0x434EC9 -> 0x428510`，对象在 `Interface+0x658`；Update=`0x428950`。新 `interface_options.c/.h` 管理三行：音乐、音效、空明流转。

真实字段/按钮：

```text
+0x57C 音乐值（只读）
+0x580 音效值（只读）
+0x590 空明流转 Button
+0x594 音乐 -
+0x598 音乐 +
+0x59C 音效 -
+0x5A0 音效 +
+0x5A8 原版 Yes/No
```

真实 Event CALL：`0x42898C`、`0x4289BF`、`0x4289F3`、`0x428A26`、`0x428A5A`，全部目标 `0x431380`。输入为 ↑↓选行；音乐/音效 LT/RT 减加；空明流转 A；B/LB/RB 无弹窗时交 Shell。

视觉焦点复用原版手形鼠标，但不模拟 hover：取当前行最右 Button 矩形，手形 X 放在右缘外约 12px，Y 对齐垂直中心。这样音乐/音效没有额外按钮动画，空明流转也保持同一视觉规则。实体鼠标/右摇杆接管时让路。

### 0.4 当前源码模块

核心边界：

- `plugin.c`：生命周期、installer 调度、worker 总调度；
- `runtime.c/.h` + `game_addresses.h`：版本/地址/协议/capability；
- `pad_input.c/.h`：SDL3 物理输入、拔插与热切换；
- `input_router.c/.h`：通用输入语义、Context、PASS/MERGE/OVERRIDE/CONSUME；
- `ui_bridge.c/.h`：一次性原版 ButtonEvent pending + owner；
- `cursor.c/.h`：实体指针/控制器指针所有权与原版手形显示；
- `battle.c/.h`：冻结 Battle；
- `frontend.c/.h`：标题等前端；
- `save_slot.c/.h`：共享存读档 UI Controller + refactor27 原版 SaveAction 子窗口；
- `interface_shell.c/.h`：主 Interface 根导航；
- `interface_items/skills/equipment/inner_stats/formation.c/.h`：state2..6 已通过页面；
- `interface_tome.c/.h`：state7 新候选；
- `interface_options.c/.h`：state8 新候选；
- `confirm_dialog.c/.h`：双按钮 Yes/No；
- `dialogue_input.c/.h`：普通剧情 A；
- `exploration.c/.h`、`movie_skip.c/.h`、`spatial_neighbor.c/.h`：各自独立能力。

源码文件名均英文/ASCII；代码内部继续使用面向初学者的详细简体中文注释。

### 0.5 EXE 样本与地址兼容策略

当前支持白名单有两个原版样本：

```text
b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf  历史精确目标样本
8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f  RPG.exe.org 完全未修改原版可信样本
```

用户已裁决两者均可 PASS。工具仍逐能力检查 CALL 目标、短机器码与字段协议；未知 SHA 或某 capability 地址不匹配时 fail-closed，不把研究版/其它补丁行为当原版。

state7 新签名：

```text
0x4262C0: 53 55 56 8B F1 B9 03 00 00 00 8D AE 8C 05 00 00
```

state8 新签名：

```text
0x428950: 53 56 8B F1 32 DB 8B 8E A8 05 00 00 8A 81 79 05 00 00
```

### 0.6 成功方案、失败方案与踩坑

**当前成功原则：**

- 复用原版真实 ButtonEvent/HitTest/生命周期，不直接改业务数据；
- 已通过的页面按 Adapter 冻结，新页面独立扩展；
- 真正同构的 SaveSlot 才共享，未知 UI 不提前抽象；
- modal 最深层优先，输入必须有明确 owner/consume；
- 手柄焦点视觉可以使用原版鼠标，但逻辑焦点必须保存在插件，实体指针接管要让路；
- 兼容性使用 SHA 白名单 + capability 协议双保险。

**禁止恢复的失败路线：**

- dev22 worker 手工构造 Interface；
- refactor8“最近两个 HitTest 就是通用 Yes/No”的启发式；
- refactor12 在具体页面未闭合前建立万能 SelectionUI/Quantity；
- 用直接写 state/value 取代原版 ButtonEvent；
- 为了让新页面工作去重写已 PASS 的 Battle/SaveSlot/state2..6；
- 用鼠标 hover 位置本身当业务逻辑焦点；
- 把静态检查 PASS 写成实机 PASS。

**本阶段新踩坑预防：** SaveAction 内存按钮 index 与屏幕上下顺序相反，必须按 2→1→0 映射；state8 手形若落进 Button 矩形会触发原版 hover，因此必须放到按钮外侧。

### 0.7 当前测试结论

机器侧已完成：

- `refactor_check.py --root <包> --exe RPG.exe.org`：**80 PASS / 0 FAIL**；
- 22/22 独立 C：x86 clang-cl `/W4 /WX /utf-8` PASS；
- lld-link PE32/i386 DLL PASS；
- `编译内容/` 只保留 ASI + INI；
- ASI SHA-256：`7fc7ac257331feb3d580553e3ce01381504a40ccc59525a1136b77e0387c1a4a`。

实机侧：旧 PASS 项继续按用户裁决冻结；state7/state8 尚未实机，不能提前封版。

### 0.8 当前阻塞项与下一步

当前没有静态/构建阻塞。唯一下一步是用户实机联合验收 state7/state8，并做一次旧功能快速回归：

1. 天书：槽位 → 三项窗口 → 存/读二次 Yes/No → B/取消/大类 ownership；
2. 机能：三行手形位置 → LT/RT → 边界值 → 空明流转 Yes/No → pointer takeover；
3. 快速回归 state2..6、Battle、Title/SaveSlot、剧情 A、SDL 拔插/热切换。

若本轮通过，再继续按“只做全游戏手柄覆盖”的路线调查剩余商店、炼化、数量框、特殊剧情 UI；不得趁机加入其它 Remaster 功能。

---

## 以下为 refactor26e 及更早累计历史接档

## H0. refactor26e 历史权威结论（当时）

### 0.1 已通过并冻结

- **state2 法宝/道具弃置/分发确定/取消：用户 PASS。** refactor26c 恢复 `ConfirmDialog_InstallHooks()`，第二类 `0x4276F0` 低层 CALL 接回 UiBridge；state2 页面 Adapter 继续独占焦点/A/B。
- **state5 五内整页：用户 PASS。** refactor26d 已确认 X/LT/RT/Y 功能、手形鼠标焦点/右上偏移和加减/蕴魂按钮瞬时动画全部正常；后续冻结视觉与动画生命周期。
- **Battle 不可交互项 A、Battle Target 旧成熟方向、SDL3 拔插重连、state3 绝学/法术既有业务、state4 及身主体**继续既有 PASS 基线。

### H0.2 refactor26e 当时回归：普通剧情 A 无反应（后续已 PASS）

用户在 refactor26d 实机发现剧情推进按 A 完全无响应。按用户要求，没有只比较最近一版，而是纵向对照 refactor21/refactor22/refactor24→refactor25→refactor26d。结论：

1. `dialogue_input.c/.h` 从 refactor24 到 refactor26e **字节级完全一致**；
2. worker 主循环中的 `DialogueInput_Update()` 从 refactor24 到当前一直存在，且相对调度顺序没有改变；
3. refactor24 的 `plugin_install_all_hooks()` 明确调用 `DialogueInput_InstallHook()`；
4. refactor25 大重构时同时漏掉 `ConfirmDialog_InstallHooks()` 与 `DialogueInput_InstallHook()`；
5. refactor26c 为法宝确认恢复了前者，却没有恢复后者；
6. 因此 RPG.exe 两处局部 GetAsyncKeyState 包装入口根本没有被 patch，剧情 A 算法虽然存在却无法进入原版输入链。

refactor26e 只恢复 `DialogueInput_InstallHook()`，不重写 Dialogue 算法。并且把安装相对顺序也恢复成 refactor24 已验证的：

```text
InterfaceFormation_InstallHooks
Exploration_InstallHooks
ConfirmDialog_InstallHooks
DialogueInput_InstallHook
```

### 0.3 与 refactor24 的“恢复完整性”机械证明

用户明确要求恢复后与 r24 对照确认完整。本版 `refactor_check.py` 新增两道硬门槛：

- **14/14 业务 Hook installer 必须全部存在且无重复；只约束真实依赖，不机械绑定无关模块顺序；**
- **14/14 worker 关键阶段必须全部存在，并满足实际输入消费/Context 优先级偏序。**

当前预检两项均 PASS。源码 SHA 裁决中，以下底层稳定模块与 r24 字节级一致：

`battle.c/.h`、`confirm_dialog.c/.h`、`cursor.c/.h`、`dialogue_input.c/.h`、`exploration.c/.h`、`frontend.c/.h`、`movie_skip.c/.h`、`pad_input.c/.h`、`save_slot.c/.h`、`spatial_neighbor.c/.h`，以及若干稳定头文件。

与 r24 不同的源码不是“恢复不完整”，而是 r24 之后已经明确发生的合法增量：

- `input_router.*`：refactor26 左摇杆 50% 角色切换、D-Pad 页面分工；
- `interface_items.* / interface_skills.c / interface_equipment.c`：state2/3/4 新分页与页面弹窗修复；
- `interface_inner_stats.*`：已用户 PASS 的五内鼠标/动画完整实现；
- `interface_formation.c`：r24 后阵形输入所有权增量；
- `interface_shell.*`：角色切换改左摇杆；
- `runtime.c / ui_bridge.c / game_addresses.h`：上述后续协议与 state5/ConfirmDialog 所需低层能力；
- `plugin.c`：版本说明与本轮恢复 Dialogue installer。

这些后续合法增量不会为了追求“与 r24 整包 SHA 相同”而回滚。真正要求恢复的是**r24 已通过底层安装/调度能力没有漏项**。

### H0.4 refactor26e 当时验收边界（后续已完成）

本轮唯一新增重点：普通剧情 A。正确语义仍是 refactor11 已通过的两阶段：

1. 逐字显示中按 A：只补全当前句；
2. 本句完整后再次按 A：推进下一句；
3. 询问框活动时 Dialogue 必须让出 A，避免同一按键既确认弹窗又推进文字。

【历史】refactor26e 当时暂缓 SDL 热切换判断；其后用户已实机确认控制器热切换 PASS，refactor27 继续冻结 `pad_input.c/.h`。

## 0.4 refactor26c 以前的当前结论已转历史

以下 0.x/后续历史章节保留因果记录；凡与本节冲突，以 refactor26d 本节为准。

## 0.5 refactor26a/26c 历史补充 法宝/道具“弃置/分发”确定/取消：前两次热修均未解决，本轮找到安装链根因

refactor26a 只修了 state2 popup 的输入 Layer：D-Pad 左右从错误的 COMMON 改成 OVERLAY。这条路由修复本身正确，但用户在 refactor26b 继续确认“确定/取消还是没修复”，说明问题不止输入路由。

重新对照 refactor21 与 refactor25/26/26a/26b 的 `plugin.c` 后得到确定差异：

```text
refactor21：
InterfaceItems_InstallHooks()
InterfaceSkills_InstallHooks()
Exploration_InstallHooks() 前
    -> ConfirmDialog_InstallHooks()  存在

refactor25～26b：
页面 Adapter 都安装
    -> ConfirmDialog_InstallHooks() 被大重构漏掉
```

这一步非常关键，因为 state2 的弃置/分发确认对象属于第二类双按钮 Update `0x4276F0`。它内部的：

```text
0x427800 -> 0x431310  Yes HitTest
0x427819 -> 0x431310  No HitTest
0x42782E -> 0x431380  Yes Event
0x42784F -> 0x431380  No Event
```

只有 `ConfirmDialog_InstallHooks() -> UiBridge_InstallConfirmDialogHooks()` 才会接入 UiBridge。缺这一步时，`InterfaceItems` 虽然能收到 A/B/方向并排出 pending，但原版第二类 popup 扫描永远不会进入 UiBridge 去消费 pending。

refactor26c 恢复 refactor21 已存在的安装调用，但**不恢复失败的“全局万能 Yes/No”架构**：`ConfirmDialog_Update/FilterHit` 在 `InterfaceItems_AnyPopupActive()` 或 `InterfaceSkills_AnyPopupActive()` 时继续让路，state2/state3 的业务焦点、A/B、视觉仍由各自页面 Adapter 独占。恢复的只是第二类原版 CALL 接线。

### 0.2 五内：鼠标焦点视觉已用户 PASS/封版；仅按钮动画仍 FAIL

用户最新裁决必须拆成两项：

- **手形鼠标焦点位置/显示：PASS，封版。** refactor26b 的 `inner_focus_anchor()` + `inner_show_node_focus()` 以及 `(+21,-36)` 右上偏移不再修改；静态工具对这段完整源码片段做固定 SHA。
- **LT/RT/Y 按钮动画：FAIL。** 正确行为与真实鼠标一致：只有实际点击对应按钮时短暂闪一下，随后立即恢复；不能持续闪，也不能累积历史选择动画。

refactor26b 虽然同步了 Button `+0x2C/+0x28`，但 pulse 推进仍被包在：

```text
controller_focus_visible && Cursor_ControllerOwnsPointer()
```

内部，而且 `InterfaceInnerStats_OnPointerTakeover()` 会直接清 `pulse_button/pulse_phase`。因此如果原版已经执行：

```text
0x44066E -> 0x431400 -> code 2
0x440678 -> 0x43E6F0(1)   // 按下动画已开始
```

随后焦点鼠标被隐藏/指针接管，release 就可能永远不到：

```text
0x4406A8 -> 0x431380 -> code 2
0x4406B2 -> 0x43E6A0(0)   // 应恢复动画
```

refactor26c 因此把“按钮点击事务”从“焦点鼠标视觉所有权”中拆开：一旦 pulse 开始，state5 自己的 11 Button 扫描优先完成 press→release；指针接管只隐藏已封版焦点鼠标，不取消 pulse；500ms watchdog 只有 phase1（尚未真正按下）可以取消，phase2/3 必须整理成 release 并继续等待原版 `0x431380` 收尾。

### 0.3 当前机器验证边界

- 20/20 独立 C：clang-cl i686 MSVC ABI、`/W4 /WX` 等价严格编译 PASS；
- lld-link PE32/i386 PASS；
- refactor26c 当时 ASI：128,000 bytes，SHA-256 `d31d1bb2770bd4ea903adf152ab3eb911ba7a1315b489f8229c8905631a56c72`；
- 无 EXE 静态检查：**59 PASS / 0 FAIL**；
- 用户完全未修改原版第二样本：**70 PASS / 1 FAIL**，唯一 FAIL 为“不是历史正式精确 SHA”；
- 第二样本 ConfirmDialog：**2/2 vtable + 第二类 4/4 CALL PASS**；
- state2～6、Battle、电影、对话等机器协议均 PASS。

静态检查只能证明代码/地址/事务结构成立，**不能提前把本轮两个实机问题写成 PASS**。

## 0.1 refactor26a 历史热修结论

用户发现 refactor26 大改后“物品→弃置”的 Yes/No 手柄选择丢失。完整对比 refactor25 与 refactor26 后确认：原版 popup 与 state2 专属 popup Adapter 都没有被删除；真正遗漏是**输入层所有权改变后，业务读取层没有同步**。

```text
refactor25：state2 LEFT/RIGHT = PASS     → COMMON 可读
refactor26：state2 LEFT/RIGHT = OVERRIDE → 只有 OVERLAY 可读
r26 popup：仍读 COMMON                  → 左右永远不可达
refactor26a：popup 改读 OVERLAY          → 与页面策略重新一致
```

本轮继续遵守“最深弹窗 → 当前页面 Adapter → Interface Shell → 底层”：popup 存活时左右只切 Yes/No，不能穿透触发 state2 翻页。没有重新启用 refactor8/refactor12 已失败的全局通用选择框抽象。

完整 r25→r26 差分与全源码 InputRouter 调用审计还确认：Battle/Target、PadInput 热插拔、Frontend、SaveSlot、ConfirmDialog、Dialogue、Exploration、Movie、Cursor、Runtime 等关键模块在 r25/r26 间字节级未变；所有发生变化的 `PressedOn/DownOn/ReleasedOn` 调用中，只有 state2 popup 左/右两条出现 Policy/Layer 不一致。refactor26a 已将这项机械检查写入 `工具/refactor_check.py`。

当前验证：无 EXE **57 PASS / 0 FAIL**；第二原版样本 **68 PASS / 1 FAIL**，唯一 FAIL 为正式精确 SHA。新弹窗修复仍必须用户实机验收。

## 1. 当前版本定位

当前开发包：**v0.3-refactor26e：五内整页与法宝确认均已用户 PASS/冻结；本版只恢复 refactor25 大重构遗漏的 DialogueInput 安装链，并用 refactor24 做能力完整、真实依赖和输入消费偏序审查。** 当前唯一新增待验重点是普通剧情 A 两阶段恢复。

**稳定主菜单生命周期基线：refactor19 已由用户实机验收通过。** 地图 Y、LB/RB、根层 B、动画期输入缓冲与重复日志抑制均以 r19 为准。refactor26 继续复用同一真实角色 ButtonEvent，但触发方式统一改为 state1～5 的左摇杆水平超过50%单次方向沿。

版本关系：

```text
dev3 → dev20 历史功能/逆向基线
  ↓
refactor4
  └─ dev20 等价重构范围实机封版
  ↓
refactor7
  └─ 活动电影 Start=ESC 实机 PASS
  ↓
refactor11
  └─ 普通对话 A 两阶段实机 PASS
  ↓
refactor12
  └─ 公共 SelectionUI/Quantity 架构被用户否决，代码回滚、知识保留
  ↓
refactor15
  └─ R3 阈值即时右键单击实机 PASS
  ↓
refactor16
  └─ Back=Shift、双灵敏度、Battle左右分页等用户整体确认“没问题”
  ↓
refactor17
  ├─ Back 精细鼠标会话
  └─ 主 Interface 第一阶段：Y + LB/RB + state1~4角色 + 根层B
       实机：Y FAIL；Shell可用但明显迟钝
  ↓
refactor18
  ├─ Y 改到 0x40CC73 -> 0x406F20：实机仍 FAIL
  └─ UiBridge pending 增 owner：实机日志证明每次 Shell 请求都能被 Event 消费，PASS
  ↓
refactor19（已实机 PASS）
  ├─ Y 不发送 Space；基线 EXE 中复用 Space 最终业务事件 0x40B230(1)
  ├─ Shell 动画期新按下沿进入最多8项语义队列，+0x590>8 后提交
  ├─ B 清除尚未提交的旧导航队列并优先退出
  └─ Runtime 对完全相同日志词条做多槽2秒连续抑制
  ↓
refactor20
  ├─ Battle 禁用项 A 逃生修复
  ├─ state1 状态页封存
  └─ state2 法宝/道具首版候选
  ↓
refactor20a
  └─ 删除 0x40B230 0x23 字节整段硬签名；改为 Space 业务特征 + 动态 CALL 目标
```

**当前已确认稳定事实：**refactor4、refactor7、refactor11、refactor15、refactor16 的对应功能已有用户实机确认；refactor17 的 Back 精细鼠标会话未在后续反馈中报告回归；refactor18 已确认 UiBridge owner 隔离有效，但地图 Y 路线失败。refactor19 的 Y、动画期缓冲与全局日志去重已由用户实机验收通过。refactor20a 已至少在当前实机环境中不再阻断主菜单/法宝页；refactor20 的法宝主体已被用户确认可用，但 LT/RT 反向与危险确认错选在 r21 修复；Battle 禁用项 A 修复仍待明确实机 PASS。

## 2. 精确目标 EXE

目标：台湾第三版项目使用的精确 `RPG.exe`。

SHA-256：

`b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf`

PE：32 位 / i386。绝对地址体系仍以这个精确 EXE 为基线；dev20/战斗/SaveSlot/页面 Hook 的实际改写点继续 fail-closed。**唯一放宽的是主 Interface 的 Space 业务入口：不再把未被本插件改写的 `0x40B230` 0x23 字节函数体当硬签名，而改为业务特征 + 动态 CALL 目标。**

## 3. 当前源码模块

现行 15 个独立 C 编译单元：

```text
runtime.c
pad_input.c
input_router.c
movie_skip.c
confirm_dialog.c
dialogue_input.c
cursor.c
exploration.c
ui_bridge.c
interface_shell.c
interface_items.c
save_slot.c
frontend.c
battle.c
plugin.c
```

职责：

### `runtime`

- Win32 API 动态解析；
- INI；
- UTF-8 中文日志；
- tick/time；
- 精确协议校验；
- CALL/IAT patch；
- RPG.exe 地址合法性检查。

### `pad_input`

- SDL3 物理手柄采样；
- 不解释业务语义。

### `input_router`

- 把物理按钮映射成语义动作；
- Context；
- `PASS / MERGE / OVERRIDE / CONSUME`；
- COMMON / OVERLAY；
- 每 tick 消费；
- Back 固定映射 `INPUT_MODIFIER_SHIFT`。

### `cursor`

- 实体鼠标/手柄 Cursor 所有权；
- 右摇杆鼠标；
- Back 精细鼠标会话；
- R3 左/右键复合语义；
- Battle Target 光标视觉。

### `exploration`

继续负责探索移动/走跑；refactor19 在既有 `0x409597 -> 0x408A70` 游戏线程 Hook 里增加安全点消费地图 Y。refactor20a 调用目标不再写死 `0x40B230`：Runtime 从原版 Space CALL 现场解析当前业务函数并以 `action=1` 调用。它不构造 Interface，也不模拟键盘。

### `ui_bridge`

- 真实 `0x431380` ButtonEvent 一次性注入；
- HitTest 分发；
- refactor18 起 pending = `button + code + owner`；
- owner 负责生命周期清理隔离，不改变 RPG.exe 真实 Event 协议。

### `battle / frontend / save_slot`

分别持有自己的业务状态与游戏线程协议。refactor18 只把 Battle/Save 的 UiBridge 请求迁到显式 owner，不重写业务。

### `confirm_dialog`

仅保留 refactor10/11 历史双按钮候选和隔离护栏。它**不是**“全游戏通用 Yes/No 成功方案”，以后具体菜单弹窗仍逐窗口实现。

### `dialogue_input`

refactor11 已验收普通对话两阶段 A，冻结。

### `interface_shell`

只负责：

- worker 捕获普通地图 Y，并在 Exploration 游戏线程复用原版 Space 当前业务 CALL 目标(action=1)；基线目标为 `0x40B230`；
- LB/RB 八大类；
- state1～5 左摇杆水平超过50%的单次角色切换；
- 根层 B 退出；
- 原版 Interface 动画门期间的 Shell 动作语义缓冲。

不提前实现页面业务，不发送 Space，不直接写 Interface state/role/close。

## 4. 输入架构与所有权

页面/弹窗优先级：

```text
最深层弹窗
  ↓ 未消费
当前具体页面 Adapter
  ↓ 未消费
Interface Shell
  ↓ 未消费
底层输入
```

长期原则：**具体菜单先独立做通；全部完成后再看真实重复代码是否值得抽公共层。** 这是 refactor12 被否决后形成的硬规则。

### Back

Back 永久作为全插件 Shift/修饰键：

```text
PAD_BACK → INPUT_MODIFIER_SHIFT
```

- 单独按 Back 无游戏业务；
- 单独按 Back 不应改变鼠标/手柄所有权；
- 后续扩展组合键优先使用 `Back + 其它键`；
- 页面 adapter 不得私自把 Back 变普通键。

## 5. 已实机通过：Battle / Target / Frontend / SaveSlot 主线

refactor4 是 dev20 等价重构范围的封版点。核心经验：

- 不用屏幕坐标模拟战斗 Button；
- 手柄业务在原版 ButtonEvent/HitTest 调用点消费；
- UI 逻辑状态和视觉动画必须分别尊重；
- 不能用 timeout 强行越过原版动画。

### Battle 顶层视觉

原版 `0x41C380` 动画期间会走 `0x41C3B2 -> 0x41C470`，每帧把 marker `+0x24` 写到第1按钮 `+0x38`。refactor3 Hook 这条唯一 CALL：先执行原版动画，再恢复当前真实焦点 marker Y。用户实机确认顶层残留修复。

### CMD1/CMD2 返回视觉

两个列表有自己的动画和默认鼠标 warp：

- CMD1 默认 warp CALL：`0x4188CF -> 0x43DF30`；
- CMD2 默认 warp CALL：`0x41A65B -> 0x43DF30`。

refactor4 只在手柄拥有战斗导航时抑制默认 warp，键鼠不变，并在动画阶段恢复真实行视觉。用户实机 PASS。

### Target

目标合法集合服从原版 selector side mode/grid；不得越阵营。目标视觉用真实屏幕 Cursor 位置，不只修改内部 MouseManager 坐标。

### Frontend / SaveSlot

标题根菜单方向/A、标题读档 SaveSlot 已通过。SaveSlot 原生选中动画必须保留：动画倒计时、目标行、selected-row、refresh 四步；不能只写 selected-row。

## 6. 已实机通过：Start=ESC

- `GLOBAL_MOVIE_OBJECT = 0x46F390`；
- 活动 flag：对象 `+0x0A`；
- `0x4018AB` 活动判定；
- `0x4018EF` 原版 ESC 关闭条件。

refactor5/6 的标题历史/资源分类/BinkOpen 复杂方案失败并永久放弃。refactor7 简化为：原版电影 active 时 Start 新按下一次就等价原版 ESC 一次。用户 PASS。

开场电影曾有 Loader 组合卡死，用户确认来自 `binkw32.dll / binkw32hooked`，不是 Start=ESC。

## 7. 已实机通过：普通对话 A 两阶段

关键地址：

- `0x46F670`：公共消息活动槽；
- `0x46F684`：当前句总字节；
- `0x46F688`：已显示字节；
- `0x4041D7`：逐字阶段局部 `GetAsyncKeyState` 装载；
- `0x40447E`：整句等待阶段局部装载；
- `0x4046F0`：下一消息推进。

正确语义：

```text
visible != total
  → A 只让原版补全当前句

visible == total
  → 第一阶段不消费
  → 第二阶段 A 走原版推进到下一句
```

用户已验收。未来若做“文字瞬间全部显示”，应让原版 visible/total 状态自然进入完整状态，这样 A 会直接下一句；不要只改渲染而让内部状态仍认为正在逐字。

## 8. 已实机通过：R3 复合鼠标

### 读取桥

原版多处轮询 `USER32!GetKeyState`（IAT `0x4601A8`）。因此插件同时保留：

- Windows `mouse_event`；
- RPG.exe `GetKeyState` IAT 叠加。

左右单击脉冲约 48ms，避免 8ms 过短被游戏线程漏采。

### 当前正确状态机

默认：

```ini
R3RightClickHoldMs=1000
```

范围 250～5000ms。

```text
R3 按下
  ↓
阈值前松开
  → LEFTDOWN
  → ~48ms LEFTUP

达到阈值
  → 立即 RIGHTDOWN
  → ~48ms 自动 RIGHTUP
  → LONG_LOCKED
  → R3继续按住时忽略其它R3语义
  → 最终物理松开只解除锁定，不再发鼠标事件
```

refactor14 把 RIGHTUP 绑物理松开，实机 FAIL；refactor15 解耦后用户 PASS。

## 9. Back/Shift 与右摇杆双灵敏度

INI：

```ini
RightStickSensitivityPercent=100
RightStickSlowSensitivityPercent=30
R3RightClickHoldMs=1000
```

灵敏度范围 1～300。低速使用百分之一像素余量累计，避免整数缩放后轻推不动。

refactor17 加入 `back_precision_mouse_session`：

```text
Back 单独按住 → 不进入鼠标模式
Back + 右摇杆真正移动一次 → 开始精细鼠标会话
摇杆回中、Back仍按住 → 会话继续
Back松开 → 会话结束
```

该逻辑是产品要求；r18 没收到它的新失败报告，但本轮仍建议快速回归。

## 10. Battle CMD1/CMD2 左右翻页

### CMD1

- `SUB1_ROW_PANEL = +0x618`；
- `SUB1_TOTAL_PAGES = +0x600`；
- `SUB1_CURRENT_PAGE = +0x604`；
- `0x41829C -> 0x431380` 上一页；
- `0x4182E1 -> 0x431380` 下一页；
- Button：panel `+0x5DC / +0x5E0`。

### CMD2

- `SUB2_ROW_PANEL = +0x77C`；
- `SUB2_CURRENT_PAGE = +0x580`；
- `SUB2_TOTAL_PAGES = +0x584`；
- `0x419FB2 -> 0x431380` 上一页；
- `0x419FDB -> 0x431380` 下一页；
- Button：panel `+0x5FC / +0x600`。

方向：`←上一页 / →下一页`。继续复用原版 Event + pending-page，禁止直接写页码。refactor16 已把 InputRouter 左右修为 OVERRIDE，用户整体确认 r16 没问题。

## 11. 选择框/弹窗架构结论

### refactor8

“最近看到两只 Button HitTest 就认为当前是 Yes/No”导致标题、SaveSlot、Battle 被误吞，灾难性 FAIL。

### refactor9

加强隔离后稳定业务恢复，但新询问框没有触发。证明“隔离优先”正确，但少量 open CALL 不能代表所有窗口。

### refactor10/11

双按钮 vtable 研究保留：

- `0x460C08` / Update `0x4272C0`；
- `0x460C28` / Update `0x4276F0`；
- 相邻还有 `0x427E80`、`0x428950` 等不同复合类型。

### refactor12

尝试抽公共 SelectionUI / Quantity。用户实机/观察确认：视觉相似窗口内部控制未必相同，公共覆盖范围非常有限。正式决定：**代码回滚到稳定基线，知识保留；每个菜单独立设计，全部完成后再横向抽公共部分。**

当前 `confirm_dialog` 只是历史隔离候选，不代表通用 Yes/No 已通过。

## 12. 主 Interface 原版对象

### 根对象

`GLOBAL_INTERFACE_UI = 0x008DED0C`。

字段：

- `+0x590` ready counter，`0x436330` 要求 `>8`；
- `+0x5BC` state；
- `+0x5C4` 当前角色；
- `+0x5EC` 角色数；
- `+0x5F8` 关闭生命周期；
- `+0x60C+i*4` 角色对象；
- `+0x620` 顶部导航；
- `+0x63C..+0x658` state1～8 页面。

顶部大类真实 Event：

- state1 `0x435BBA -> 0x431380`；
- state2 `0x435BD2 -> 0x431380`；
- state3 `0x435BEA -> 0x431380`；
- state4 `0x435C05 -> 0x431380`；
- state5 `0x435C20 -> 0x431380`；
- state6 `0x435C3B -> 0x431380`；
- state7 `0x435C56 -> 0x431380`；
- state8 `0x435C81 -> 0x431380`；
- exit `0x435C9C -> 0x431380`，原版 code=1；
- role `0x435E93 -> 0x431380`，code=2。

r17 实机已经证明：**在 Interface 已打开时，这组 LB/RB、角色、B 业务可以工作。** 所以 r18 不推翻这些地址，只修事件生命周期。

## 13. 地图 Y 入口三轮纠错

### 13.1 r17：特殊休整三项动作——FAIL

历史对象：

- Update `0x413850`；
- 第0项 `0x413933 -> 0x431380`；
- 后续确实存在 prepare/alloc/`0x434920` Interface ctor。

它属于**休整/特殊地图三项动作菜单**，第0项“調態”，不是普通探索通用菜单键入口。用户 r17 实测普通地图 Y 完全无法打开菜单。

### 13.2 r18：`0x40CC73 -> 0x406F20` 门控——仍 FAIL

r18 尝试在普通探索状态机中只叠加这一次门控返回，不写 state、不调 ctor。静态上路径自洽，但用户实机仍无法用 Y 打开菜单，而且没有命中 Y 入口日志。

因此 `0x40CC73` 只能作为状态机研究证据，不能继续标为“普通地图已确认菜单键入口”。

### 13.3 r19：直接复用原版 Space 后面的业务事件

用户提出：**可以取巧复用地图 Space 的事件，但不要发送 Space 按键。**

重新反汇编 `0x44A4D0`：

```text
0x44A4DF  cmp eax,0x20
0x44A4E2  jne 0x44A583
0x44A4E8  cmp [0x89F808],0
0x44A4EE  jne 0x44A583
0x44A4F4  cmp [0x468BF0],0
0x44A4FA  je 0x44A583
0x44A500  push 1
0x44A502  jmp 0x44A57B
0x44A57B  call 0x40B230
```

所以原版 Space 的业务动作可以明确表示为 `0x40B230(1)`。

`0x40B230` 自己还会重新检查：

- action 非0；
- `0x89F808==0`；
- `0x89F81E==0`；
- 调 `0x40B340`；
- `0x89F820!=0`。

r19 实现：

```text
worker：Y新按下 → map_y_pending=1
Exploration 游戏线程：
  先运行原版 0x408A70
  → InterfaceShell_OnExplorationGameThread()
  → 再检查 Context / 0x89F808 / 0x468BF0
  → Runtime_MapSpaceEventTarget() 取得原版 Space 当前 CALL 目标
  → 以 action=1 调用该目标
```

明确禁止：

- 发送 `VK_SPACE`；
- `PostMessage` Space；
- SendInput/keybd_event；
- worker 直接调用地图业务函数；
- 直接写 `0x89F818`；
- 手工分配/ctor Interface。

r19 还增加分层 Y 日志：捕获、Context变化、busy、mode=0、最终提交各自可见。

主 Interface r20a 协议护栏：**Space 业务特征 + 动态 CALL 目标 + 1/1 短锚点 + 10/10 ButtonEvent CALL**。

## 14. Shell “钝”的两层根因与 r19 缓冲

### 14.1 r17 的严重迟钝：UiBridge pending 被跨模块清理

r17 日志表现为很多“请求”后才偶尔一次 Event 消费。根因是 Battle 非活动分支每8ms全局 `UiBridge_ClearEvent()`，会把 Interface pending 一起删掉。

r18 引入 owner：

```text
pending = button + code + owner
owner = BATTLE / SAVE_SLOT / CONFIRM_DIALOG / INTERFACE
```

用户 r18 日志已证明这层修复有效：Shell 每次请求后都能紧跟对应 `[UI事件] ...已由原版按钮消费`。

### 14.2 r18 仍偏钝：原版动画门 + 插件丢动画期按键

精确原版 `0x436330`：

```text
cmp [ecx+0x590],8
setg al
ret
```

切换大类时原版会把 `+0x590` 置0；直到动画更新超过8才再次允许根导航。

原版门本身就是旧式 UI 的节奏限制。r18 又先检查 ready，再读取手柄新按下沿，所以动画期间按下会直接消失，进一步放大“钝”。

### 14.3 r19：不跳动画，先缓存明确输入

r19 增加最多8项的动作语义队列：

```text
LB/RB/左右/B 新按下
→ 立即保存 ACTION，不保存 Button 指针
→ 原版动画继续
→ +0x590>8
→ 重新按当前对象查真实 Button
→ 通过 UiBridge owner=INTERFACE 提交
```

B 优先：按 B 会清空尚未提交的旧导航动作，只留下退出请求。

这只能消除插件造成的“按了被吃掉”，不会缩短原版动画。如果实机确认提前按键都能被缓存但视觉仍太慢，下一阶段才研究是否安全缩短 Interface 动画。

### 14.4 r19 全局日志抑制

Runtime 使用64槽固定缓存记录完整 UTF-8 日志指纹。同一词条连续2秒内再次出现时不写；每次重复刷新计时，因此持续快速操作时整个连续阶段只保留第一次。多槽可以处理“RB请求/UI消费”这种交替重复。

日志去重只影响文本文件，不参与任何输入业务。

## 15. 主 Interface 产品规格

### Shell 默认（refactor26）

- 左摇杆水平超过50%：state1～5 上一/下一角色；一次推杆只触发一次，回中死区后才能再次触发；
- LB/RB：大类；
- LT/RT：页面存在子类型/数值/翻页时由页面 adapter 实现；
- D-Pad ←/→：不再承担 Shell 换人；需要分页的 state2/3/4 统一上一页/下一页；
- B：返回一层；根层 B 退出地图；
- Back：Shift。

页面专属输入优先。state6 阵形明确禁止左摇杆换人。

### 状态

只读为主；左摇杆>50%切角色，LB/RB大类，根B退出。

### 法宝 / 道具

- ↑/↓列表；
- LT/RT子类型；
- D-Pad ←/→上一页/下一页；
- 左摇杆>50%切角色；
- A主要动作；
- X：暂置=分发，其它=弃置。

### 绝学

- ↑/↓ 技能；
- LT/RT 子类型；
- ←/→ 角色；
- A 使用；
- 使用确认是页面专属窗口。

### 及身 / 装备

- X 循环：兵刃 → 护甲 → 饰物；
- ↑/↓ 候选装备；
- A 装备；
- LT/RT：上一页/下一页；
- D-Pad ←/→：上一页/下一页，与 LT/RT 共用同一原版分页事务；
- 左摇杆水平超过50%：切角色。

### 五内

- X 循环五个五内焦点；
- 用户提供的 `五内1～五内5` 鼠标 icon 是**目标效果示意**，不是原版 hover 证据；
- 插件逻辑焦点驱动原版鼠标 icon 到指定位置；
- LT/RT：当前五内减/加；
- Y：“蕴魂”确认；
- D-Pad 左右不承担加减/换人；
- 左摇杆水平超过50%：切角色。

### 阵形

- ←/→ 同横排左右；
- ↑/↓ 上下/前后列；
- A 确认；
- 同时修 Battle Target 当前二维方向算法；
- 可共享纯几何邻居算法，但业务模块分开。

### 天书

复用已经稳定的 SaveSlot：

- ↑/↓ 槽；
- ←/→ 翻页；
- A 弹“存档/读档/取消”；
- 三项窗口 ↑/↓、A、B；
- 不复制第二套 SaveSlot。

### 机能

用户设置页截图中的鼠标 icon 同样是目标视觉：

- ↑/↓：音乐、音效、空明流转；
- 音乐/音效：LT/RT 调节；
- 空明流转 A：页面专属 Yes/No；
- A 选择、B 返回；
- 机能左右不切角色。

完整规格另见《主菜单手柄交互设计.md》。

## 16. 已确认失败/踩坑总表

- 战斗坐标模拟 Button：失败；
- SaveSlot 只写 selected-row：失败；
- hover 代替 selected 动画：失败；
- Target 只写内部坐标、不真实 SetCursorPos：失败；
- Target 不按 side mode 过滤：失败；
- refactor5/6 复杂电影资源/标题历史分类：失败；
- refactor8 HitTest 时间窗通用询问框：灾难性失败；
- refactor9 少数 open CALL 代表所有询问框：不成立；
- refactor12 先抽 SelectionUI/Quantity：架构被否决；
- R3 阈值 RIGHTDOWN、物理 release RIGHTUP：用户实机失败；
- Battle 只写业务分页、不修 InputRouter：假功能；
- dev22 worker 手工 new/ctor Interface：失败并永久禁止；
- dev24/r17 把 `0x413933` 特殊休整菜单当普通入口：r17 实机 Y FAIL；
- 用高 repeat/让用户多按几次掩盖 UiBridge pending 被跨模块清除：禁止。

## 17. 当前实机状态

### 已 PASS

- refactor4 dev20 等价封版范围；
- refactor7 Start=ESC；
- refactor11 普通对话 A 两阶段；
- refactor15 R3 阈值即时右键单击；
- refactor16 用户整体确认“没问题”：Back=Shift、双灵敏度、R3默认1秒、Battle左右分页等。

### 已 FAIL / 已否决

- refactor8 通用选择框启发式；
- refactor12 预先公共化 SelectionUI/Quantity；
- refactor14 R3 RIGHTUP 绑物理 release；
- refactor17 普通地图 Y 特殊对象路线；
- refactor18 普通地图 Y `0x40CC73 -> 0x406F20` 路线。

### r18 已确认有效部分

- 手工打开 Interface 后 LB/RB 有效；
- state1～4 左右角色有效；
- 根层 B 有效；
- UiBridge owner 隔离后，一次 Shell 请求稳定对应一次原版 Event 消费；
- 但整体手感仍偏钝。

### r19 已验收

- 地图 Y → 原版 Space 当前业务 CALL 目标(action=1)；基线目标 `0x40B230`；
- 动画期间 Shell 新按下沿缓存；
- B 清旧导航队列并优先退出；
- Runtime 全局相同日志2秒连续抑制。

## 18. r19 时期历史阻塞项

r19 第一阶段已经通过。state1 状态页因无额外内部业务已直接封存；refactor20 的 state2 法宝/道具仍待实机，refactor20a 额外先验证主 Interface 特征兼容热修。

如果 Y 仍失败，不再猜地址；直接根据 r19 分层日志判断：

1. 没有“Y已捕获” → InputRouter/Context 问题；
2. 捕获后“Context已变化” → worker 与游戏线程间状态变化；
3. `0x89F808!=0` → 原版地图动作忙；
4. `0x468BF0==0` → 当前原版模式本来就不允许 Space 业务；
5. 已记录“Space 当前业务目标(action=1)已调用”但菜单不出现 → 说明该业务链后续状态存在问题；继续追解析到的目标及后续状态，而不是模拟键盘。

如果 Shell 仍“钝”：先区分“动画期按键是否丢失”与“原版动画本身慢”。r19 已不再有 r17 的 pending 丢失问题，也不应再用提高 repeat 掩盖。

## 19. r19 时期历史下一步

r19 第一阶段通过后：

1. 封存 Y + Shell 基础；
2. 若用户仍认为原版动画节奏明显过慢，单独研究 Interface 动画加速，不能与页面业务混改；
3. 开始 **状态页** adapter；
4. 法宝/道具；
5. 绝学；
6. 及身/装备；
7. 五内；
8. 阵形 + Battle Target 二维方向修正；
9. 天书；
10. 机能；
11. 全部主要页面完成后，横向审查哪些成功代码真实重复，再决定是否抽公共组件。

## 20. 构建与交付规则

- 源码文件名英文；
- 源码详细简体中文行内注释；
- 文档文件名简体中文；
- 文档集中 `文档/`；
- `证据/` 不放图片；
- 每包累计接档；
- build.bat UTF-8 BOM + CRLF + `chcp 65001`；
- 20 个 C 独立 `/W4 /WX /utf-8`；
- x86 / PE32 / i386；
- `/nodefaultlib`；
- 编译内容只保留 ASI + INI；
- 工具/缓存/OBJ/LIB/EXP/PYC 不得混入交付包。

当前容器没有真正 `i686-w64-mingw32-gcc`，因此不伪造 MinGW PASS。Windows MSVC `build.bat` 与 clang-cl x86 严格验证保留。本版为 20 个 C 独立单元。

## 21. refactor20a 历史机械封版目标（当前以第29节为准）

最终封包前必须达到：

- `refactor_check.py` 0 FAIL；
- 目标 RPG.exe SHA 正确；
- 8/8 核心协议；
- 46/46 稳定 CALL；
- 2/2 Start=ESC；
- 4/4 对话；
- 历史 ConfirmDialog 2/2 vtable + 4/4 CALL 只作研究护栏；
- 主 Interface **Space 业务特征 + 动态 CALL 目标 + 1/1 短锚点 + 10/10 ButtonEvent CALL**；
- Y 代码中不存在 Space 键盘注入；
- Shell 有动画期语义动作队列；
- Runtime 有全局多槽重复日志抑制；
- 当时 18/18 C 严格编译；
- PE32/i386；
- 文件 SHA 清单全量复算一致；
- 包内图片 0；
- 构建垃圾 0；
- ZIP CRC PASS。

这些结果都不能替代 r20 的 Battle/state2 用户实机验收。

## 22. 独立接档注意事项

接档者必须特别记住：

1. **不要重新把 `0x413933` 当普通地图菜单入口。** 它是特殊休整三项动作菜单；
2. **不要再把 `0x40CC73 -> 0x406F20` 写成已确认普通地图 Y 入口。** r18 实机仍 FAIL；
3. r19 已证明 Y 可以复用“原版 Space 的业务事件”，不是发送 Space；refactor20a 基线目标 `0x40B230` 只作记录，运行时跟随 Space CALL 当前目标；
4. 该业务目标(action=1)必须从游戏线程安全点调用，worker 只置 pending；
5. **不要让 Battle/Save/其它模块全局清 UiBridge pending。** owner 是 r17 严重迟钝问题的根修；
6. 原版 `Interface+0x590>8` 是真实动画门。r19 先缓存输入，不直接绕过它；
7. 如果仍嫌慢，先验证“不丢键”后再独立讨论动画加速；
8. refactor12 的“先公共化选择框”已经被用户明确否决；
9. 普通对话、R3、Start=ESC 属于用户已 PASS 业务，不因主菜单开发随便改；
10. 用户截图中的五内/机能鼠标 icon 是**期望实现图**，不是原版 hover 证据；
11. 每次开发新页面前先复核上一轮源码与实机结论，再开始修改。

## 23. refactor20 新增累计结论

### 23.1 Battle 禁用项 A 假死

根因：普通 `request_confirm()` 没有调用已有 `button_usable()`，禁用 Button 仍可能留下 `UI_EVENT_OWNER_BATTLE` pending；后续 B 因桥已被占用无法排取消。

修正：

```text
A
→ focused_button
→ button_usable
→ 不可用：忽略，不建 pending
→ 可用：原版 ButtonEvent

B
→ 清 UI_EVENT_OWNER_BATTLE
→ 当前 Context 原版取消链
```

### 23.2 state1 状态页

无页面内部操作。r19 Shell 已完整满足：角色、大类、B。**不要为了“每页一个文件”写空 Adapter。**

### 23.3 state2 法宝/道具

新增 `interface_items.c/.h`。已静态闭合：

- 6 子类型 CALL `0x438093`；
- 特殊 A/B `0x438136 / 0x43817A`；
- 上/下页 `0x43820E / 0x438274`；
- 行 Event `0x4382DB`；
- 特殊按钮可用状态机器码 `0x4380D0`；
- 页面字段 `+0x584/+0x590/+0x594/+0x598/+0x59C/+0x5A0/+0x5AC/+0x5B0`。

产品语义：

- LT/RT 子类型；
- ↑/↓ 物品与跨页；
- A 当前物品；
- X 当前原版可用的分发/弃置；
- state2 弹窗页面专属 A/B/方向；
- ←/→、LB/RB、根 B 继续交给 r19 Shell。

### 23.4 抽象纪律

state2 弹窗即使结构与旧 ConfirmDialog 相似，也由 InterfaceItems 独立拥有。只有后续多个页面实机成功并证明字段、生命周期、Event 完全相同，才允许重新抽公共代码。

## 24. refactor20a 兼容热修累计结论

用户遇到 r19/r20 代码线启动失败：主 Interface 预检认为 `0x40B230` 长度 0x23 的整段函数体不一致，随后整个 Hook 安装停止。由于地图 Y 的业务设计只是“复用原版 Space 当前事件”，本插件并不需要也不应该要求该函数体完全未被别的补丁 Detour。

refactor20a 的正式规则：

- 保留精确地址体系和所有真正 Patch 点的 fail-closed 校验；
- Space 分支改用 mask 特征，只验证 `VK_SPACE / 0x89F808 / 0x468BF0 / action=1`；
- `0x44A57B` 解析当前 `E8 rel32` 目标，不再要求固定为 `0x40B230`；
- Y 在 Exploration 游戏线程调用解析到的目标；
- 若 Space 业务特征或 CALL 本身消失，仍拒绝安装；
- refactor20 的 Battle 禁用项 A 修复、状态页封存、state2 法宝页业务代码不因本热修改变。

## 25. refactor21 累计结论：法宝安全修复 + 绝学页

### 25.1 用户实机事实

refactor20a 下 state2 法宝页主体可操作，但有两个实机 BUG：LT/RT 方向与产品语义相反；弃置确认框画面默认“取消”时直接 A 仍执行“确定”，会实际丢弃物品，属于危险错误。

### 25.2 根因与修复

- state2 subtype 内部 index 不保证等同屏幕从左到右顺序。r21 改用真实 Button `+0x34` X 坐标寻找视觉前后邻居；
- type1 popup 构造时 `+0x58C=1` 先于真实 HitTest，因此不能把它当作首帧视觉焦点。r21 新 popup 默认逻辑取消；未进入手柄导航时被动观察原版 `0x431310` HitTest；A 只点击当前焦点真实 Button，B 固定取消。

### 25.3 全局方向硬规则

**LB/LT/← = 上一项/左侧/前一个；RB/RT/→ = 下一项/右侧/后一个。** 后续任何顺序型页面不得根据内部 index 反转这条规则。

### 25.4 state3 独立架构

新增 `interface_skills.c/.h`，只负责 state3。已确认 `Interface+0x644`、列表 `+0x5DC`、子类型 panel `+0x5E4`、页/行字段 `+0x5EC..+0x5F8`、popup `+0x5FC`，以及四个真实 ButtonEvent CALL `0x43C363/0x43C3B6/0x43C3E8/0x43C461`。LT/RT、上下/跨页、A、使用确认均通过原版 ButtonEvent，不直接改游戏状态。

### 25.5 当前下一步

**state3 已由用户完整实机 PASS并冻结。当前应一次性联合验收 r22 的 Battle 不可用项A事务解锁、state4及身、state5五内；通过后进入 state6阵形 + Battle Target 二维方向统一。** 不得因为页面外观相似恢复 refactor12 通用 SelectionUI。

## 26. refactor22 历史权威接档增量（已被第27节取代）

> 本节记录 v0.3-refactor22 交付当时的权威状态，现已由第27节取代；保留用于理解 r23 的实机反馈与修正因果。

### 26.1 项目最终职责边界

本项目现在只做**手柄操控模组**。BugFix、随时存档、免CD、成长/掉宝、宽屏、Mod Loader、兼容引擎等由其它独立模块负责，最终共同组成 Remaster。本 DLL 后续不得吸收这些非手柄功能。

### 26.2 当前实机状态

- r19 Shell：PASS；
- state3 绝学/法术：用户 2026-08-20 明确完整 PASS，`interface_skills.c/.h` 本版字节冻结；
- Battle 不可用项 A：上一轮仍 FAIL，具体为 A 后当前菜单控制锁住，B 退出重进恢复；r22 新事务解锁待验；
- state4 及身：r22 新候选；
- state5 五内：r22 新候选。

### 26.3 Battle 新根修

此前只做 Button `+0x45/+0x04` 建立前检查，仍覆盖不了“视觉/业务门后原版拒绝”的情况。r22 让 confirm 成为可失败事务：deferred 超时；Event 真正被原版消费后重新开始完整最小视觉握手计时；同 Context 仍不转层则自动清 watch/settle；B 先清 Battle owner。目标是不可交互 A 完全无副作用且无需退出重进。

### 26.4 state4 及身（r22 历史实现）

原版 `Interface+0x648`；三栏位、9行候选、真实上一/下一页均闭合。X 循环兵刃→护甲→饰物；↑/↓候选；A装备；左右继续 Shell 切角色；LT/RT无业务。六个 ButtonEvent CALL 全部只走 `0x431380`，不写装备状态。

### 26.5 state5 五内（r22 历史实现）

原版 `Interface+0x64C`，`0x440610` Update 扫 11 个真实 Button，统一 Event CALL `0x4406A8 -> 0x431380`。五个内部节点静态闭合为 0迅/1烈/2神/3魔/4魂。用户 X 顺序固定烈→神→魔→魂→迅；左右减/加；Y蕴魂。Cursor 只显示焦点，不成为逻辑真值；业务数值仍完全由 RPG.exe 修改。

### 26.6 构建/静态结果

- 18 个 C 独立 `/W4 /WX /utf-8` 等价严格编译 PASS；
- PE32/i386 PASS；
- ASI SHA-256 `7c25ae2624aad4844a43c26414d9e7e4643a3726ca1043696af0db2e3c58aa8f`；
- `refactor_check.py` 无 EXE：51 PASS / 0 FAIL；
- 编译内容只有 ASI + INI；缓存/OBJ/LIB/EXP/PYC 0。

### 26.7 EXE 样本

正式精确目标仍为 `b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf`。本轮用户 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f` 样本：仅 SHA 检查 FAIL，其余 60 项中的所有机器协议均 PASS，包括 state4/state5。记录为协议一致第二样本，不自动扩展正式支持。

### 26.8 当前阻塞与下一步

阻塞只剩实机：Battle 不可用项 A、state4、state5、SDL3 热插拔。用户明确要求 state4 做完不停，state5 一起交付验收，因此本包在此处才形成候选。四项通过后下一项：**state6 阵形 + Battle Target 二维方向统一**；再后是 state7 天书、state8 机能。

### 26.9 SDL3 热插拔回归与 r22 修复

用户在本轮补充确认：r21 已失去手柄热插拔，拔插后不能自动恢复。静态复核定位到 `PadInput_Poll/pad_read_buttons` 的生命周期顺序，而不是 InputRouter 页面语义：旧代码先查询旧句柄 Connected，之后才 Update；无句柄时更不会 Update。因为本插件不跑 SDL 事件循环，设备快照可能陈旧。

r22 固定：每 tick `SDL_UpdateGamepads`；旧句柄断开立即 Close 并清物理按钮/全部轴；无句柄约 256ms 重枚举；新句柄首帧 `prev=current`，不制造热插拔 Pressed/Released。设备断开不清 Battle/Interface 业务状态。

这一项和 Battle 不可用 A、state4、state5 一样仍需用户实机 PASS。当时 r22 联合验收范围是四项：

1. Battle 不可用项 A 无动作且立即可继续操作；
2. state4 及身；
3. state5 五内；
4. SDL3 运行中拔插/无手柄启动后插入自动恢复且无首帧误触。

四项通过后再推进 state6 阵形 + Battle Target 二维方向统一。

## 27. refactor23 历史权威接档增量（已由第28节取代）

> 本节保存 v0.3-refactor23 当时的权威状态与实现因果。**r23 后续实机已经否决 state5/Target/state6 的候选结果；当前事实必须以第28节 refactor24 为准。**

### 27.1 用户最新实机裁决

- state3 绝学/法术：PASS，冻结；
- **state4 及身/装备：PASS，冻结；**
- **Battle 不可交互项按 A 后锁死：PASS，保留 r22 修复；**
- state5 五内：r22 FAIL，症状是 X 切换后历史节点继续闪，绕一圈后多个图标同时闪；
- Battle Target：旧四方向仍 FAIL，轴/行列语义不是真二维；
- SDL3 热插拔：r23 打包时仍按待验记录；**后续用户已明确补充：上一轮其实已经 PASS，当前以第28节为准。**
- 用户明确要求：先修五内，再修 Target 真二维，然后再做阵形；可连续推进后一起验收。

### 27.2 state5 历史闪烁根因与 r23 修复

旧设计从一开始要求“插件逻辑焦点 + 原版鼠标仅作视觉”。r22 只实现了鼠标移动，却没有把合成鼠标从 state5 真实 Button 的鼠标历史链隔离。

原版 state5 `0x440610` 每帧对 11 个 Button 经过：

- `0x44066E -> 0x431400` 鼠标边沿；
- `0x4406A8 -> 0x431380` ButtonEvent，而 0x431380 内部还会再次 HitTest/维护历史状态。

r23 因此做双层隔离：`InterfaceInnerStats_HookMouseEdge` 在手柄合成焦点模式对本页按钮返回无鼠标边沿；UiBridge 无 pending 时再通过 `InterfaceInnerStats_FilterVisualOnlyButtonEvent` 跳过 0x431380 的视觉鼠标历史维护。手柄真正 ←/→/Y 的 pending code=2 优先消费，不受过滤；实体鼠标/右摇杆接管会立刻撤销两层隔离。

### 27.3 shared 二维邻居算法

新增 `spatial_neighbor.c/.h`，只共享几何算法，不共享 Battle/阵形业务。

每个候选只属于一个主方向扇区：`abs(dx)>=abs(dy)` 归水平，否则归垂直；再按符号区分左右/上下。水平优先最小 |dy| 后 |dx|，垂直优先最小 |dx| 后 |dy|。这从结构上禁止一个候选同时成为“右”和“下”之类的歧义。

### 27.4 Battle Target 真二维修正

- dev15 selector side mode、grid map、合法目标集合继续原样保留；
- 真实阵位坐标能对整套合法目标取得时全部使用真实坐标；
- 否则整次统一使用正常逻辑网格 `x=列/y=行`；
- 删除旧的交叉轴 fallback；
- 四方向统一调用 `SpatialNeighbor_Find`。

Battle 不可交互 A 已通过的确认事务没有因为 Target 修改而重写。

### 27.5 state6 阵形首版

静态闭合：`Interface+0x650`，Update `0x43F9A0`，page+0x580 panel，page+0x584 exchange pending，+0x588 source，+0x58C target，+0x590 hover，+0x594 role map；panel+0x59C..+0x5B8 为 8 个真实 Button；`0x43FA05 -> 0x431380` 为统一 Event。

方向读取 8 Button 中心，调用 shared spatial。A 对当前阵位注入 code=2：第一次/第二次选择与真正交换完全由原版状态机处理。B 在 pending 时注入 code=1 取消并消费本次 Cancel；未 pending 时不抢 B，让 Shell 正常返回。插件不直接写阵形业务字段。

预检踩坑：state6 Update 第一条对象读取是 page+0x57C，稍后才读 +0x580 panel。第二 EXE 样本帮助发现临时预检把第一条误写成 +0x580；r23 只修预检锚点，Adapter 的 +0x580 panel 使用本来就是正确的。

### 27.6 r23 当时的冻结与回归边界（历史）

- `interface_skills.c/.h` 当时与 state3 PASS 基线 SHA 完全一致；后续 refactor26 因合法新增分页不再整文件冻结；
- `interface_equipment.c/.h` 当时与 state4 PASS 基线 SHA 完全一致；后续 r25/r26 因合法新增分页不再整文件冻结；
- Battle.c 因 Target 必须改动，不能整文件冻结；`refactor_check.py` 单独机械保护已通过的“不可交互 A 事务解锁”结构。

### 27.7 SDL3 热插拔

继续沿用 r22：每 tick 先 `SDL_UpdateGamepads`，再判旧句柄、关闭、低频重枚举、打开并采样；新连接第一帧令 previous=current 防止虚假边沿。**这是 r23 打包时的历史表述；用户随后明确确认热插拔已 PASS，r24 冻结该链。**

### 27.8 构建与静态检查

- 20 个独立 C，i686 clang-cl `/O2 /GS- /Zl /W4 /WX /utf-8 /TC`：20/20 PASS；
- lld-link `/dll /nodefaultlib /machine:x86 /entry:DllMain@12`：PASS；
- PE32/i386 ASI SHA-256：`f218972e4fdcc99cd7d04f1b3b0455a3a19d3c5eca5764d487cfb4cdc9ca49d0`；
- `refactor_check.py` 无 EXE：**54 PASS / 0 FAIL**；
- 编译内容白名单：ASI + INI；构建垃圾 0。

### 27.9 第二 EXE 样本

【历史】旧工具曾因单 SHA 门槛把第二样本记作 65 PASS / 1 FAIL；refactor27 已按用户裁决把该完全未修改原版样本纳入支持白名单，并继续用逐协议检查约束兼容性。

### 27.10 当前阻塞项与后续顺序

本轮联合实机重点：

1. state5 X 绕多圈后仍必须只有当前焦点，不允许历史图标闪；
2. Battle Target 四方向真正水平/垂直；
3. state6 阵形二维移动、A 两阶段交换、B 取消；
4. SDL3 热插拔恢复。

已通过的 state3、state4、Battle 不可交互 A 只做快速回归。若 state5/Target/state6 通过，主 Interface 后续继续 state7 天书，再 state8 机能；不得因为共享 spatial 成功就把其它页面业务抽成万能菜单框架。

## 28. refactor24 历史权威接档增量（已由第29节取代）

> **这是本包的最高优先级当前状态。** 前文所有 refactor22/refactor23 的“待验/下一步”只保留历史因果；与本节冲突时一律以本节为准。

### 28.1 用户最新实机裁决

已经 PASS / 冻结：

- state3 绝学/法术：完整 PASS；
- state4 及身/装备：PASS；
- Battle 不可交互项按 A：PASS，按下无业务反应且不锁手柄；
- SDL3 手柄热插拔：PASS，运行中拔插或重新连接可自动恢复。

refactor23 严重 FAIL：

- state5 五内：按 X 后看不到预期的原版手形鼠标焦点；
- Battle Target：r23 新二维算法把原本目标选择改坏；
- state6 阵形：↑/↓ 无效、←/→邻居错误，而且 ←/→ 会错误落回主 Interface 角色切换。

用户要求：**不继续新页面，先把五内 → Target → 阵形三项修正确。**

### 28.2 state5：为什么 r23 双层隔离仍看不到鼠标

r23 的“两层业务隔离”方向本身仍正确：合成手形鼠标不能进入 state5 的真实鼠标边沿/历史 HitTest，否则会重现 r22 “X 绕一圈所有历史节点都闪”。真正的新问题在几何计算。

r23 自己读取 Button `+0x3C/+0x40` 作为宽高，并把0当作“没有几何”。但原版 `0x431310` 明确允许这两个 override 为0；为0时沿 `Button+0x30 -> Sprite+0x35C -> frame owner+0x34 -> frame geometry` 读取 `+0x08/+0x0C` 的真实宽高。于是 r23 经常提前失败，逻辑 focus 虽变了，`Cursor_ShowMenuFocusAt` 根本没有拿到新坐标。

r24 把完整算法下沉到 `UiBridge_GetButtonScreenRect/Center`。state5 继续保存唯一逻辑焦点、继续双层隔离，但鼠标落点使用原版完整矩形。实体鼠标/右摇杆接管仍立即让路。

### 28.3 shared SpatialNeighbor：r23 45°扇区被实机否决

r23 用严格45°扇区避免“同一点同时算右和下”：水平要求 `|dx|>=|dy|`，垂直要求 `|dy|>|dx|`。这对规则方格成立，但幽城 Target/阵形是错列/斜排；真正的上下邻居可能有明显横向偏移，因此被直接排除。

r24 改为：

1. 只按符号取方向半平面：左 `dx<0`、右 `dx>0`、上 `dy<0`、下 `dy>0`；
2. 左右优先最小 `|dy|`，再最小 `|dx|`；
3. 上下优先最小 `|dx|`，再最小 `|dy|`。

这恢复 r22 Battle 已成熟的屏幕导航原则，同时仍保持 shared helper 纯几何、无业务。

### 28.4 Battle Target r24 回归根修

保持不变：dev15 合法目标集合、selector side mode/阵营限制、A确认/B取消、目标手形鼠标、目标事件事务。

r24 只改邻居选择：

- 当前目标有真实屏幕坐标时，尽量只用能取得真实坐标的合法目标；
- 某一个候选缺坐标不再把整套合法目标强制降级；
- 屏幕坐标完整且该方向无邻居时，视为真实边界，停在当前项；
- 只有当前坐标本身缺失，或存在坐标缺口且屏幕路径不能回答时，才退到正常 `x=列/y=行` 逻辑网格；
- 旧交叉轴 fallback 永久删除。

### 28.5 state6 Formation：原版阵位表 + D-Pad 硬所有权

静态业务协议仍是 r23 已闭合的原版链：`Interface+0x650`，Update `0x43F9A0`，panel `page+0x580`，8 Button `panel+0x59C..+0x5B8`，统一 Event `0x43FA05 -> 0x431380`；A/B 继续由原版两阶段交换/取消处理。

r24 解决两个新根因：

**空间拓扑**不再由 Button 几何推导，直接使用 RPG.exe state6 自己的 8 阵位坐标表：

```text
0x46BFF8 X: 227,171,115,59,283,227,171,115
0x46C018 Y: 28,56,84,112,56,84,112,142
```

**输入所有权**改成 fail-closed：只要 Interface state==6，D-Pad 四方向就归 Formation；Adapter 不可用/page 未就绪时也消费，绝不让 ←/→ 变成 Shell 切角色。每次 state6 Update 还定向删除 Shell 队列中未提交的 ROLE_PREV/ROLE_NEXT，防止上一页动画期残留穿透。

阵形鼠标视觉若需要 Button 中心，则调用第28.2节完整 UiBridge 几何，与空间拓扑解耦。

### 28.6 冻结边界

本轮不得回归：

- `interface_skills.c/.h`：state3 PASS；
- `interface_equipment.c/.h`：state4 PASS；
- `pad_input.c/.h`：SDL3 热插拔 PASS；
- Battle 不可交互 A：虽然 `battle.c` 因 Target 必须修改，但确认 watchdog/禁用项拒绝结构由工具单独机械保护。

### 28.7 EXE / 静态 Oracle

正式精确目标仍为：

`b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf`

用户确认完全未修改的 `RPG.exe.org` 解出的 `RPG.exe`：

`8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`

继续作为可信原版静态 Oracle / 协议样本。r24 新检查的 state6 X/Y 两张阵位表也在该样本 2/2 一致；工具仍保留正式 SHA 门槛，因此该样本会只有 SHA 一项 FAIL，不能偷偷改成正式精确支持。

### 28.8 refactor24 当时的阻塞与下一步（历史）

当前只等待用户实机验收三项：

1. **state5 五内**：X 循环时原版手形鼠标必须随唯一焦点移动；绕多圈仍只能当前节点，历史节点不闪；
2. **Battle Target**：四方向必须符合屏幕真实水平/垂直关系，且 A/B、合法集合、Target 鼠标不回归；
3. **state6 阵形**：四方向都有效，←/→绝不换角色；A 两阶段交换、B 取消正常。

这三项未通过前**不进入 state7 天书或其它新页面**。三项全部通过后，才继续 state7 天书，再 state8 机能。

## 29. refactor25 历史接档增量（已由 refactor26 取代）

### 29.1 用户最新裁决

- Battle Target：r24 已经回到“改新排序之前”的旧成熟屏幕坐标方案；用户决定验收通过，不再继续改排序。
- SDL3 热插拔：用户补充说明此前已经通过，正式冻结。
- state4 原有及身主体此前通过。

### 29.2 本轮改动

**state4 及身：** LT=上一页、RT=下一页。实现不新增页状态，直接复用 `equipment_request_page()`：点击原版 Prev/Next 按钮，等待 RPG.exe 真实页码变化，再按同一落点规则恢复行焦点。第一/末页直接忽略，无事务。

**state5 五内：** LT=减少、RT=增加；X 与 Y 不变；←/→重新 PASS 给 InterfaceShell。Shell 的角色 ButtonEvent 能力范围从 state1..4 扩到 state1..5。五内本身不再实现角色切换。r24 的合成手形鼠标双层业务隔离与完整 Button 几何仍保留，等待实机确认。

### 29.3 不改动/冻结

- `battle.c`：用户接受的 Target 旧成熟方案整文件 SHA 冻结；
- `pad_input.c/.h`：热插拔 PASS SHA 冻结；
- `interface_skills.c/.h`：r25 当时 state3 PASS SHA 冻结；refactor26 因新增 D-Pad 分页已合法取消整文件冻结；
- Battle 不可交互 A 的确认事务不改；
- state6 阵形本轮不改，仍为 r24 待验候选；
- 没有新增 RPG.exe 地址、Hook 或第三方库。

### 29.4 静态验证

- 无 EXE：55 PASS / 0 FAIL；
- 用户完全未修改原版第二样本：66 PASS / 1 FAIL，唯一 FAIL 为正式支持 SHA 不同，其余协议全部 PASS；
- 20/20 C 严格编译与 PE32/i386 链接：PASS；ASI SHA-256 `94aed4977be1676bb432161de4a1c4468c245c4608f1bf00afd3587b3539c755`，大小 123,904 bytes。

### 29.5 下一步实机顺序

1. 五内 X 唯一鼠标焦点及历史闪烁；
2. 五内 LT减/RT加、←/→换人；
3. 及身 LT上一页/RT下一页与边界；
4. state6 阵形 r24 候选；
5. 快速确认 Target/热插拔冻结项没有 BIG 回归。

## 30. refactor26 统一操控权威规格（五内视觉已由 refactor26b 补充）

### 30.1 用户最新操控裁决

- LB/RB 大类不变；各页面既有 LT/RT 与 X 语义不变。
- 角色切换从 D-Pad 左右迁移到**左摇杆水平超过50%**；一次推杆只触发一次，必须回中后才能再次触发。
- 所有当前需要翻页的主 Interface 页面统一增加 D-Pad ←上一页、→下一页：state2 法宝、state3 绝学、state4 及身。
- state4 原有 LT/RT 翻页继续保留；D-Pad 与 Trigger 必须共用同一个原版分页事务。
- state5 五内保持 X切节点、LT减、RT加、Y蕴魂；D-Pad 不承担加减或换人。
- 未来 state8 机能按同一产品逻辑：音乐/音效聚焦后 LT/RT 减/加。

### 30.2 左摇杆50%角色切换

InputRouter 新增与 D-Pad 完全独立的水平模拟方向沿：SDL3 左摇杆 X 达到 `±16384` 后只产生一次 -1/+1；保持推住不重复；回到现有 `PAD_STICK_DEADZONE` 中间区后 re-arm。Shell 仅在 state1～5消费该语义，并继续提交已确认的真实角色 ButtonEvent。state6 Formation 明确拒绝该角色切换。

### 30.3 state2/3/4 D-Pad 左右分页

三个页面不新建分页状态机：

- state2 → `items_request_page()`；
- state3 → `skills_request_page()`；
- state4 → `equipment_request_page()`。

全部继续点击原版 Prev/Next ButtonEvent，等待原版真实页变化。禁止直接写 page。state3/state4 的已通过业务因本轮合法增强不再用整文件 SHA 冻结，而由协议/无直写/确认安全等不变量保护。

### 30.4 refactor26 五内动画历史方案（已被 refactor26b 根修取代）

用户确认 r25 五内功能正常，但 LT/RT 加减后按钮动画持续存在，是严重视觉 BUG。重新反汇编 state5 Update `0x440610` 后闭合：

```text
0x44066E -> 0x431400 -> code=2 -> 0x43E6F0(1)   原版按下反馈
下一游戏帧
0x4406A8 -> 0x431380 -> code=2 -> 0x43E6A0(0)   原版释放/确认 + 业务
```

r25 只注入第二段，导致视觉生命周期不完整。r26 改为页面私有三阶段 pulse，在原版游戏线程扫描节奏里重放完整 press→release，不手写 Sprite 动画状态。UiBridge 在 pulse 期间报告 busy，避免切角色/切大类中断按钮事务；500ms 超时只清插件私有 pulse。

### 30.5 已 PASS / 冻结与当前待验

已 PASS/冻结：
- Battle Target 用户接受的旧成熟屏幕方向方案；
- SDL3 热插拔；
- Battle 不可交互项 A；
- state3/state4 的既有业务语义。

refactor26 待实机：
1. 左摇杆50%阈值、单次触发/回中再触发；
2. state2/3/4 D-Pad 左右分页与边界；
3. state5 LT/RT/Y 动画只瞬时响应一次且不留历史残影；
4. state6 Formation 现有候选四方向/A/B。

上述通过后再继续后续页面，不用新的页面开发掩盖当前输入/视觉回归。

### 30.6 refactor26 历史机器验证

- 20/20 独立 C：i686 MSVC ABI、`/O2 /GS- /Zl /W4 /WX /utf-8 /TC` 严格编译 PASS；
- PE32/i386 DLL 链接 PASS；最终 ASI 125,440 bytes；SHA-256 `02f108bb50ec36eb25342b594291052e283d1dcb5adf7c529366c758e066c6c8`；
- `refactor_check.py` 无 EXE：**56 PASS / 0 FAIL**；
- 用户完全未修改原版第二样本：**67 PASS / 1 FAIL**，唯一 FAIL 为历史正式 SHA 不同，其余协议全部 PASS；
- Battle Target 冻结 `battle.c` SHA-256 `05a6f7223bcbf4923c771fee45ddd339ed6022238ed8b08a8ca42c8d62ac1a97`；
- 热插拔冻结 `pad_input.c` / `.h` SHA-256 分别为 `f5606267065aed185eafba8165162c489d4d340f76fe1404bd0682fa134d223d` / `96e856f204b4b406ee1a3aa1059a5fb17ab538645dfc1887edbdabc5b6920b8f`。

机器验证只能证明结构/协议/构建成立，refactor26 的新操控与五内视觉仍必须实机裁决。

---

## 2026-08-26 Public API v1 增量

### 架构
新增 `Castle_PadSupport_API.h / pad_public_api.h / pad_public_api.c / Castle_PadSupport.def`。
不改现有页面 Adapter 输入语义。worker 在 ControlModes 裁决后调用 `CastlePad_PublicApiPublishFrame()`。

### 对外 ABI
唯一导出：`CastlePad_GetApi`。v1 只读，提供物理输入、语义输入、详细控制模式及外部 UI 总门。

### 线程
第三方读取 provider 发布的 32 位标量快照，不读取内部结构，不进入 SDL。

### 已废弃外部集成方式
机器码函数签名、`PadInputState` 偏移、`ControlModeState` 偏移均不得作为第三方 ABI。

### 构建
`build.bat` 新增 `pad_public_api.c`，链接使用 `Castle_PadSupport.def`，编译内容附带
`开发接口/Castle_PadSupport_API.h`。

### 当前待实机确认
Public API 本身不改变 refactor42 游戏业务；仍需用 Backlog 等外部消费者实测
`LB / D-Pad / CANCEL / AllowsExternalUiInput` 全链。
