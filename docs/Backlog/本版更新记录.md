> **当前权威状态（2026-08-27）：Castle Backlog v0.3.4-test3。**
> `PanelStrideY` 现已真正支持 80～160：≤110 同屏最多四条，>110 自动改为最多三条。
> 默认值改为 135；左右翻页量自动不超过当前三/四条容量，避免三条布局跳过记录。
> test1 在可见条目达到四条时仍可崩溃：默认 `PanelStrideY=118` 令最上姓名 Y=`380-35-3×118=-9`；
> 同一 SF2 一帧多画还会改写 `+0x50/+0x54/+0x58/+0x5C`。test2 将步长限制为 80～110、最终 Y 再夹紧，并在每次绘制前后恢复完整游标。
> 用户实机确认 test4 在剧情中打开不显示、剧情结束后打开必崩。
> 根因分别是：截断 `0x40B150` 连带丢失 `0x434500` 绘制队列登记；私有 F-Name 池把裸 C 字符串
> 错传给要求游戏字符串对象的 `0x4070D0`。本版删除整个私有姓名框池，不创建/析构任何 SF2；
> 活动态只调用 `0x434500(scene_world,0xBB8)` 保留绘制，不运行完整 SceneWorld update；
> 历史条目复用原版 `F-Talk + 0x46F658 F-Name + 姓名文字 + 正文` 组合框，人物图单独屏蔽。
> 真实剧情只临时使用只读 mode，关闭时恢复；自由探索继续使用 synthetic 消息载体。
> `mode=2/3` 和未知剧情模式继续 fail-closed；手柄只通过 `CastlePad_GetApi` Public API v1 协作。
> 下文较早版本中与本段冲突的“当前方案”只按历史记录阅读。

# 本版本更新记录

## 零点二版

- 继承零点一版已经实机可工作的记录、打开、浏览、退出、输入门和环形历史；
- 记录新增最多 64 字节 NameList 姓名快照；
- 展示从固定底框单条替换改为同屏最多四个原版 F-Talk 对话框的纵向现代列表；
- 有姓名记录使用左侧姓名列、右侧正文；无姓名记录保持原版左边距；
- 按用户要求，现代列表完全不显示人物图；
- 删除浏览期 `0x403C60` 调用，不卸载/重建人物 `%d-2.SF2`；
- 修复用户实机报告的 `FrameID >= m_SafHeader->TotalFrameNumber` 跨人物报错路径；
- 新增 `0x40486E` 通用框 draw CALL 与 `0x4049FF` 正文 draw CALL 的窄链式 Hook；
- 默认 `PageSize` 从 5 改为 4，与同屏条数一致；
- 机器预检和检查器新增两处 CALL 签名；
- 构建脚本在同步前清理两个明确文档镜像中的旧 Markdown，避免改名后残留旧接档。

零点一版底层主链实机可用、人物回放明确 FAIL；零点二版现代无人物展示已静态/构建 PASS，待用户实机复核。详细状态以《截至零点二版的完整接档》为准。

---

# v0.3.3-test3 更新

- 修复剧情中打开 Backlog 的核心所有权错误：新增稳定剧情零写入旁路。
- 新 Hook `0x404859 / 0x404899 / 0x4048E6`，只隐藏真实剧情视觉，不改 speaker/message 全局。
- SceneWorld update 在 Backlog 活动期间冻结；draw 路径继续。
- 自由探索 synthetic dialogue 与剧情旁路正式分离。
- 删除 PadSupport 内部签名/数据扫描，改为正式 `CastlePad_GetApi` Public API v1。
- 构建脚本现在把全部中文文档与最新 `backlog_check.py` 一起放入 release。


---

# v0.3.3-test3 编译修复补记

- 修复 `pad_bridge.c` 对不存在计时函数的调用。
- PadSupport 延迟绑定重试改为直接使用 `GetTickCount()` 毫秒时间。
- `PAD_BRIDGE_RETRY_MS` 不再经过 `Runtime_MsToTicks()`，避免时间单位混用。
- 本轮只修编译和重试计时实现，不改变游戏内行为。


# v0.3.3-test4 更新

- 修复 test3 自由探索打开 Backlog 后“只有输入/日志，没有 F-Talk 对话框和正文”的回归。
- 根因：test3 对所有活动状态都截断了 SceneWorld update，synthetic dialogue 无法完成原版消息绘制准备。
- 改为双路径调度：剧情旁路继续完全冻结；自由探索 synthetic 恢复链式调用前一层 SceneWorld update。
- synthetic 仍使用 event/map gate + read-only mode 阻断正常游戏推进。
- PadSupport Public API v1 不变；用户已实机确认手柄/API 正常。


# v0.3.4-test2 更新

- 继承 test1 的绘制队列登记修复和无私有 SF2 原版组合框；
- 修复四条有姓名记录时最上姓名 Y=-9 的确定越界；
- `PanelStrideY` 默认/上限改为 110，运行时位移与姓名最终 Y 双重夹紧；
- 保存并恢复 F-Talk/F-Name `+0x50/+0x54/+0x58/+0x5C` 绘制游标；
- 检查器同步验证上述不变量；
- 多条目崩溃修复待用户实机确认。

# v0.3.4-test3 更新

- 修复用户设置 `PanelStrideY=135` 无效果：不再统一夹到 110；
- 80～110 自动使用最多四条，111～160 自动使用最多三条；
- 默认间距改为 135；
- 翻页量自动不超过当前可见容量；
- 保留 test2 的负 Y 夹紧和 SF2 四游标恢复；
- 自适应三条布局待实机确认。
