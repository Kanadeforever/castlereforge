# Castle_Quest dev6zd 任务可见性全量审计

本文件记录现行运行层结论，不替代 `研究/冻结任务数据库/` 的历史取证材料。

25 个 Base 已在 dev6zc 使用原版事件空间与 GameVar 写入链重新交叉校对。确认的新运行缺口只有 Q002：Event37 后 `Var505=1, Var1008=0, Var1007=1` 时 `find_doll.when` 成立，但旧顶层 Active/Journal 未覆盖。该问题已修正并由 dev6zd 继承。

本版进一步清理“研究事实”和“任务阶段”的边界：Q004 Event32/33 虽是真实场景事件，但四小鬼并不是原版营救阿吉的硬性推进门槛，因此旧 `optional_clues` 不再作为 Base Stage；Remastered 若希望玩家先调查，由 Addon Event33 Insert 表达。

Q001 取鸡汤则相反：Event18 确实把 Var1 从 0 推到 1，是原版主线真实推进，所以保留 Base Stage，只删除 Guidance 标签和特殊求值。

现行运行统计为 25 Quest、25 Addon、146 Base Stage、146 Canonical、0 Guidance、0 Warning、556 Stage-local Route、1 Addon Insert。
