# v0.3.0-dev9-about5 + mods.ini 节间空行修复实机测试清单

## About about5 重点验收

1. 打开“关于”，右上角必须使用与“设置”一致的标准 Windows 关闭按钮，不再出现 about4 的工具窗口小方块关闭按钮。
2. 正文最后一行必须完整显示，不能被“相关链接”标题或链接按钮覆盖。
3. 默认 about.cpp 内容下 About 高度应自动适配，不出现 about3 截图中的正文截断。
4. 在源码中临时把 About 正文增加 2～4 行重新构建，窗口高度应随正文折行结果增加，不需要改 launcher_gui.c 固定坐标。
5. Ultimate ASI Loader / cnc-ddraw 链接仍应可点击并交给默认浏览器。
6. 关闭 About 后主窗口恢复可操作。

> about5 历史包曾封存 dev9 游戏运行时；2026-08-29 的源码修复已修改 `mod_loader.c` 的配置排版逻辑，但没有修改 Locale、ddraw、Hook 地址、ASI 加载或 Overrides 查找语义。

## A. About 窗口

- [ ] 主界面顶部右侧仍为 `[关于] [设置]`，关于在设置左侧；
- [ ] 点击“关于”打开独立 About 窗口，不是普通信息 MessageBox；
- [ ] About 右上角使用与“设置”一致的标准 Windows 标题栏关闭按钮，不是工具窗口小方块 X；
- [ ] About 正文完整，不压按钮、不截断；
- [ ] 默认链接显示为蓝色下划线；
- [ ] 点击 Ultimate ASI Loader / cnc-ddraw 链接能由系统默认浏览器打开；
- [ ] 关闭 About 后主窗口重新可操作；
- [ ] 修改 `about.cpp` 后重新构建，正文/链接可以独立更新。

## B. INI 自动换行

- [ ] 打开包含很长 `;` 注释的 INI，长行会在编辑区右边界自动折到下一视觉行；
- [ ] 不出现横向滚动条；
- [ ] 缩窄 INI 编辑器时折行增加；拉宽时折行减少；
- [ ] 长注释视觉折成多行后仍全部保持绿色，不出现紫/蓝/橙错色；
- [ ] 很长的 `Key=Value` 视觉折行后变量名/值颜色仍合理；
- [ ] 保存后用文本编辑器核对：自动折行没有把原来的一个逻辑行写成多个真实行。

## C. 未保存/错误提示

- [ ] 任意修改后，底部立即以亮红色显示 `● 未保存修改`；
- [ ] 制造缺等号、空变量名等错误后点击保存，底部显示亮红色 `● 保存失败：...`；
- [ ] 弹窗能指出行号和原因；
- [ ] 错误定位仍落在真实逻辑行，即使前面有多条视觉自动折行长行；
- [ ] 修正后可正常保存，状态恢复。

## D. dev9 GUI 回归

- [ ] 主窗口最小客户区仍为 1024×640 DIP；
- [ ] 连续缩放无旧像素残留；
- [ ] 设置、重新扫描、启动游戏正常；
- [ ] ASI/Overrides 启停与拖动顺序正常；
- [ ] 缺失项“移除”只清理配置引用。

## E. 已封存运行时烟雾回归

本包的两个游戏运行时 DLL 与正式 dev9 二进制逐字节一致，仅需确认部署没有混入旧文件：

- [ ] 游戏可正常启动；
- [ ] 台湾繁中/标题转区正常；
- [ ] cnc-ddraw 在 dev9 已验收环境中行为正常。

## F. mods.ini 节间空行回归

- [ ] 以 `[ASI]`、空行、`[Overrides]` 的空配置开始，放入多个新 ASI 后启动游戏；
- [ ] Core 自动补全结果必须是 `[ASI]` 后紧跟所有 ASI 条目，然后保留一行空白，再出现 `[Overrides]`；
- [ ] 恢复同一初始配置，打开 Launcher GUI 点击“重新扫描”；
- [ ] GUI 自动补全结果必须与 Core 完全一致；
- [ ] 节尾存在多个连续空行时，所有新条目都必须排在第一条节尾空行之前；
- [ ] 只含空格或制表符的行也按节间空白处理；
- [ ] 节内注释、未知节、旧条目顺序和 `0/1` 开关不被改写；
- [ ] Core 与 GUI 写回失败时，原 `mods.ini` 保持完整。
