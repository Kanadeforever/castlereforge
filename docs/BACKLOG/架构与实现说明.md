> **当前权威状态（2026-08-27）：Castle Backlog v0.3.4-test3。**
> `PanelStrideY` 现已真正支持 80～160：≤110 同屏最多四条，>110 自动改为最多三条。
> 默认值改为 135；左右翻页量自动不超过当前三/四条容量，避免三条布局跳过记录。
> test1 在可见条目达到四条时仍可崩溃：默认 `PanelStrideY=118` 令最上姓名 Y=`380-35-3×118=-9`；
> 同一 SF2 一帧多画还会改写 `+0x50/+0x54/+0x58/+0x5C`。test2 将步长限制为 80～110、最终 Y 再夹紧，并在每次绘制前后恢复完整游标。
> 用户实机确认 test4 在剧情中打开不显示、剧情结束后打开必崩。
> 根因分别是：截断 `0x40B150` 连带丢失 `0x434500` 绘制队列登记；私有 F-Name 池把裸 C 字符串
> 错传给要求游戏字符串对象的 `0x4070D0`。本版删除整个私有姓名框池，不创建/析构任何 SF2；
> 活动态只调用 `0x434500(scene_world,0xBB8)` 保留绘制，不运行完整 SceneWorld update；
> 历史条目复用原版 `F-Talk + 0x46F658 F-Name + 姓名文字 + 正文` 组合框，人物图单独屏蔽。
> 真实剧情只临时使用只读 mode，关闭时恢复；自由探索继续使用 synthetic 消息载体。
> `mode=2/3` 和未知剧情模式继续 fail-closed；手柄只通过 `CastlePad_GetApi` Public API v1 协作。
> 下文较早版本中与本段冲突的“当前方案”只按历史记录阅读。

# 架构与实现说明

## 设计目标

本插件把 VN 常见 Backlog 做成《幽城幻剑录》的独立 QOL 能力，同时遵守三条边界：游戏对话框自己画、剧情状态完整恢复、手柄/宽屏插件仍能按原架构工作。

## 模块

```text
plugin.c
  └─ 生命周期与 8ms worker
      ├─ runtime.c：路径、INI、日志、前台门、机器协议预检
      ├─ sdl_input.c：可选 SDL3 动态加载与标准按钮采样
      └─ backlog.c：游戏线程捕获、历史环、浏览、原版 UI 回放、恢复
```

`game_addresses.h` 只放已闭合地址；`platform.h` 只放 Win32/x86 类型和调用约定。业务模块不各自复制魔法地址。

## 线程边界

worker 只读键盘/SDL 和少量只读状态门，用原子计数提交动作。它不复制正文、不调用游戏函数、不加载人物资源。

场景 vtable[0] 包装器运行在游戏线程。它捕获页面、消费动作、切换显示指针，再调用安装前的场景更新函数。若其它插件已经替换 vtable[0]，Backlog 保存其当前指针并按 thiscall 链式调用。现代浏览明确禁止调用 `0x403C60`，避免跨人物 SAF/SF2 的 FrameID 越界。

## 历史环

每条固定保存：页长度、speaker/style/variant、最多 64 字节的原版 NameList 姓名和 512 字节原始页。静态最大 256，INI 有效范围 16～256，默认 128。`head` 指向下一写槽；`offset=0` 永远换算为最新。

没有动态堆分配，避免老游戏内存分配器、CRT 和卸载生命周期不一致。加入姓名快照后，256 条的虚拟数据区约 146 KiB，但 ASI 文件中零区不会按同大小实写。

## 导航

- 上：`offset+1`，到最旧再按回到 0；
- 下：`offset-1`，在 0 再按跳到 `count-1`；
- 左：增加 `PageSize`，越界夹到最旧；
- 右：减少 `PageSize`，越界夹到最新；
- 方向支持延迟与间隔可配的按住重复；
- 退出为单次按下沿，不重复。

## 现代整屏 UI 回放

正文不转成 Unicode，也不由插件重新分行。历史 CP950/控制码被复制到插件缓冲，`0x46F664` 只在查看期间指向它。

原版 `0x404800` 仍是消息层入口，但两个内部 CALL 被窄包装：

- `0x40486E -> 0x407510`：同一个 `F-Talk.SF2` 对象临时改变 Y 坐标，最多在 640×480 中从上到下画四次；
- `0x4049FF -> 0x402EE0`：复用原字体对象、表面、裁剪和颜色，逐条在左侧姓名列与右侧正文列绘制。

当前滚动锚点位于最下方，更旧三条向上排列；记录不足四条时仍向底部对齐。默认 `PageSize=4`，正好整屏跳转。

现代模式不显示任何人物图。查看期间只把 `0x46F675` 临时设 0；已经加载的当前剧情人物对象、`%d-2.SF2`、F-Name 对象和姓名指针均不释放、不重建。姓名来自捕获时复制的 NameList 原文字节。

## 暂停与恢复

查看期间不冻结系统时钟或渲染。只使用原版消息/事件已经存在的模态门：Event yield/block=1、地图输入 gate=0、消息 mode=无效、target/current=4。

关闭采用两阶段：先把消息 ID 清零一段场景更新间隔，随后直接恢复原人物显示标志、姓名指针和全部快照，不进行资源重载。这同时保护手柄插件的普通对话 A pending，也修复旧版人物 FrameID 报错。

## 宽屏兼容

不修改宽屏检查的 `0x40B087/0x40B08C`、`0x403E30/0x404800` 入口和 `vtable[1]=0x40B050`。新增 Hook 只位于 `0x404800` 内部两个未经宽屏占用的绘制 CALL。自由地图查看时消息 ID 用临时非零值，使宽屏提前走“消息只画中央一次”的既有路径。退出屏障期间 ID 为 0，但宽屏侧画会把 target/current 临时清零，仍不会重复画消息 UI。

## 失败关闭

启动必须通过：固定基址、PE32 i386、场景/消息/说话人入口、F-Talk CALL、正文 CALL 和 vtable[1]。任一绘制 Hook 安装失败会恢复此前已装的窄 CALL，不启用 Backlog。SDL 缺失不属于失败，只关闭手柄能力。

---

## v0.3.3-test3 剧情旁路与 Public API 修正

### 剧情中打开

旧版在真实剧情进行时仍把原版消息全局改造成 synthetic/read-only 状态，这会让正在运行的
`0x403E30` 消息状态机或其它 ASI 看到一套半途改变的全局数据。

当前普通稳定剧情采用“零写入旁路”：
- 打开前只读取并确认 `mode=0/1`、`current=target=4`；
- `g_active=1` 后 SceneWorld update 完全冻结；
- 不写 `dialogue_id/mode/total/visible/display_buffer/speaker/name/current/target/event/map`；
- `0x404859` 当前人物图、`0x404899` 当前 F-Name、`0x4048E6` 当前姓名文字在绘制层跳过；
- `0x40486E` F-Talk 与 `0x4049FF` 正文继续展开成历史列表；
- 关闭时剧情旁路无需“恢复”任何 RPG.exe 对话全局，因为整个会话从未写过它们。

自由探索没有真实消息绘制载体，因此仍由 Backlog 建立 synthetic dialogue，并只在这条路径恢复自己修改过的事件/地图/消息字段。

### 手柄

`pad_bridge.c` 只做公共 ABI 绑定。输入所有权和确认/取消映射仍全部属于 PadSupport。


## v0.3.3-test4：SceneWorld update 分流修复

test3 为避免剧情中打开时继续推进真实消息状态机，把 Backlog 活动期间的 `SceneWorld update` 全部截断。
用户实机确认这会造成一个新的自由探索回归：synthetic dialogue 虽然写入了 `dialogue_id/current/target/display_buffer`，
但原版消息系统没有得到更新机会，`0x404800` 内的 `F-Talk` 与正文绘制 CALL 不会到达，因此 Backlog 只有输入、没有画面。

test4 改成严格分流：

- **剧情旁路**：继续完全冻结 `SceneWorld update`，保持“真实剧情全局零写入”的安全边界；
- **自由探索 synthetic**：继续调用前一层 `SceneWorld update`，恢复 v0.3.2 已验证的消息绘制准备行为；
- synthetic 模式仍同时设置地图输入门关闭、Event yield/block 和 `BACKLOG_READ_ONLY_MODE`，所以允许 update 运行不等于允许正常游戏推进。

## v0.3.4-test2 当前架构

活动态不再执行完整 SceneWorld update，只调用 `0x434500(scene_world,0xBB8)` 登记 draw。四个槽位使用
`backlog_panel_shift_y()`，位移最小 -330；姓名最终 Y 最小 0。F-Talk/F-Name 在一帧重复绘制时，
每次都从打开本轮前保存的 `+0x50/+0x54/+0x58/+0x5C` 游标开始，结束后恢复，不把 layer 状态留给原游戏。
私有 NamePanelPool 已删除；有姓名组合框复用原版 `0x46F658`。

## v0.3.4-test3 自适应容量

`backlog_visible_slot_capacity()` 根据间距返回 4 或 3。所有“可见数量、槽位编号、纵向位移、左右翻页”
都读取同一个容量，避免只改绘制而导航仍按四条。135 时三条位移为 -270/-135/0；160 时为 -320/-160/0。
