> **当前权威状态（2026-08-26）：Castle Backlog v0.3.3-test4。**
> 本版继续保留 v0.3.3-test3 的“剧情旁路零写入”，并修正 test3 的自由探索绘制回归：
> 普通稳定剧情（`dialogue_id!=0`、`mode=0/1`、`current=target=4`）打开 Backlog 时，
> 不改写原版 `dialogue_id/mode/buffer/speaker/current/target/event/map` 全局，并完全冻结
> `SceneWorld update`；自由探索 synthetic dialogue 则继续调用原版 `SceneWorld update`，只依靠
> event/map gate + read-only mode 阻断推进，以便原版消息系统真正进入 `F-Talk` / 正文绘制链。
> `mode=2/3` 和未知剧情模式继续 fail-closed。手柄协作只通过 `CastlePad_GetApi` Public API v1；
> PadSupport 不存在或 API 不可用时，Backlog 仍保持键盘/鼠标可用。
> 下文较早版本中与本段冲突的“当前方案”只按历史记录阅读。

# 第三方依赖说明

## 1. 运行时硬依赖

Castle Backlog 本身只依赖 RPG.exe、Win32 `kernel32/user32` 与游戏已经存在的资源。
**Backlog 不再加载 SDL3.dll，也不拥有任何手柄设备。**

键盘和鼠标始终由 Backlog 自己处理：
- Virtual-Key 名称配置；
- 鼠标滚轮打开/上下浏览；
- 鼠标右键退出。

## 2. Castle_PadSupport.asi 是可选协作依赖

单独安装 Backlog 时完全没有手柄输入，这属于设计行为。

若同时安装支持 Public API v1 的 `Castle_PadSupport.asi`，Backlog 运行时只做：

1. `GetModuleHandleA("Castle_PadSupport.asi")`；
2. `GetProcAddress(..., "CastlePad_GetApi")`；
3. 请求 `CASTLE_PAD_API_VERSION_1`；
4. 检查 `magic / struct_size / api_version / capability_flags`；
5. 只调用函数表，不扫描 PadSupport 内部内存。

Backlog 使用的公开能力：
- 物理 `LB`：打开/退出；
- 物理 D-Pad：历史导航；
- 语义 `CANCEL`：退出，自动遵守 PadSupport 的 `SwapConfirmCancel`；
- `AllowsExternalUiInput()`：调查模式、RT 临时鼠标、Back 常驻鼠标时阻断 Backlog 手柄输入。

## 3. 已废弃方案

以下方案均不得恢复：
- Backlog 自己初始化 SDL3；
- 扫描 `PadInput_Down()` / `ControlModes_BlocksMapMovement()` 短函数机器码；
- 搜索 `PadInputState` / `ControlModeState` 数据布局；
- 写死 PadSupport 模块基址偏移。

这些方案已在 v0.3.1/v0.3.2 实机证明会因编译器优化或结构变化失效。

## 4. 公共接口头文件

Backlog 源码随附 `Castle_PadSupport_API.h` 的 v1 契约副本。
该文件必须与 PadSupport 发布的同名公共头保持 ABI 一致。


## v0.3.3-test4 依赖边界

本轮没有修改 PadSupport Public API v1，也没有新增第三方依赖。
手柄正常工作这一实机结论保持：BACKLOG 仍只通过 `CastlePad_GetApi` 协作。
本轮修复完全位于 BACKLOG 自己的 SceneWorld 调度。
