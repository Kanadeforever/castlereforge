# 存档增强 RuntimeSDK 实机测试清单

## v0.3.0-test5本轮验收

test5仅增加槽位一像素描边开关，宿主1070项通过；下列标题重进/光标问题依然存在，不得勾为通过。

- [ ] `[Visual] SlotTextOutline=1`时，“新/自動/快速”都有一逻辑像素深色描边，原字色保留。
- [ ] `SlotTextOutline=0`关闭描边；修改后重启生效，未填写默认开启。
- [ ] 字重0/50/100变化时描边仍为一像素邻域；垂直偏移、红字明暗与已验收书卷提示不变。

### 后续已复现失败，修复后必须专项回归

- [ ] 标题读档 → 鼠标右键退标题 → 再进入，连续重复；新/自動/快速仍显示。test4失败，根因是阶段2被误当成仍在关闭。
- [ ] 16位实机下光标经过红“新”保持在上层；test4失败，运行时光标tile已转565，现解码仍按555。
- [ ] 补宿主测试：保存界面阶段0→1→2、active关闭再开启；运行时头+0x427C=0x41的RGB565光标数据。

以上尚未修复，详见完整接档的2026-09-24后续诊断。单纯Alt+Tab未确认触发，不将截图快捷键引起的界面退出混同为失焦。

用户已实测test1：提示、书卷正常，自动/快速标签显示；字太细、切换闪断、提示对比不足。
用户后续实测test2问题已在test3调整；用户已验收书卷/提示描边/无宽屏提示，换图自动保存后自动“新”也确认正常。
test4有1033项宿主检查，以下新改动与完整回归仍须复测，不以离线测试代实机。
后方旧test7字样只作历史迁移回归，不恢复私有手柄链。

- [ ] 日志版本为`v0.3.0-test5`，没有误用旧ASI。
- [ ] 字重0/25/50/75/100等细调生效，文字笔画完整；不影响书卷提示。
- [ ] 垂直偏移默认-2，实机与原槽号基线协调；负数上移、正数下移正常。
- [ ] 光标经过“新/自動/快速”始终在文字上方；透明处仍可见文字，手柄隐藏光标时不挖空文字。
- [ ] 不加载Widescreen时F5后显示书卷提示，日志记录默认4:3后端及实际绘制。
- [ ] 有/无Widescreen两种组合都显示槽位标签；若仍不显示，保留`[可视诊断]`行定位，不改其它插件。
- [ ] “自動/快速”笔画完整、字重合适，不遮挡槽号、等级、长地点名。
- [ ] 上下切换、鼠标划过各槽时不闪断；高亮动画与手柄手感不变。
- [ ] 0↔96循环分页及88～91边界页类型不串行、不贴错槽。
- [ ] 打开动作/确认框仍保留标识；实际开始读档立即隐藏，loading中不残留，重开列表恢复但不清除记录。
- [ ] 亮土黄背景和暗场景均能看清完整描边；书卷不变，无气泡底板。
- [ ] 完成约2.6秒、失败约4.2秒后消失，不残影；静音仍可见。
- [ ] 手动存1再自动存91可同时各有红“新”；手动再存2只移动手动标记。
- [ ] 自动再存92只移动自动标记；快速存档不抢任一标记。
- [ ] 红“新”缓慢明暗变化且始终可辨，`NewPulse=0`常亮。
- [ ] 读取、浏览、翻页不清除标记，重启仍分别保持。
- [ ] 空快速/自动槽有类型但无“新”，保存失败不移动标记。
- [ ] `.SAVESTATUS`位于multimedia/save，SE2/M/A/N四行、CRLF共26字节；迁移保留原M/A及轮换游标。
- [ ] 新文件成功写入后才删除旧`.LATESTSLOTS/.NEXTAUTOSLOT`；模拟目标不可写时保留旧文件，TSF不受影响。
- [ ] 备份后测试状态缺失/损坏：不崩溃、不猜旧档顺序，下次成功保存建立标记。
- [ ] 标题、天书、地图存档点，以及4:3/16:9/21:9、Alt-Tab恢复正常。
- [ ] Visual六开关与两个微调参数分别有效，关闭显示或缺资源不影响存档/轮换/输入。
- [ ] 保留槽禁写、F5/F9、安全fallback与自动轮换无回归。

> 当前配置为 `Castle_SaveEnhance.toml`，日志为 `mods/logs/Castle_SaveEnhance.log`；官方插件
> 必须携带同目录 Runtime。下文旧 test7 日志措辞和 INI 内部键检查按历史兼容项理解。
> 下方涉及.NEXTAUTOSLOT的历史轮换检查，test4请检查.SAVESTATUS的N字段；旧文件只在迁移前存在。

> test6 已确认 Win32 状态写入成功，但位置误为 `exe\Save`。test7 改到真实
> `multimedia\save`，并兼容迁移旧三字节游标。测试前备份整个存档目录。

## 1. 启动

- [ ] 原版 `RPG.exe` SHA-256 为 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`。
- [ ] 删除/移走旧 `AnytimeSave.asi`。
- [ ] 游戏正常启动。
- [ ] `mods/logs/Castle_SaveEnhance.log` 出现 `[装载] RuntimeHost：Hook/Input/Save/Clock/Path/File/Module/TOML 统一协调。`
- [ ] 随后出现 `[启动] 开始目标机器码预检查与兼容性检查。`
- [ ] 出现 `Runtime Save v1 已接管0号与91~99号手动存档禁用策略`。
- [ ] 随后出现 `SaveEnhance 存档事务已提交；SaveAction 策略由 Runtime Save v1 统一执行`。
- [ ] 最后出现 `[状态] SaveEnhance 已完整安装，可以开始实机功能测试。`
- [ ] 没有目标机器码失败/回滚严重错误。
- [ ] **如果上述启动阶段日志不完整，停止后续功能测试并保留日志。**

## 2. 0~99

- [ ] 第一页为 0、1、2、3。
- [ ] 第二页为 4、5、6、7。
- [ ] 旧 `Save001.TSF` 仍由 UI 槽 1 读取。
- [ ] 23 页附近 88、89、90、91 对应正确。
- [ ] 第 24 页为 92~95。
- [ ] 第 25 页为 96~99。
- [ ] 不能产生第 26 页或槽 100。

## 3. 循环分页

键鼠：

- [ ] 第一页执行上一页 -> 第 25 页。
- [ ] 第 25 页执行下一页 -> 第一页。
- [ ] 第 25 页上一页仍去第 24 页。
- [ ] 第一页下一页仍去第 2 页。

Controller：

- [ ] 同样能 0↔96 循环。
- [ ] 跨页后焦点行合理，不出现页面已变但焦点仍属于旧页。

## 4. 保留槽普通菜单只读

分别测试槽 0、91、99：

鼠标/键盘：

- [ ] “读档”可用。
- [ ] “存档”不可执行。
- [ ] 尝试保存前后对应 TSF 的 SHA-256 不变。

Controller：

- [ ] 打开槽 0、91、99 的三项窗口后，手柄默认焦点为“读档”，不能落到“存档”。
- [ ] 上/下只在“读档/取消”两项之间移动，不出现无高亮或多按一次才响应。
- [ ] 打开窗口使用的同一个 A 不得立即执行读档；松开后再次按 A 才确认当前项。
- [ ] “读档”和“取消”仍可执行。
- [ ] 切到鼠标/调查模式后输入正常；SaveEnhance没有强制焦点或私有手柄链。

普通槽 1、45、90：

- [ ] “存档”正常可执行。
- [ ] Controller 不被 SaveEnhance 误禁用。

## 5. F5 Quick Save

在普通自由地图：

- [ ] 按一次 F5 生成/更新 `Save\Save000.TSF`。
- [ ] 若日志先显示原版 gate 拒绝，但当前已有冻结锚点，应继续显示“使用安全 fallback 写 0 号”，随后 Quick Save 成功。
- [ ] 0 号从普通读档菜单可读。

在以下状态按 F5：

- [ ] 对话；
- [ ] 菜单；
- [ ] 存档界面；
- [ ] 商店/旅店等 UI；
- [ ] Battle；
- [ ] 剧情 transient；
- [ ] 地图切换瞬间；

都应拒绝，不排队，不使用入口 fallback。

## 6. F9 连按 Quick Load

默认 `QuickLoadPresses=2`：

- [ ] 单按一次 F9 不立即读档。
- [ ] 1200ms 内第二次 F9 才读取 0。
- [ ] 超时后第二次不能执行旧确认。
- [ ] `Save000.TSF` 不存在时失败但不崩溃。
- [ ] 读档成功后不会马上把“第一次看到的地图”当场景切换写自动档。

把 `QuickLoadPresses=3`：

- [ ] 前两次都不读，第三次才读。

## 7. Controller Quick

装当前 Castle_PadSupport：

- [ ] 插件日志显示 `Runtime Input v1 已接入`；实际提供者为 PadSupport 时组合键有效，加载顺序前后都测一次。
- [ ] RB+R3 在自由行动只触发一次 Quick Save；一直按住不会每 Tick 连存。
- [ ] 松开后重新组合可再次触发。
- [ ] RB+Start 第一次只进入 Quick Load 确认。
- [ ] 第二次组合完成读取。
- [ ] F9 一次 + RB+Start 一次也能完成两次确认。
- [ ] 菜单/剧情/战斗中 Controller 组合不执行 Quick 操作。

不装 PadSupport：

- [ ] SaveEnhance 仍正常启动。
- [ ] F5/F9 正常。

## 8. 自动档首次填充

为加速可暂设 `IntervalMinutes=1`。

- [ ] 第一次自动保存写 91。
- [ ] **第二次必须能识别 91 已存在并写 92**，第三次写 93；这是 test6 首要回归点。
- [ ] 后续依次填 94……99。
- [ ] 日志不应出现 `[自动存档诊断] ... 看不到槽文件`；若出现，立即保留完整日志。
- [ ] 不修改 0 或 1~90。

可以暂时删除某个自动档，例如 `Save094.TSF`：

- [ ] 下一次优先重新使用空缺 94，而不是依赖旧指针继续到别处。

## 9. 自动档全满环形覆盖

- [ ] 91~99 全部存在。
- [ ] `multimedia\save\.NEXTAUTOSLOT` 恰好为三个 ASCII 字节 `091`~`099`，没有换行。
- [ ] 记录九个文件的 hash 和状态文件当前槽号。
- [ ] 下一次自动保存只修改状态文件所指向的一个 TSF。
- [ ] 其它八份 hash 不变。
- [ ] 成功且游戏文件层回读确认后，状态文件推进到下一槽；99 后回到 91。
- [ ] 完全退出并重新启动游戏，下一次仍从状态文件记录的槽继续。
- [ ] 不出现任何 91->92 之类批量重命名。

## 10. 场景切换自动保存

可设 `IntervalMinutes=0` 单独测换图：

- [ ] 第一次进入地图只建立 baseline。
- [ ] A 图 -> B 图，日志先记录地图身份变化。
- [ ] 不在切图瞬间硬写。
- [ ] B 图连续稳定 8 Tick 后冻结锚点并补存。
- [ ] 读取该自动档正常。
- [ ] 快速连续 A->B->C 不把 B 的旧 anchor 写进 C。

## 11. 定时自动保存

- [ ] `IntervalMinutes=1` 稳定地图约 1 分钟后自动保存。
- [ ] 成功后定时基准从成功时间重新计算。
- [ ] `IntervalMinutes=0` 不再定时。
- [ ] `SaveOnSceneChange=1` 时即使定时=0，换图仍自动保存。

## 12. 安全 fallback 回归

最后再测试过去已知危险的楼梯/Scene Mode：

- [ ] 普通菜单安全随时存档原有行为无回归。
- [ ] 原版禁存 transient 只有已有 frozen anchor 时才走 Writer header fallback。
- [ ] 读取后回安全入口，而不是坏档/崩溃。
- [ ] Quick Save 在该 transient 直接失败，不假装保存入口。
- [ ] AutoSave pending 可等安全后补存。

## 13. 声音

放一个自己选择的 WAV，例如 `ok.wav`：

- [ ] `QuickSaveSuccess=ok.wav` 时成功播放。
- [ ] 文件名空白时不播放。
- [ ] 写不存在的文件时不播放且操作仍正常。
- [ ] 写 `..\x.wav` / `C:\x.wav` 等非法内容被静默禁用。
- [ ] `AutoSaveSuccess` 留空时后台自动存档不打扰。
- [ ] `Volume=100` 时响度与原 WAV 基本一致。
- [ ] `Volume=70` 明显低于 100，但音质/速度正常。
- [ ] `Volume=30` 进一步降低。
- [ ] `Volume=0` 完全静音，但存档/读档结果不受影响。
- [ ] 使用 16-bit PCM WAV 验证 1~99 缩放；若故意使用不支持编码且 Volume<100，应只静默不播放，不崩溃。

## 14. 长时间

- [ ] 连续运行 30~60 分钟。
- [ ] 多次 Quick/Auto/手动保存后仍可读。
- [ ] 91~99 持续滚动，不越来越慢。
- [ ] 日志不按 20Hz 刷同一个错误。
- [ ] 游戏正常退出。

## 15. 候选升级条件

本轮可视验收及以下核心回归全部无问题，才考虑从测试版升为稳定候选：

- [ ] 无错槽覆盖；
- [ ] 0/91~99 普通菜单绝对不能实际保存；
- [ ] 1~90 正常；
- [ ] Quick Save/Load 正常；
- [ ] Controller 实际不绕过 disabled；
- [ ] 循环分页正常；
- [ ] 自动滚动正常；
- [ ] 危险楼梯 fallback 无回归；
- [ ] 无崩溃。


## test7 专项：91~99 轮换

- [ ] 第一次自动存档日志为槽 91；
- [ ] 紧接着出现 `[自动槽] 游戏文件层已确认槽 91 可读取`；
- [ ] 第二次自动存档必须为槽 92；
- [ ] 继续换图应依次 93、94……99；
- [ ] 第一次成功后 `multimedia\save\.NEXTAUTOSLOT` 创建且内容为 `092`；
- [ ] 游戏启动、进入地图及第一次自动保存全程不崩溃；
- [ ] 日志出现 `[自动槽状态] 已写入 multimedia\save\.NEXTAUTOSLOT，下一候选=92。`；
- [ ] 不再出现 test5 的 `[自动槽状态] 无法创建 Save\.NEXTAUTOSLOT`；
- [ ] 若存在 test6 旧 `exe\Save\.NEXTAUTOSLOT`，首次启动应继承其值，正确写入后删除旧文件；
- [ ] 完全退出并重启后仍读取新文件，不依赖 INI 中的 `[Internal]`；
- [ ] 91~99 全满后，下次按 `multimedia\save\.NEXTAUTOSLOT` 环形覆盖；
- [ ] 把状态文件改成非法内容或移走后，插件应记录回到 91，而不是写越界槽；
- [ ] 删除中间任一自动档后，下一次应优先补最低编号空槽；
- [ ] 不应再出现 test4 的“保存成功但 Win32 看不到文件”诊断。
