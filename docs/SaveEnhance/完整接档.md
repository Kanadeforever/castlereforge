# 《幽城幻剑录》存档增强截至该版本的完整接档

## 当前接档：2026-09-24，v0.3.0-test7 RuntimeSDK

**用户实机验收通过：用户明确反馈“测完了，我这边验收通过，都修好了，可以直接提交”。**
本轮负数字重、光标遮挡及标题读档右键退出后重新进入三项已关闭，用户授权直接提交。
这是用户实机结论；1359项宿主检查及62项静态检查为另两类证据，不相互替代，也不扩张为整仓所有组合验收。

test5仅新增`[Visual] SlotTextOutline=1`：为槽位“新/自動/快速”统一开启一逻辑像素深褐近黑描边，0关闭。
复用DrawWord已有八方向描边，不改变SlotTextWeight/SlotTextOffsetY、红字脉动、书卷提示或状态协议，重启生效。
test5历史1070项宿主检查通过，新增37项验证三种槽位文字在0/50/100字重下的开关、固定描边邻域和字色。
test6实际修复标题重进的显示判定：允许原版已知初始态0/完成态2，仍拦截过渡态1、未知阶段及真实读档交接。
新增16项回归先在test5代码上复现4项失败，修复后1086项全部通过。测试保留同一个对象，阶段1→2，active关闭再开启，
连续重进三次，并检查不写原生字段、不清除M/A、读档期间仍隐藏。标题重进修复已获用户实机验收。
test7按用户顺序实现负数字重，再修运行时光标颜色格式；保留test6标题重进修复，不改Controller、书卷提示或.SAVESTATUS。
当前1359项宿主检查通过。光标转换回归先在旧解码器复现13项失败，再按SF2头+0x427C标记选择565后消除；
负数字重、光标与标题重进已获用户实机验收，不能把宿主通过当作这条用户确认的来源。构建和静态结果见本目录验证报告。

### test7负数字重与光标修正

- SlotTextWeight现为有符号-100～200，默认0。正数仍表示额外0.01像素/单位；负数表示边缘减轻强度，
  -100完全采用原24点阵到20像素的真实面积覆盖率，-50在旧全覆盖与真实覆盖率间取中间效果。不是直接删像素或整字透明化。
- 原0及正值外观不变；完全覆盖的笔画始终100%不透明。负字重的描边按一像素邻域覆盖差绘制，每点只混合一次，
  避免多次叠黑抵消减轻效果。字重、描边开关和垂直偏移独立，修改TOML后重启生效；16位颜色仍有量化。
- 光标按资源自身标记读取：+0x427C=0x41时RGB565，否则RGB555，不按输出表面位深猜测。
  磁盘书卷继续默认RGB555，25帧像素对照仍通过；不新增Hook、不重画光标、不改变既有匹配阈值。
- 测试私有内存按原0x44CD7B的公式转换RLE字面/重复颜色，再设置0x41；预期画面从转换前颜色独立计算，
  不用被测解码结果自证。覆盖555/565资源、16/32位表面、4:3/16:9/21:9及显隐、透明孔、动画/位置不变。
- 新增一次性日志“已按RGB565读取原版转换后的运行时光标资源”，成功保护仍有原“光标不透明像素”日志。

本节是当前实现的权威摘要；后方“历史档案”完整保留旧版证据，仅作追溯。
历史档案中的INI、Standalone、PadSupport私有链、test7待测项与旧产物哈希不能当作当前实现或本轮验收。

### 已确认与尚待确认

**2026-09-24问题闭环：标题重进在test6代码修复，光标格式在test7代码修复，随test7获得用户实机验收。**

- 精确复现：标题进入读档，鼠标右键回标题，再次进入；“新/自動/快速”全部不显示。单纯Alt+Tab未证实触发。
  `.SAVESTATUS`仍为M=014/A=096/N=097，无需删除或重建状态文件。
- 根因：`SaveVisual.cpp::VisibleSave`以`SaveSlot+0x580 != 0`拒绝绘制。原版`0x424C6A`把过渡结束值设为2，
  标题`0x448110`复用原SaveSlot并重新开启active，没有把其阶段值恢复为0，故第二次进入被永久误挡。
  test6已区分当前读档/关闭与完成态；未通过清零原版对象字段强迫显示。
- 光标旧失败原因：原版`0x428E10`在16位模式调用`0x44CD7B`原地转换tile颜色为RGB565，并把SF2头`+0x427C`标为`0x41`。
  test6及之前仍按Color555解释该内存，导致CursorAlreadyVisible匹配失败。test7已区分555/565，不放宽阈值或重画光标。
- test4的1033项测试只模拟阶段1回0及磁盘光标，不能证明真实重开/内存565通过。
  test6补上标题完成态复用，test7补上运行时565覆盖；随后用户单独确认实机通过，形成闭环。
- 用户test5日志中M/A/N已正确读取，且出现实际绘制日志；“活动SaveSlot没有有效行”也可能来自入场动画。
  test6将该条改为一次性“暂未取得有效行”提示，并在阶段2且有效行恢复时记录“已识别复用完成态的活动存档列表”。

- 用户此前已确认手柄和存档槽位操作顺畅；旧迟钝问题已经解决，不应重做手柄控制。
- 用户实测v0.3.0-test1：存档提示正常工作，书卷动画正常显示，自动/快速槽位标签已显示。
- 同轮反馈：标签太细，切换选中存档时闪断，浅金提示文字与土黄背景对比不足。
- test2修复：保留笔画的缩放并加粗、消除高亮切换引起的停画、提示文字加完整深色描边。
- 用户随后实测test2：自动/快速标签消失，不加载Widescreen时提示不显示，但Quick保存成功。
- 已确定无宽屏原因：Display默认后端正常返回正值2，被`!=CASTLE_OK`误拒绝。test3改为仅负值失败。
- test3同时移除父/行更新开关的额外门槛并补诊断。用户已明确验收：提示描边舒服，关闭宽屏插件也正常；此部分后续保持不变。
- 用户最初报自动“新”没有，状态为M=014/A=999；随后打开换图存档实测确认自动“新”正常写入，撤回故障判断。
- 用户进一步要求：粗细要细调，不接受三挡；新增文字略偏下；开始读档才隐藏且loading不残留；两个状态文件合并。
- test4新增`SlotTextWeight=0～200`（每1额外0.01像素、边缘混色）、`SlotTextOffsetY=-2`（范围-12～12），只影响槽位文字。
- test4通过实际菜单LoadSlot调用通知及原版退出阶段隐藏标签；仅打开确认框不再隐藏，不清除最新记录。
- test4将M/A/N合并原子写入`.SAVESTATUS`，成功迁移后清理旧状态文件，失败保留旧文件。
- 另外修复红“新”压住光标的问题：只读当前光标图形并保留其已绘制的不透明像素；不重画光标，不触碰Controller显隐/动画。
- test4当时1033项宿主检查、0失败；当前test7为1359项，最终MSVC x86构建与静态结果见[验证报告](静态验证结果.json)。
- test4时这些新增项尚待复测；本轮test7负字重、标题重进和光标遮挡已获确认，未逐项反馈的扩展测试不自动补勾。
- 本轮只改`src/SaveEnhance`及`docs/SaveEnhance`，未修改Controller、RuntimeSDK、Widescreen、Quest或Backlog。
- `参考资料`只读，`src/SaveEnhance/backup`不读取、不修改、不参加构建。用户已明确授权“修完图层等工作做完后直接提交”。

### 当前架构与存档核心

官方插件必须与`Castle_Runtime.dll`同目录；可以不装Mod Loader及其它业务插件。
`DllMain`只记录模块，正式初始化由Runtime Client生命周期进入。配置为ASI同目录`Castle_SaveEnhance.toml`，
日志为`mods/logs/Castle_SaveEnhance.log`。Mod Loader场景经过Loader-ready门，不能恢复Loader Lock内完整初始化。

| 服务 | 本插件职责 |
|---|---|
| Hook | 十个菜单补丁、两个分页helper、原五个存档CALL及一处菜单LoadSlot通知，与旧save gate恢复统一事务 |
| Save | 只登记0和91～99普通手动禁写策略；Runtime统一包装SaveAction，deny-wins |
| Input | 读取语义快捷动作，不按PadSupport文件名直连，不写手柄焦点/按钮字段 |
| TOML / Log | 只读配置、独立日志 |
| Path / File / Module | WAV路径、文件读取、可选系统音效依赖 |
| Clock | 连按、定时保存、书卷节拍和红色“新”明暗变化 |
| Overlay / Display | Present前叠加绘制与最终画面几何；不新增Present Hook或图形设备 |

槽位0为Quick，1～90为Manual，91～99为Rolling Auto，25页×4行；0和91～99普通菜单只读。
F5快速存档；F9默认2次/1200ms确认快速读档。手柄语义快捷为RB+R3 / RB+Start，缺少提供者时键盘继续可用。
自动档先填最低空槽，再按`.SAVESTATUS`的N字段轮换；第一次观察地图及成功快速读档后只建立基准。

安全锚点继续沿用已验证方案：default Scene Mode连续8个严格自由行动Tick，冻结稳定窗口首个样本。
需要fallback时仅在Writer内暂替换World头`0xC8`字节，返回立即恢复；World总长`0x27D8`，后方GameVar不回滚。
Quick仍受严格自由行动门约束，自动保存可pending等待安全时机；不改变保存调用顺序，不批量重命名TSF。

TSF由原游戏函数读写，存在性由游戏File Open判断。旁挂元数据位于ASI根外，沿用已验收的Win32路径/文本文件路线，
不调用曾导致崩溃的游戏File Read/Write，不绕过Runtime File的相对路径限制，也不引入没有消费者的通用`.state`。

### 书卷与提示

- 只读`multimedia/Sys/Sys.dat`索引中的`CURSOR.SF2`，不把样本物理偏移硬编码进插件。
- 正式书卷为Section1图像33～57、Section0组合159～183，共25帧；用户所贴静态PNG对应图像48。
- 每帧原持续值为2 LegacyTick；本版视觉节拍100ms/帧，2500ms循环，不声称已对齐原鼠标时钟。
- 保留逐帧相对坐标，RGB555 RLE解码，透明像素不覆盖背景；资源越界/损坏/未知压缩安全拒绝。
- 文字读取原版`Font24.Fnt`所需11字的24×24单色点阵，不换系统字体，不调用未经闭合的原版文本绘制ABI。
- 默认右下，`BottomRight=0`改右上；位置取Runtime Display的最终画面几何，不硬编码宽屏侧宽。
- 原版繁体字形：“自動存檔”“快速存檔”“存檔完成”“存檔失敗”。完成约2600ms，失败约4200ms。
- 原版保存同步占用游戏线程，写盘期间不能保证动画继续播放；同帧完成时直接显示真实结果，不为动画延迟保存或另起线程。
- 从实际绘制首帧起计时；test2浅金/失败浅红文字有一逻辑像素深褐近黑八方向描边，无气泡、底板或英文。

### 槽位标签与独立红色“新”

- 原槽号不动，两字“自動/快速”在左，再向左是红“新”；手动槽不加“手動”。
- 使用20像素字格，覆盖采样保留原24点阵笔画；test1直接取样至18丢细线，已修复。
- `SlotTextWeight`默认0，不额外加粗；25/50/100/200分别额外0.25/0.5/1/2像素，小数部分混合边缘与背景色，16位色存在量化。
  原test3字重相当于100。`SlotTextOffsetY`默认-2，负数上移；类型与“新”共用基线。两项重启生效，不影响书卷提示。
- 红“新”默认1600ms周期亮红/暗红变化，始终可见；仅整数三角波借现有Clock，不增加线程、定时器或动画系统。
- 每帧读取原槽号控件父坐标、相对位置和运行时偏移；不按截图像素定位、不缓存跨帧UI指针。
- `SaveSlot+0x5BC`是选中高亮动画倒数，每次换选中项设4，并非翻页锁。test1误判导致闪断；test2不再因此停画。
- 控件`+0x44`显示槽号必须等于页基址+行号，避免错贴；标题读档、天书、地图存档点共用只读路径。
- 打开动作窗/确认框仍保留标签，真正开始LoadSlot时隐藏；原版`+0x5B9`及`+0x580`关闭阶段接续隐藏到loading结束。
  空保留槽仍显示类型但无“新”，正常重新进入列表恢复显示；隐藏与删除最新记录是两件事。
- 自动与手动各有一个最新标记；快速槽不建立第三套标记，也不抢另外两类；浏览、选中、读档不清除。

`multimedia/save/.SAVESTATUS`统一保存M（最新手动）、A（最新自动）、N（下一覆盖）。格式为四行：

```text
SE2
M=014
A=096
N=097
```

写出26个ASCII字节、CRLF，读取也接受LF。M只许1～90或999，A只许91～99或999，N只许91～99。
999表示该分类尚未记录成功保存；N不能用来倒推A。三个字段一次原子写入，自动保存同时更新A/N，手动只改M。

手动从已有菜单/隐藏命令SaveSlot调用成功返回后更新，自动从保存成功且File Open回读后更新；File Open仅证明能打开，
不是新增TSF完整性校验。失败不移动“新”。状态缺失/损坏时不按槽号、文件时间或轮换游标猜测，下一次成功保存才建立标记。
先写`.SAVESTATUS-TMP`、Flush、关闭，再同目录替换；失败保留旧元数据并清理临时文件，当前TSF与内存状态不回滚。
手工复制/替换TSF不会自动重算“最新”，本版只跟踪插件观察到的成功保存。

有效`.SAVESTATUS`优先；缺失/无效时读取旧`.LATESTSLOTS`（16字节LF或19字节CRLF），再读取真实存档目录的三字节
`.NEXTAUTOSLOT`，无效才尝试历史`exe/Save/.NEXTAUTOSLOT`。新文件成功写入后才删除这些旧元数据，不删除目录或TSF。
迁移失败保留旧文件，下次可重试。没有合法状态则M/A未知、N=91；不写回TOML或注释。

### 绘制边界、地址和资源

在Runtime BEFORE_PRESENT回调借用`renderer+8`的DirectDraw表面，核对Display代次与最终尺寸，未知/过渡状态跳过。
明确RGB掩码的16/24/32位与负行距受支持；每次Lock成功都配对Unlock，不静态导入ddraw/gdi32，不新建窗口。
初始化缓存25帧和11字，绘制帧不读盘、不分配内存；可视资源或服务失败只降级反馈，不拆掉存档核心。

光标层级：只在槽位文字绘制期间，从原MouseManager的当前SF2帧解码小型透明遮罩，按Display中央偏移定位。
先核对最终画面确实已有大多数光标颜色，才保护不透明像素；透明孔仍可见文字。这样Controller隐藏光标时不会整块挖空标签。
不调用MouseDraw（它会推进动画），不Hook光标、不修改位置/显隐。16位通道展开与再打包有至多9级量化误差，
不能误用8位通道的8级容差。未知图形/多层或非受支持格式不猜测，安全跳过保护；本轮原版光标场景已获实机确认。

- 锁定台湾第三版`RPG.exe`：SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`，基址`0x400000`。
- gate/MapTick/SaveSlot/Writer/LoadSlot：`0x40A0C0` / `0x40ADE0` / `0x43B320` / `0x43B360` / `0x43B4D0`。
- prepare/post-load：`0x44B150` / `0x44B1F0`；File ctor/dtor/open：`0x4416F0` / `0x441710` / `0x4417C0`。
- 三个owner：`[0x8DED0C]+0x654`、`[0x89FCD0]+0x580`、`[0x8E241C]+0x5B4`，字段内是指针。
- SaveSlot vtable `0x460B50`，行vtable `0x460B70`；行数组`+0x584`，页基址`+0x598`，行有效档标志`+0x57C`。
- 行原槽号控件`+0x5A4`，坐标公式由`0x431680`闭合；原相对坐标540/10，不修改它。
- MouseManager全局`0x8E1C4C`，位置`+0x238/+0x23C`、sprite `+0x240`、原绘制开关`+0x248`；
  sprite的parser `+0x35C`，parser原SF2 `+0x20`、刚选择帧`+0x38`，原MouseDraw锚点319/260。
- 菜单读档CALL `0x424D98 -> 0x43B4D0`在Runtime事务中只通知标签隐藏；返回值和参数原样传递。
  `0x424DBC`置`SaveSlot+0x5B9=1`，`0x424E71`转`+0x580=1`，避免旧对象仍active时覆盖loading。
- Font24：SHA-256 `37ccd58694209b0dee4e2d6d1b6eca8e7bd56e9f21bf67f95bf53fdd98f291b7`，每字72字节。
- Sys.dat：SHA-256 `fef92575d7ff968ac73d85c745ff35c24add5e17e1ffbc4caca095ad66e6c3ea`；光标样本偏移59272720、长145753。

更多原字节、World字段及历史地址见[静态逆向证据与地址表](静态逆向证据与地址表.md)。
研究参照仓库已有SF2/资源容器工具及Controller/Quest/Widescreen的只读实现，没有修改被参照模块。

### 文件职责、构建和验证

- `source/Castle_SaveEnhance.cpp`：保存核心、SDK生命周期与可视结果调用点。
- `source/SaveVisual.cpp` / `SaveVisual.h`：资源加载、UI取位、像素绘制、最新状态持久化和接口。
- `source/SaveVisualCore.h`：RLE、边界、字形覆盖采样、状态协议、红色脉动等无进程依赖算法。
- `tools/test_savevisual.py` / `visual_host_tests.cpp`：真实资源只读+状态/UI/DirectDraw模拟宿主测试。
- `tools/verify_saveenhance_candidate.py`：EXE、MiscInfo、最终ASI静态检查。
- `templete/Castle_SaveEnhance.toml`：Visual六个开关和两个微调数值；声音与自动参数不因可视功能改变。

唯一构建说明是[源码目录readme](../../src/SaveEnhance/readme.md)。build.bat自动发现x86工具链，只输出根build下的ASI和TOML，
不复制Markdown、不含批处理注释，保留echo原有尾随空格。成功/失败清理本模块`_build`和`tools/__pycache__`，不清其它插件。
宿主测试现共1359项，覆盖25帧独立解码对照、576个源像素缩小不丢、三种owner、切换不闪断、状态原子替换失败、像素格式、描边与锁失败，
还包含默认Display成功返回2、负值失败与更新开关不阻挡可见槽号。test2宿主只模拟返回0，未覆盖真实默认后端，是测试遗漏。
test4增加201种粗细、基线偏移、真实自动/手动“新”像素、读档阶段隐藏、合并格式/迁移/失败保留旧文件测试。
还覆盖三种画幅在16/32位下的光标遮挡、不透明/透明区域、隐藏光标，以及位置/动画完全不改写。
最终静态报告见[静态验证输出](静态验证输出.txt)。测试运行器临时文件全部隔离于本模块`_build`并清理。

环境踩坑：本次PATH重复超长使cmd误报chcp/findstr不存在，只在验证子进程去重，未改系统设置。
Python3.14临时目录0700权限曾阻断受限账户，工具改用项目内随机目录继承权限；两次失败临时目录已按精确路径删除。
不得为了清理测试递归删除工作区、模块根或用户临时目录根。

### 历史禁区与下一步

- 不恢复INI内部游标、exe/Save新写入、游戏内部File Read/Write、无条件save gate、批量改名91～99。
- 不恢复旧test7私有PadSupport链或双焦点状态机；SaveAction禁写归Runtime，导航归Controller。
- 不把对话气泡60～72当书卷，不把原字库“216字节/字”的旧猜测当证据。
- 旧TSA/YCS精确恢复与通用Runtime.State没有加入本版。

本轮负字重、光标层级和标题重进已完成用户验收并获提交授权，不再列为当前阻塞项。
[实机清单](实机测试清单.md)保留重启、空槽、三种入口、不同画幅等扩展回归，未逐项反馈的不扩张为全部通过。
下一步等待用户确定新的增强项；不自行改动已验收的书卷、描边、无宽屏提示或自动“新”。
更远的自动触发合并、读档保护期、重要档锁定/转手动、精确恢复仍是讨论项，本版未实现。

---

## 历史档案：以下为旧test7及迁移阶段记录，不代表当前实现

下方所有“当前”“待测”“下一步”均指该历史记录形成时；以本文上方2026-09-24接档为准。

> **2026-09-07 RuntimeSDK 当前状态：** SaveEnhance 官方版强制依赖 Runtime。固定菜单、分页和
> 存档 CALL 使用 Hook 事务；SaveAction 改由 Runtime Save 单一包装并执行 deny-wins，不再与
> Controller 串私有链。Input、Module、Path、File、TOML、Log 已接入；配置为
> `Castle_SaveEnhance.toml`，日志为 `mods/logs/Castle_SaveEnhance.log`。WAV 缩放读取经 File。
> `.NEXTAUTOSLOT` 保持既有三字节路径/格式，它不是 `.state`。下文 Standalone、INI 和旧链仅按历史阅读。
>
> **2026-08-31 历史迁移阶段：** 当时固定菜单、分页 helper、五个存档 CALL 和 SaveAction
> 曾进入同一 Runtime 事务并与 Controller 串链。RuntimeSDK 联合运行已通过完整资源环境验收；
> 存档插件自身既有已知问题继续按本文测试清单处理。

> 日期：2026-08-28
> 正式插件：`Castle_SaveEnhance.asi`
> 目标游戏：台湾第三版原版 `RPG.exe`
> 目标 SHA-256：`8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`
> 当前构建产物 SHA-256：`db9ccdb1b3a2cfac6bd0d6f32b9a8f69d78a212bc3246f0ec43a935407ea5bed`

本文是源码包和文档包的独立接档入口，汇总当前架构、地址、已确认结论、失败方案、构建方法、测试边界、阻塞项和下一步。只拿到其中一个包，也应能据此继续工作。

## 一、当前状态

当前源码是旧 `AnytimeSave` 的完整升级替代，不应同时加载 `AnytimeSave.asi`。旧实现只保存在 `src/SaveEnhance/backup` 供历史对照，正式构建、验证和文档均忽略该目录。

已经由用户实机确认的结论：

- test3 已进入 Castle Mod Loader 的 `InitializeASI` 正式生命周期并完成全部 Hook 安装；
- test4 的 F5 Quick Save 修复成功；
- F9 Quick Load 主链正常；
- 外置 WAV 能正常播放；
- test4 自动保存能调用原版 SaveSlot 成功，但 Win32 文件探测看不到游戏自定义文件层，导致每次误选 91。

test5 已确认：

- 自动档存在判断改为游戏自己的 File 对象；
- 91~99 有空槽时按最低编号填空；
- 九槽全满后按持久化环形游标覆盖；
- 自动槽 91 能成功写入，游戏 File 层也能回读；
- 但 Win32 无法在 `RPG.exe\Save` 创建 `.NEXTAUTOSLOT`，重启后游标仍会丢失。

test6 已实机确认：

- 放弃会导致实机崩溃的游戏内部 `File::Read/Write` 试验；该提交已经单独回退；
- 保持与日志相同的 Win32 `CreateFileW/WriteFile` 路线；
- 日志与状态文件共用同一个“模块路径 → 所在目录 → 追加文件名”构造器；
- Win32 三字节状态写入成功且不再崩溃；
- 但文件被写到 `exe\Save`，该目录不是游戏真实存档目录。

test7 当前候选：

- 状态路径改为 `RPG.exe` 目录上一级的 `multimedia\save\.NEXTAUTOSLOT`；
- 新路径无合法游标时兼容读取 test6 旧 `exe\Save`，正确写入后删除旧文件；
- 恢复 owner 指针解引用，0、91~99 的存档按钮能真正进入 disabled；
- 不修改 PadSupport 源码或模块，只读取 Public API v1；
- SaveEnhance 自己在 RPG.exe SaveAction Hit/Event 外层维护“读档/取消”两项焦点，普通槽完整链回原目标；
- MSVC x86 `/W4 /WX` 与 45 项静态验证全部 PASS，等待实机验收。

尚未由用户实机确认：

- `multimedia\save\.NEXTAUTOSLOT` 是否创建并记录 `092`；
- test6 旧 `exe\Save` 游标是否只迁移一次；
- 新状态文件能否跨游戏重启保持 91~99 的环形覆盖顺序；
- 保留槽手柄是否默认落到读档，并在读档/取消之间无延迟切换；
- 0/91~99 菜单禁写、Controller、循环分页和长期运行的完整回归。

静态 PASS 不能替代这些实机结论。

## 二、正式功能

### 槽位

- 0：Quick Save，只允许 F5 或 Controller 快捷保存写入，普通菜单只读；
- 1~90：普通手动存档；
- 91~99：Rolling Auto Save，普通菜单只读；
- 页面从原版 8 页×4 扩展为 25 页×4，编号为 0~99；
- 第一页执行上一页跳到 96~99，最后一页执行下一页跳回 0~3。

### 快捷操作

- F5：Quick Save 到槽 0；
- F9 连按 2 或 3 次：Quick Load 槽 0；
- RB+R3：Controller Quick Save；
- RB+Start：Controller Quick Load 请求；
- 键盘和 Controller 共用同一个 Quick Load 连按状态机。

Quick Save 只允许严格自由行动。原版 save gate 即使在自由行动时仍可能拒绝；当前地图已有冻结安全锚点时，Quick Save 可以复用安全 fallback 写槽 0。对话、战斗、菜单、存档界面、剧情 transient 和切图瞬间仍直接拒绝。

### 自动保存

触发源：

- 真正切换地图；
- `IntervalMinutes` 定时。

第一次观察地图或刚完成 Quick Load 时只建立基准，不立即自动保存。换图或到时时如果当前不安全，任务保持 pending，等新地图建立冻结锚点后再写。

槽位选择：

1. 用游戏 File 对象依次检查 91~99；
2. 有空槽时选择最低编号空槽；
3. 全部存在时读取内存中的环形游标；
4. 原版 SaveSlot 成功后，再用游戏 File 层回读刚写入的槽；
5. 只有回读成功才推进并持久化下一游标。

每次最多改一个 TSF，不做 91→92 的批量重命名。

### `multimedia\save\.NEXTAUTOSLOT`

这是自动轮换元数据，不是 INI 配置，也不是游戏进度：

- 与日志共用同一路径构造器，以 `RPG.exe` 为模块基准追加
  `..\multimedia\save\.NEXTAUTOSLOT`；
- 写入前会创建缺失的 `multimedia\save` 最后一级目录；
- 新路径无有效游标时读取 test6 旧 `exe\Save`，正确写入后删除旧文件但不删除目录；
- 文件名以点开头、没有后缀；代码没有额外设置 Windows Hidden 属性；
- 内容固定为三个 ASCII 字节 `091`~`099`，没有换行；
- 文件不存在、长度不是 3、不是数字或不在 91~99 时，安全回到 91；
- 有空槽时仍优先补最低编号空槽，游标不会覆盖空槽策略；
- 自动存档成功但状态文件写失败时，本次 TSF 仍有效，当前进程继续使用内存游标；下次启动可能回到 91，并在日志中明确报告；
- 第一次成功自动存档前文件可以不存在，这是正常首次运行状态。

INI 中不再允许或生成 `[Internal] NextAutoSlot`，ASI 导入表也不再包含 `WritePrivateProfileStringW`。

### 安全 fallback

旧无条件随时存档会把 `0x40A0C0` 强制改成返回 1，已知会在楼梯/多层 Scene Mode 形成坏档。当前方案：

1. 每张地图只在 default Scene Mode 下采样；
2. 原版 Map Tick 先执行，插件后观察；
3. 连续稳定 8 Tick；
4. 冻结稳定窗口第一个样本作为入口级锚点；
5. 原版禁存但允许扩展时，Writer 期间只临时替换 World 前 `0xC8` 字节；
6. Writer 返回立即恢复实时 World；
7. `0xC8` 后 5000 个 GameVar 不回滚。

用户已在磐沙堡楼梯危险点确认旧 AnytimeSave 的这条安全 fallback 能生成可读 TSF，读档回到入口级安全位置而不是形成坏档。这是沿用的实机正向证据，不代表所有特殊场景已全部验证。

### 保留槽双重写保护

对于槽 0、91~99：

- SaveAction Update 期间临时把“存档”按钮设为原版 disabled；
- Update 返回立即恢复按钮状态，避免 UI 对象生命周期污染；
- 菜单和隐藏命令的 SaveSlot 调用层再次拦截保留槽；
- “读档”和“取消”保持可用；
- owner 的 SaveSlot 字段先解引用成真实 `SaveSlot*`，不再把字段地址误当对象；
- 无 PadSupport 时原版 Hit/Event 自然尊重 disabled；
- 有 PadSupport 时只读取 Public API v1，SaveEnhance 自己维护 index1=读档、index0=取消两项焦点；
- 打开窗口的同一个 A 必须先释放，防止立即执行默认读档；
- PadSupport 进入鼠标/调查模式时，SaveEnhance 取消强制焦点并链回原目标；
- 普通槽的 Hit/Event 完整调用安装前目标，不改变 PadSupport 普通存读档行为。

### 声音

INI 只填写单个外置 WAV 文件名：

- 空、非法路径、不存在、winmm 不可用或播放失败时静默；
- `Volume=0` 静音；
- `Volume=1~99` 读取 WAV 到进程堆，只缩放 PCM 8/16/24/32-bit 或 extensible PCM 的内存副本；
- `Volume=100` 直接交给 Windows 原样播放；
- 不修改磁盘 WAV、游戏音量或 Windows 总音量；
- user32 和 winmm 都是运行时可选动态依赖。

## 三、配置

当前 TOML 位于 ASI 同目录，名称 `Castle_SaveEnhance.toml`；本节后续 INI 代码块保存旧版本键语义，
最新逐键说明见《安装与TOML配置说明.md》。

```ini
[Quick]
Enable=1
ControllerEnable=1
QuickLoadPresses=2
QuickLoadWindowMs=1200

[AutoSave]
Enable=1
SaveOnSceneChange=1
IntervalMinutes=5

[Sound]
Volume=70
QuickSaveSuccess=
QuickSaveFailed=
QuickLoadConfirm=
QuickLoadSuccess=
QuickLoadFailed=
AutoSaveSuccess=
AutoSaveFailed=
```

范围：

- `QuickLoadPresses`：2~3；
- `QuickLoadWindowMs`：300~3000；
- `IntervalMinutes`：0~1440，0 关闭定时；
- `Volume`：0~100。

不要再向 INI 添加 `[Internal]` 或 `NextAutoSlot`。

## 四、关键地址与结构

所有地址只对本文锁定的原版 EXE 成立，未知哈希必须重新逆向，不能只改 expected bytes 强行放行。

### 10 个固定菜单补丁

| VA | 原版字节 | 作用 |
|---|---|---|
| `0x424D8D` | `8D 54 01 01` | LoadSlot 槽号改为 0 基 |
| `0x424DE7` | `8D 4C 02 01` | SaveSlot 槽号改为 0 基 |
| `0x425324` | `8D 54 02 01` | 聚焦控件槽号 |
| `0x4253C0` | `8D 54 10 01` | 元数据槽号 |
| `0x425486` | `8D 4C 10 01` | 选择标签槽号 |
| `0x4255BA` | `8D 54 11 01` | 行标签槽号 |
| `0x424ECB` | `8B 8A C2 03 00 00` | 页数改为 25 |
| `0x42565A` | `8B 91 C2 03 00 00` | 箭头页数改为 25 |
| `0x425626` | `7E 11` | 首页 previous 边界 |
| `0x425665` | `7D 11` | 末页 next 边界 |

循环分页读取点：

- `0x424E90`；
- `0x424EC2`。

### 5 个 CALL Hook

| VA | 作用 |
|---|---|
| `0x40CCC2` | 正常菜单保存许可 |
| `0x40B078` | Map Tick |
| `0x43B34C` | Writer |
| `0x424DF2` | 普通菜单 SaveSlot |
| `0x44A82D` | 隐藏命令 SaveSlot |

### 存档与 File 层

- save gate：`0x40A0C0`；
- Map Tick：`0x40ADE0`；
- SaveSlot：`0x43B320`；
- Writer：`0x43B360`；
- LoadSlot：`0x43B4D0`；
- save prepare：`0x44B150`；
- post-load：`0x44B1F0`；
- Game File ctor：`0x4416F0`；
- Game File dtor：`0x441710`；
- Game File Open：`0x4417C0`；
- Game File 对象大小：`0x14`。

原版文件名证据：

- `0x46BB04`：`%s%03d`；
- `0x46BB0C`：`Save`；
- `0x46A43C`：`.TSF`。

### SaveAction

- constructor：`0x425FE0`；
- vtable：`0x460B90`；
- Update slot：`0x460BA8`；
- 原 Update：`0x4262C0`；
- SaveAction 的 Save button：`+0x594`；
- Button disabled：`+0x04`；
- SaveSlot 的 SaveAction：`+0x5A4`；
- selected row：`+0x594`；
- page base：`+0x598`。

### Map / World 与安全门

- DataCenter*：`0x89F804`；
- actor array：`0x89F7F0`；
- controlled actor index：`0x468BF0`；
- World*：`0x978508`；
- runtime manager：`0x8E1C48`；
- map busy：`0x89F808`；
- dialogue id：`0x46F670`；
- Battle UI：`0x89FD74`；
- Title UI：`0x8E241C`；
- Interface UI：`0x8DED0C`；
- Save Point UI：`0x89FCD0`；
- Movie object pointer：`0x46F390`。

World 长度 `0x27D8`，fallback 只处理头部 `0xC8`。

## 五、Castle Mod Loader 生命周期

正确顺序：

```text
LoadLibraryExW(ASI)
  -> DllMain 只记录模块并 DisableThreadLibraryCalls
  -> Loader 完成 Locale/Overrides 处理
  -> GetProcAddress("InitializeASI")
  -> InitializeASI 读取配置、预检查并安装 Hook
```

`DllMain` 内不得恢复 test1/test2 的完整初始化。最终 PE 必须导出 `InitializeASI`，否则构建脚本和静态验证器都拒绝产物。

## 六、构建

直接运行：

```text
src\SaveEnhance\build.bat
```

当前脚本：

- 通过 Visual Studio 官方 `vswhere.exe` 自动定位带 x86 C++ 工具的安装，并初始化 `VsDevCmd.bat`；
- 未找到 Visual Studio 时允许复用已经初始化好的 x86 Developer Command Prompt；
- 从当前开发环境发现 `cl.exe/link.exe/dumpbin.exe` 和 Windows SDK 库，不写死工具版本或本机绝对路径；
- 参数为 C++17、UTF-8、`/O2 /Oi- /W4 /WX /GR- /GS- /Zl /NODEFAULTLIB`；
- 使用 `/Brepro` 生成可复现链接产物；
- 最终只输出仓库根 `build\Castle_SaveEnhance.asi` 与 `Castle_SaveEnhance.toml`；
- TOML 来自 `src\SaveEnhance\templete\Castle_SaveEnhance.toml`；
- 不向 `build` 复制 Markdown；
- 中间文件只放 `src\SaveEnhance\_build`，并与 `tools\__pycache__` 一起在成功或失败后删除；
- 不删除根 `build` 里其它插件产物；
- 构建后检查 PE32/i386、DLL、非零入口和 `InitializeASI` 导出；
- 批处理文件保持 UTF-8 BOM + CRLF；中文 `rem` 注释和 `echo` 文本行末保留两个半角空格。

本轮还修复了 MSVC 对裸函数前置声明的 C2488：`__declspec(naked)` 只留在函数定义，前置声明不带该属性；函数实现和生成逻辑不变。

工具链升级后不需要修改仓库脚本中的物理路径；只要 Visual Studio 安装包含 x86 C++ 工具和 Windows SDK，`vswhere.exe` 与 `VsDevCmd.bat` 就会选择当前有效工具链。GitHub Actions 也走同一发现流程。

## 七、静态验证

验证命令：

```text
python tools\verify_saveenhance_candidate.py ^
  --rpg "路径\RPG.exe" ^
  --miscinfo "路径\Public\MiscInfo.ENC" ^
  --asi "路径\Castle_SaveEnhance.asi"
```

2026-08-28 使用仓库参考原版与本机 MSVC 产物得到：

- RPG.exe SHA、PE32、ImageBase：PASS；
- 10 个菜单点、2 个循环分页点、5 个 CALL：PASS；
- SaveAction vtable、save gate、SaveSlot/LoadSlot：PASS；
- Game File ctor/dtor/open 前缀：PASS；
- MiscInfo 大小、解码 SHA、`+0x3C2=8`：PASS；
- ASI PE32/i386、DLL、非零入口：PASS；
- Export Table 含 `InitializeASI`：PASS；
- Import Table 仅 `KERNEL32.dll`：PASS；
- 无 CRT/UCRT/VCRUNTIME：PASS；
- user32/winmm 保持动态依赖：PASS；
- `WritePrivateProfileStringW` 已从导入表消失：PASS；
- ASI 含 `..\multimedia\save\.NEXTAUTOSLOT` UTF-16LE 路径标记：PASS；
- 必需导入含 `CreateDirectoryW/DeleteFileW/GetLastError`：PASS；
- 当前 ASI SHA-256：`db9ccdb1b3a2cfac6bd0d6f32b9a8f69d78a212bc3246f0ec43a935407ea5bed`。

详细机器结果见 `docs/SaveEnhance/静态验证输出.txt` 和 `静态验证结果.json`。

## 八、历史失败与不得恢复的方案

### test1：过宽预检查

三个不必要的 SaveSlot constructor Hook 增加冲突面，整组布尔日志又无法指出具体失败地址。test2 已删除这些 Hook，改为只读三个 owner 并逐地址诊断。不得恢复。

### test1/test2：Loader Lock 内完整初始化

INI、模块查询、VirtualQuery/VirtualProtect 和机器码安装放在 `DllMain`，用户日志停在配置后，全部功能未安装。test3 改用 `InitializeASI` 后实机确认修复。不得恢复 DllMain 业务初始化。

### test3：EXE 目录硬拼 Win32 路径

用 `<RPG.exe目录>\Save\Save091.TSF` 判断自动档，连续自动保存都误选 91。不得再用 EXE 目录硬拼来判断游戏 TSF 是否存在。

### test4：Win32 相对路径探测

改用 `Save\Save091.TSF` 相对路径后，SaveSlot 返回成功，Win32 仍看不到游戏文件层中的槽 91。test5 改为游戏 File 对象。不得恢复 GetFileAttributes/GetLastError/FILETIME 作为 TSF 权威。

### Windows LastWriteTime 选最旧

旧文档曾写“九槽全满后按 LastWriteTime 覆盖最旧”，当前源码没有这样做。游戏 TSF 对 Win32 不可见时无法可靠取得时间，因此当前改用 `multimedia\save\.NEXTAUTOSLOT` 持久化环形游标。

### INI `[Internal] NextAutoSlot`

短暂实现过，但用户明确否定。INI 容易在复制配置、重装或替换时丢失轮换状态。当前必须写 `multimedia\save\.NEXTAUTOSLOT`，不得恢复 INI 写回。

### 用游戏内部 File::Read/Write 保存状态

曾按 `0x441A30/0x441AB0` 调用游戏内部读写函数，机器静态检查通过，但用户实测游戏崩溃。
该候选已通过中文 Git 回退提交撤销，不得在没有完整崩溃定位前恢复。当前只保留已验证可用的
Game File `Open(mode=1)` 来判断 TSF 是否存在；三字节状态文件继续走日志同款 Win32 文本写入。

### `source_alt` 保留槽方案对比与 test7 落地

- 已吸收：owner 字段先解引用成真实 `SaveSlot*`，disabled 逻辑能找到真实窗口；
- 不可整文件照搬：备选源码会恢复 INI `NextAutoSlot`、重新引入 MSVC C2488 裸函数声明错误，
  并直接二次 Hook Controller 已占用的 `0x426365`；
- 手柄迟钝根因已确认：Controller 进入三项窗口时把私有 `action_focus` 初始化为 index2=存档；
  备选 wrapper 只把 index2 的 HitTest 隐藏，却没有把 Controller 焦点迁到 index1=读档，
  因而会出现“没有任何高亮、按 A 无反应、需要多按一次方向”的确定性死焦点；
- test7 没有修改 Controller。SaveEnhance 从 PadSupport Public API v1 读取语义输入，在自己的
  RPG.exe wrapper 中独立维护读档/取消两项焦点；普通槽始终链回安装前目标；
- 当前仍需实机确认鼠标接管、加载顺序和二次确认窗口边界，不把机器 PASS 写成实机 PASS。

### 批量重命名 91~99

一次自动存档会修改多个文件，中途失败可能破坏整条历史。当前每次只让原版 Writer 改一个 TSF，不采用。

### TSA/YCS 精确旁挂

历史实验存在路径语义和写入时机问题，当前正式实现不读取、不写入 TSA/YCS。它们不是本版本依赖。

## 九、兼容与安全边界

- 只支持锁定的 RPG.exe；
- 旧 `AnytimeSave.asi` 与 SaveEnhance 会竞争相同 Hook，必须移除；
- 未知插件先改目标机器码时 SaveEnhance fail-closed；
- Save001~Save159 是外部人工流程档，不能证明原版槽位容量；
- Controller 只通过 Castle_PadSupport API v1 读取状态，不读写私有全局；
- 状态文件异常只影响跨重启环形游标，不应让当前 TSF 保存结果变失败；
- 自动/Quick 的静态验证不证明剧情、战斗、楼梯等运行时门一定正确。

## 十、包内角色

```text
src\SaveEnhance\
  build.bat                         一键构建，输出到仓库根 build
  截至该版本的完整接档.md           源码包独立接档
  source\Castle_SaveEnhance.cpp    核心源码
  source\PatchUtil.h               补丁描述与只读字节比较
  source\PluginLog.h               无 CRT 日志工具
  source\Win32Mini.h               最小 Win32 声明
  templete\Castle_SaveEnhance.toml  构建时复制的运行配置模板
  tools\verify_saveenhance_candidate.py
  tools\工具详细说明.md
  backup\                           旧 AnytimeSave 备份，正式工作忽略

docs\SaveEnhance\
  完整接档.md
  安装与TOML配置说明.md
  静态逆向证据与地址表.md
  实机测试清单.md
  静态验证输出.txt
  静态验证结果.json
```

根 `build` 按当前项目构建标准只放 `Castle_SaveEnhance.asi` 与 `Castle_SaveEnhance.toml`；
Markdown 保留在源码包和 `docs\SaveEnhance`，构建脚本不复制。

## 十一、当前阻塞项与下一步

唯一真正阻塞稳定发布的是 test7 实机回归。下一步按《实机测试清单》执行，优先顺序：

1. 确认当前 test7 启动及第一次自动存档不崩溃；
2. 首次自动存档写 91 并回读成功；
3. 检查日志出现状态文件成功行，且 `multimedia\save\.NEXTAUTOSLOT` 内容为 `092`；
4. 完全退出并重启，确认游标继续；
5. 填满 91~99，确认按环形游标单槽覆盖；
6. 删除一个中间自动档，确认优先补最低空槽；
7. 回归 0/91~99 的存档按钮不可选、读档/取消两项手柄焦点和 1~90 正常写；
8. 回归 F5/F9、鼠标接管、循环分页、危险楼梯和长时间运行；
9. 收集完整 `Castle_SaveEnhance.log`，再决定是否从 test7 升为稳定候选。
