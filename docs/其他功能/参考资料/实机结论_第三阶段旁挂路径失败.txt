v0.3.0-dev3 实机结论：旁挂路径模型失败，正式路线停止 TSA
日期：2026-08-19

【用户实机日志原文】
《幽城幻剑录》随时存档插件 v0.3.0-dev3 启动。
[日志] 本次启动已清空旧日志；编码=UTF-8，换行=CRLF。
[模式] TSA 增强存档 + 原版安全回退。
[格式] 原版 TSF 结构保持不变；额外状态只写入独立 .TSA。
[启动] TSA 路径根目录已固定为 RPG.exe 所在目录，并使用绝对 Unicode 路径。
[启动] 所有增强存档 Hook 已安装。
[读档] 没有找到与当前 TSF 匹配的 TSA；仅按原版 TSF 行为恢复。
[回退] 已在默认 Scene Mode 连续稳定 8 Tick 后冻结入口级原版安全锚点。
[回退] 已在默认 Scene Mode 连续稳定 8 Tick 后冻结入口级原版安全锚点。
[存档门] 已扩展允许；当前 Scene Mode=1，默认 Scene Mode=0，原版安全回退=有，精确恢复=Scene Mode 已验证
[保存] TSF 已成功写入；TSA 已排队，并将立刻进行第一次绝对路径提交尝试。
[TSA] 重新打开 TSF / 计算 CRC 尚未成功；第 1 次尝试，Win32 错误码=2
[TSA] 重新打开 TSF / 计算 CRC 尚未成功；第 30 次尝试，Win32 错误码=2
[存档门] 已扩展允许；当前 Scene Mode=1，默认 Scene Mode=0，原版安全回退=有，精确恢复=Scene Mode 已验证
[TSA] 重新打开 TSF / 计算 CRC 尚未成功；第 44 次尝试，Win32 错误码=2
[读档] 没有找到与当前 TSF 匹配的 TSA；仅按原版 TSF 行为恢复。
[回退] 已在默认 Scene Mode 连续稳定 8 Tick 后冻结入口级原版安全锚点。
[退出警告] 仍有 TSA 未提交；对应原版安全回退 TSF 仍然可用。
[退出] 随时存档插件卸载。

【已确认】
1. 原版 TSF Writer 成功。
2. vanilla fallback 正常建立并用于扩展许可。
3. dev3 已经真正执行第一次旁挂 I/O，不再是 dev2 的“只排队”。
4. Win32 错误码 2 = ERROR_FILE_NOT_FOUND。
5. 这说明插件按“RPG.exe 目录 + 传入相对 TSF path”构造的绝对路径，并不是原版自定义文件系统真正落盘的位置/语义。

【v0.3.1 决策（由 v0.3.1a 继承）】
用户明确接受“如果旁挂路线走不通就精简，只保留安全可用”。因此 v0.3.1 当时决定正式删除 TSA/YCS；v0.3.1a 继承该架构、CRC、路径推导、Load Hook、恢复 Hook 和重试队列，只保留已经实机验证过的原版 TSF 安全回退机制。
