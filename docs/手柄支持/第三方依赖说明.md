> 当前权威版本：v0.3-refactor42（自动最近目标 + 确定/取消布局候选，2026-08-24）。实机稳定基线为 v0.3-refactor41：默认确定键按住/松开互动/取消键退出与 LT 兼容模式均已确认正常；R40目标轮盘及更早基线继续冻结。R42 新增两个默认开关：`Investigation.AutoFocusNearest=1` 在每次建立调查会话时复用R40距离第0项、肩键锁和resolver/25点probe自动尝试最近目标；`Controls.SwapConfirmCancel=0` 为南键确定/东键取消，设为1切换成东键（PS O）确定/南键（PS X）取消。确定/取消交换覆盖所有语义页面、调查激活和Back/RT鼠标左右键（确定=左键、取消=右键），但RB+ABXY战斗快捷键、X/Y专属功能等固定物理位置操作不变。用户补回的INI中文注释全部保留，新选项同样使用中文。PadInput、Cursor、RPG地址和业务UI冻结。下文更早“当前/候选”表述与本段冲突时按历史记录处理。

# refactor42 依赖补充

**新增第三方运行时依赖：0。** AutoFocus只复用项目自有R40整数排序；Swap只复用现有SDL南/东键采样、Runtime配置和InputRouter。

新增公开键 `AutoFocusNearest` 与 `SwapConfirmCancel` 均通过Win32 Profile API读取，不需要配置库。InputRouter新增Raw接口和fixed表仍是项目自有纯C。

鼠标左右键交换不新增Windows API；继续调用既有 `Cursor_PulseLeftClick/RightClick`。RB快捷固定不新增Battle Hook。Python检查器仍只用标准库。

---

# refactor41 依赖补充（历史，已实机验收通过）

**新增第三方运行时依赖：0。** 仍只动态使用兼容x86 `SDL3.dll`；A/B/LT/RT都来自现有 `PadInput_Pressed/Down/Released`，没有新增SDL导出、头文件或import library。

新增的是项目自有配置实体 `Castle_PadSupport.ini` 和公开整数键 `Investigation.ActivationMode`。它由Win32 Profile API读取，不引入INI解析库；默认0，范围0..1。

状态机只增加整数/布尔字段和既有函数调用，没有线程库、容器库、计时库、CRT或新的RPG.exe协议。A松开继续复用现有48ms点击桥，B取消继续复用释放屏障，RT继续复用鼠标模式。

构建内容增加中文文档和Python检查器镜像；检查器仍只依赖Python 3标准库。`--artifact-only` 是工具功能，不是游戏运行依赖。

---

# refactor40 依赖补充（历史，已实机验收通过）

**新增第三方运行时依赖：0。** 仍只动态使用项目既有兼容 x86 `SDL3.dll`；LB/RB 复用已经采样的 SDL Gamepad 按钮，不新增 SDL API。`SDL_RumbleGamepad` 仍是可选能力。

R40 在继承 R39 纯整数方向评分的基础上，用饱和 `u32` 世界距离、屏幕半平面/叉积、resolver index 和最多96项插入排序实现肩键循环；约12°交还门使用 `dot/cross` 与 `sin²≈43/1000`。没有引入 `float`、`atan2`、`sqrt`、`qsort`、数学库、CRT helper、图形库、新 DLL 或新 INI 键，因此 `/nodefaultlib` 边界保持不变。

LB/RB 确认目标后的短震继续复用既有 `Investigation_UpdateRumble()` 与可选 `SDL_RumbleGamepad`；没有新增震动入口或强制运行依赖。缺少震动导出时输入和目标循环仍可工作。

---

# 第三方依赖说明

> **v0.3-refactor36 当前候选（2026-08-22）：** refactor33 的统一商店主体已经用户实机确认；refactor35 实机反馈为各商店主体可用，但上下到列表边界不会连续翻页、移动项目会自动打开道具信息且 Y 无法关闭。本轮只修 Shop：上下越界分别调用原版上一页/下一页并落到新页末项/首项；左右显式翻页尽量保留当前行；两侧列手形相对 refactor35 统一向左 10 像素、向下 5 像素。
>
> **Y 道具信息裁决：** `0x417829` 与公共 `CALL_CMD0_EVENT` 是同一条原版 close Event CALL，必须复用全局 Hook，不得再次安装；`info+0x580` 只表示打开/关闭动画，稳态必须比较 `info+0x56C` 与原版关闭/打开目标 `+0x588/+0x58C`。方向/X/翻页只更新原版项目链并抑制最后开窗步骤；仅 Y 放行打开，已打开时 Y 点击 `info+0x598` 的真实关闭按钮。信息窗及动画期间其它商店输入保持模态阻断。
>
> **边界与验证：** 不按装备店、道具店、药店名称分支，继续复用 opcode `0x36 -> 0x413FA0 -> 0x89FCD4` 的同一 Shop Adapter；价格、库存、页、行、数量与成交结果均不直接写。天书偶发跳焦点继续低优先级，Tome/SaveSlot/Cursor 六文件逐字节保持 refactor33。27/27 C 单元严格编译通过，两次无时间戳 PE32/i386 链接字节一致；无 EXE **73 PASS / 0 FAIL**，未修改 `RPG.exe.org`（SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`）**93 PASS / 0 FAIL**。ASI **153,600 bytes**，SHA-256 `05e7ef5e96f32d6a55a819a5cb590ac2302adb9441564495a3df62882af2ef0c`。机器 PASS 不冒充实机 PASS。
>
> **交付裁决：** 从本版起不再内置散装 `证据/` 树；源码、文档与检查器构成接档材料。若以后确有新增证据，只合并维护单一 `文档/验证汇总.md`。
>
> **历史阅读规则：** refactor32 的存档点完整路由与客栈原位返回、refactor33 的地图十字键仅步行和统一商店主体均已验收并冻结；refactor34 的全局 Shop 形状硬门、refactor35 对共享 close Event 的重复 Hook 与对动画 mode 的稳态误判均已作废。当前修正以本文件顶部 refactor36 段落为准。

## refactor36 依赖变化

- 无新增运行时依赖；仍只需要现有兼容的 x86 SDL3.dll 与既有 ASI Loader 环境。
- 无新增源码第三方库；分页、说明窗与视觉修正全部复用项目既有 Win32/SDL3 边界和原版 ButtonEvent。
- 逆向资料和 `RPG.exe.org` 仅作离线 Oracle，不随交付包发布。
- 不再携带散装证据 TXT；这不改变运行依赖或可构建性。

## refactor35 依赖变化（历史）

**新增第三方运行时依赖：0。** 本轮只恢复 Shop 主体入口，并把既有信息窗 Hook 拆成可选能力；仍只复用项目已有 Runtime、UiBridge、Cursor、InputRouter 与 RPG.exe 原版 UI。没有新增 DLL、INI 键、数据文件、图形库、库存库或 CRT。

- 装备店、道具店、药店与同结构店铺共享同一个原版 Controller，不需要任何店型数据库或外部配置；
- 玩家运行依赖仍只有既有兼容 x86 `SDL3.dll` 和 ASI Loader；
- Godot 固化包、补充 JSON、原版 EXE 与商店截图只作研究证据，不进入部署包。

## refactor33 依赖变化（历史，已验收）

**新增第三方运行时依赖：0。** 地图十字键只复用现有 SDL3 语义与 Exploration Hook；商店只复用项目既有 InputRouter、UiBridge、Cursor、Runtime 和 RPG.exe 原版 UI。没有增加图形、库存、存档、配置或 CRT 库，也没有新增 INI 键。

- 玩家仍只需兼容的 x86 `SDL3.dll` 与既有 ASI Loader；
- 四张商店参考图和 Godot/JSON/原版 EXE 均只作研究资料，不进入部署；
- `shop.c/.h` 是项目自有纯 C，不是第三方组件。

## refactor32 依赖变化（历史，已验收冻结）

**运行时新增第三方依赖：0。** `save_point.c` 仍只读取原版包装对象；共享三项动作和深层 ConfirmDialog 都复用项目既有控制器。客栈来源记忆完全属于 Inn 私有状态，Synthesis 不再依赖 Inn。没有新增存档库、文件格式、图形库、输入库、配置键或 DLL。

- 游戏运行仍需要兼容的 x86 `SDL3.dll` 与既有 ASI Loader 环境。
- 当前容器用系统 GCC/GNU Binutils 生成交付 PE32，仅属于构建工具，不是玩家运行时依赖；包内 `build.bat` 仍保留 MSVC x86 正式构建路线。
- Godot 兼容引擎固化包、原始逆向 JSON、`RPG.exe.org`、`multimedia.zip` 都是只读取证资料，不进入编译、不随模组部署。

## refactor31 依赖变化（历史）

refactor31 同样没有新增第三方依赖；当时的 Inn/Synthesis 语义交接已删除，但不影响依赖结论。

## refactor30 依赖变化（历史）

refactor30 同样没有新增运行时依赖；当时对存档点 owner 的 state7 判断已作废，但不影响依赖结论。

## refactor29 依赖变化

**新增第三方依赖：0。** 客栈与炼化只复用 RPG.exe 原版 ButtonEvent/HitTest；剧情 mode=3 选择只复用现有 Win32 `SetCursorPos/mouse_event` 与 Cursor 模块；隐藏鼠标只喂原版命中坐标，选择框由 RPG.exe 自己绘制。SDL3、Ultimate ASI Loader、Win32 API 需求均与 refactor28 相同。


## refactor28 依赖变化

**新增第三方依赖：0。**

治疗目标只复用现有 `InputRouter`、`UiBridge`、`Cursor` 与 RPG.exe 原版 ButtonEvent；state8 只改两个焦点常量。SDL3 使用方式、Win32 API、无 CRT 链接策略都没有变化，也没有引入新的图形、存档、输入或配置库。

---

## 历史累计记录（只作因果证据）

> 以下内容保留各历史版本当时的设计、失败方案、地址和测试记录。里面出现的“当前/待验/下一步”不再代表 refactor28 现行状态。

## 0. refactor27 依赖变化

**新增第三方依赖：0。** state7/state8 都只使用既有 Win32、SDL3 抽象层与 RPG.exe 原版 UI 协议。没有引入存档库、配置库、图形库或 CRT。

SDL3 拔插重连与控制器热切换均已由用户实机确认 PASS，`pad_input.c/.h` 在本轮继续作为冻结底座，不为新页面重写。

## SDL3

运行时需要与 32 位插件接口兼容的 x86/Win32 `SDL3.dll`。本包不联网下载，也不夹带未经确认的 SDL3 二进制；缺失时写中文日志并保留原版键鼠路径。

refactor26d 没有增加 SDL API；热插拔实现与 refactor26 字节级保持。继续原样沿用 r22 热插拔时序：插件不跑 SDL 自己事件循环，因此每 worker tick 主动 `SDL_UpdateGamepads`，再处理旧句柄断开、低频重枚举、`SDL_OpenGamepad` 与采样；新连接首帧抑制虚假 edge。**用户已经实机确认这条热插拔修复 PASS，refactor26 继续字节级冻结该实现。**

## 新增代码依赖

`spatial_neighbor.c/.h` 是项目自有纯 C 算法，没有第三方库。r24 新增的完整 Button 几何也只是复刻 RPG.exe 原版字段/指针链；state5 视觉、Battle Target、state6 阵形都只复用既有 Win32 / RPG.exe 协议。

## Windows / CRT

继续使用既有 Win32 API；正式 ASI `/GS- /Zl /nodefaultlib`，不引入额外 MSVC CRT。

**历史 refactor26d 新增第三方依赖：0；refactor27 同样新增第三方依赖 0。**
