# v0.3-refactor39（LT角色中心连续轮盘跟手修复）文件校验清单

> 当前权威版本：v0.3-refactor39（LT 角色中心连续轮盘跟手修复候选，2026-08-23）。实机基线为 v0.3-refactor38：普通隐藏鼠标误震已由用户确认修复并冻结；R38 的 LT 连续旋转比旧 latch 明显改善，但用户确认仍“不够跟手”，且摇杆转到某些方向时不能稳定选中该方向上的可互动目标。R39 只继续修 LT 方向几何与切换迟滞：轮盘中心改为当前角色屏幕位置，取消 R38 的 ±63.4° 硬候选锥，使用无水平/垂直/对角偏差的纯整数角度评分，并把迟滞收紧到约 2～3°。Back/RT/LT 优先级、原版 resolver/25 点验证、鼠标模式和 R38 已通过的震动边界全部冻结。下文任何更早“当前/候选”表述与本段冲突时均按历史记录处理。

- Castle_PadSupport.asi：181,248 bytes；SHA-256：a49a89c20fea24f2fe53ed048341e23734718e67b1a6ef7fde7b85803b1dc160
- Castle_PadSupport.ini：SHA-256：cb1999f3951a4584a350e5eaeedf0819d5b3ec3673a7bd8db164987a4af82121

要求：29 个 C 单元；PE32/i386 DLL；无 CRT 默认库、OBJ/LIB/EXP/PYC 和散装证据目录；编译内容只含 ASI 与 INI；RPG.exe.org 仅离线复核且不打包。`源码/readme.md` 是 GitHub 入口英文文件名例外。静态 PASS 不等于实机 PASS。

本版新增机械护栏：
- 必须使用 actor 世界坐标减 camera 生产 `wheel_origin_x/y`；
- 禁止 R38 的鼠标固定 anchor 和 ±63.4°硬候选锥回归；
- 必须保留 `left_selected_object`、新方向先于旧 probe、右杆 manual override；
- R38 已实机通过的隐藏鼠标静默震动门继续锁定。

最终机器验证：
- 29/29 C 单元严格编译 PASS；两次全新对象目录的无时间戳链接结果：逐字节一致。
- 综合检查（不带 EXE）：75 PASS / 0 FAIL。
- 综合检查（含可信原版 `RPG.exe.org`）：96 PASS / 0 FAIL。
- `RPG.exe.org`：462,848 bytes；SHA-256：`8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`；仅用于离线协议验证，不进入发布包。

实机状态：R38 普通隐藏鼠标误震 **用户 PASS/冻结**；R39 角色中心轮盘 **待用户实机验收**。
