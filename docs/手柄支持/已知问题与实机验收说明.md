> 当前权威版本：v0.3-refactor37（R36 底座重做候选）。此前废弃的旧 R37 实验实现不作为继承基线；本版只从中移植原版互动 resolver 调查能力。下文若保留 R36 标题或历史说明，与本节冲突时一律以本节为准。

# 新 R37 状态

已完成静态检查、29单元严格编译与PE32结构检查，仍待实机验收。天书读取存档极偶发跳焦点/显示鼠标和列表末页按右偶发确认，按用户要求继续延期。

---

# 已知问题与实机验收说明

> **v0.3-refactor36 当前候选（2026-08-22）：** refactor33 的统一商店主体已经用户实机确认；refactor35 实机反馈为各商店主体可用，但上下到列表边界不会连续翻页、移动项目会自动打开道具信息且 Y 无法关闭。本轮只修 Shop：上下越界分别调用原版上一页/下一页并落到新页末项/首项；左右显式翻页尽量保留当前行；两侧列手形相对 refactor35 统一向左 10 像素、向下 5 像素。
>
> **Y 道具信息裁决：** `0x417829` 与公共 `CALL_CMD0_EVENT` 是同一条原版 close Event CALL，必须复用全局 Hook，不得再次安装；`info+0x580` 只表示打开/关闭动画，稳态必须比较 `info+0x56C` 与原版关闭/打开目标 `+0x588/+0x58C`。方向/X/翻页只更新原版项目链并抑制最后开窗步骤；仅 Y 放行打开，已打开时 Y 点击 `info+0x598` 的真实关闭按钮。信息窗及动画期间其它商店输入保持模态阻断。
>
> **边界与验证：** 不按装备店、道具店、药店名称分支，继续复用 opcode `0x36 -> 0x413FA0 -> 0x89FCD4` 的同一 Shop Adapter；价格、库存、页、行、数量与成交结果均不直接写。天书偶发跳焦点继续低优先级，Tome/SaveSlot/Cursor 六文件逐字节保持 refactor33。27/27 C 单元严格编译通过，两次无时间戳 PE32/i386 链接字节一致；无 EXE **73 PASS / 0 FAIL**，未修改 `RPG.exe.org`（SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`）**93 PASS / 0 FAIL**。ASI **153,600 bytes**，SHA-256 `05e7ef5e96f32d6a55a819a5cb590ac2302adb9441564495a3df62882af2ef0c`。机器 PASS 不冒充实机 PASS。
>
> **交付裁决：** 从本版起不再内置散装 `证据/` 树；源码、文档与检查器构成接档材料。若以后确有新增证据，只合并维护单一 `文档/验证汇总.md`。
>
> **历史阅读规则：** refactor32 的存档点完整路由与客栈原位返回、refactor33 的地图十字键仅步行和统一商店主体均已验收并冻结；refactor34 的全局 Shop 形状硬门、refactor35 对共享 close Event 的重复 Hook 与对动画 mode 的稳态误判均已作废。当前修正以本文件顶部 refactor36 段落为准。

## refactor36 当前待验与低优先级记录

### 必须实机确认

- 所有同结构商店保持可用，尤其装备店、真正的道具店、药店。
- 上下越界连续跨页的页向与首/末落点正确，左右显式翻页仍保留行。
- 移动项目绝不自动开说明；Y 在左右列都能开、关当前道具说明；说明窗存在时输入不穿透。
- 两侧手形左10/下5的最终观感；数量窗仍无手形。

### 已知低优先级

- 天书共享存读档动作页偶发松键后跳到“存档”并出现手形；切换键鼠后可能消失。用户已裁决为全手柄完成后再修，本轮只保证 Shop 不清理非自己显示的手形，未修改 Tome/SaveSlot/Cursor。

### 验证边界

静态检查、Oracle 短签名、严格编译和确定性链接均已通过，但无法证明游戏帧序、动画和所有城市数据下的行为；refactor36 在用户实机确认前仍是候选。

## refactor35 待验记录（历史；反馈已转入 refactor36）

refactor33 的地图十字键仅步行、所有商店买卖主体和其它内容均已由用户验收通过；refactor34 的“所有商店无响应”是明确阻断回归。本轮需先验证主体恢复，再复核继承的三项反馈：

1. **全商店主体**：装备店、道具店、药店至少各进一家，X/A/B、上下、翻页、类别及买卖数量窗都必须恢复；任何相同菜单结构的商店也使用同一套操作；
2. **列标记位置**：左右列手形应落在用户参考图的标题框内，X 不变，只沿 Y 下移；
3. **Y 道具信息**：上下选物、X 换列、A 买卖、分页/类别都不能自动打开信息；只有 Y 打开/关闭。信息窗存在时其它手柄键不得在下面打开数量窗；
4. **数量窗视觉**：左右切换仍有原版确定/取消方框，界面上不再出现手形；LT/RT、A、B 业务保持 refactor33 已通过行为。

天书“打开存读档准备窗口、松开后偶发跳到存档按钮并出现手形”仍按用户裁决列为低优先级。refactor34 为它加入的 Shop 类形状硬门已经被商店实机结果否决并撤销；仅保留不影响主体的 Shop 私有手形所有权。若仍能偶发复现，本轮只记录，不修改天书/SaveSlot/Cursor，待全手柄完成后结合日志和稳定复现步骤处理。

### 已验收并冻结

- refactor33：地图十字键仅步行、统一商店主体；
- refactor32：存档点完整深层输入、客栈从哪里进入就返回哪里；
- 炼化以及更早全部已通过内容。

## refactor32 历史待验记录（其后已通过）

代码、严格编译、PE32 链接、自动检查和原版 Oracle 地址协议均已通过；当前只剩用户实机确认以下两条完整状态链：

1. **地图存档点全流程。** 前置“是否存档”→槽位/分页→三项动作→二次 Yes/No→逐层返回都必须接受手柄；直接确认与覆盖确认如出现也要验证。动画中快速按键不得穿透地图，退出重进不得复用旧指针。
2. **客栈按来源项返回。** 从诸态进入主 Interface 并退出后仍停诸态；从炼化进入并退出后仍停炼化。两条路线都要求逻辑/视觉一致，下一颗 A 重进原来源项。关闭途中若实体鼠标/右摇杆接管，则尊重实际 hover。

### 已验收并继续冻结

- 炼化两层的 ↑/↓、←/→、LB/RB、A、根层 B、第二层 B、可/不可炼化路径均已由用户验收通过；本轮未改炼化业务，只删除其对 Inn 的错误固定首项耦合。
- refactor30 的 L/R 方向修正继续 PASS；`L=上一/左/减`、`R=下一/右/加` 不变。
- 其余此前完成内容按用户裁决全部保持 PASS；回归只需快速扫，不得重新降级成“未验收”。

## refactor30 / refactor29 历史待验记录

以下旧章节中的未勾选项记录的是当时测试过程。用户已经把此前完成内容裁决为通过，现行阻塞只以上述 refactor32 两条状态链为准。


## refactor28 历史封存（当时当前）唯一新增实机范围

### 1. state3 治疗法术角色目标 —— 待实机

必须实机确认：

- 治疗法术选择“使用/确定”后，手形位于当前角色顶部图标**正中心**；
- ←/→ 能在所有当前队伍角色之间正确移动，顺序与顶部显示一致；
- A 对当前手形所在角色实际施放，不出现视觉角色与实际目标错位；
- B 立即等价原版右键取消，回到普通技能菜单并恢复常规手柄操作；
- 实体鼠标接管后仍可按原版方式点击角色/右键取消；右摇杆鼠标也不被破坏；
- 目标 modal 期间 LB/RB、上下列表、分页、换角色等底层输入不能穿透。

### 2. state8 手形位置 —— 仅视觉快速确认

state8 业务已经用户 PASS。本版仅检查三行手形是否比 refactor27 **约左 10px、下 5px**，同时音乐/音效仍不能出现持续/额外 hover 动画。

### 已冻结不应回归

state7 天书、state8 业务、state2～6 既有功能、Battle Target/Battle 禁用项 A、标题与 SaveSlot、地图 Y、剧情 A 两阶段、SDL3 拔插/热切换均已通过。若这些在 refactor28 出现异常，应按 **BIG 回归** 处理，而不是当成治疗目标功能本身的正常试错。

---

## 历史累计记录（只作因果证据）

> 以下内容保留各历史版本当时的设计、失败方案、地址和测试记录。里面出现的“当前/待验/下一步”不再代表 refactor28 现行状态。

## refactor26e 当前最高优先实机项

### A. 普通剧情对话 A —— refactor26e 待验

本轮唯一明确需要新增实机验收的是剧情 A。静态审查已经确认 refactor25 大重构遗漏 `DialogueInput_InstallHook()`，而 `dialogue_input.c/.h` 与 worker 的 `DialogueInput_Update()` 从 refactor24 至今都没有丢。refactor26e 已恢复缺失 installer，并完成 r24 能力完整与真实依赖审查。

- [ ] 文字逐字显示时第一次按 A：只立即补全当前句；
- [ ] 当前句已经完整显示时再次按 A：推进到下一句；
- [ ] 长对话连续按 A 不得出现“完全没反应”或一次 A 连跳两句；
- [ ] 有询问框时 A 必须由询问框优先消费，不能同时推进背后的剧情文字；
- [ ] 实体鼠标/键盘原版对话行为保持；
- [ ] 进入/离开主 Interface、Battle、标题/SaveSlot 后再次触发剧情，对话 A 仍正常。

### B. 已经通过并冻结，本轮只做快速回归

- [x] state2 法宝/道具弃置/分发确定取消：refactor26c 用户 PASS；
- [x] state5 五内整页：refactor26d 用户 PASS，包括手形焦点、偏移、LT/RT/Y 按钮只瞬时响应一次；
- [x] Battle Target：旧成熟屏幕方向方案 PASS；
- [x] Battle 不可交互项 A：无副作用 PASS；
- [x] SDL3 拔插重连：PASS；控制器热切换后续也已由用户实机确认 PASS，二者一起冻结。

### C. refactor24 完整恢复审查 —— 静态已通过

- [x] 14/14 `plugin_install_all_hooks()` 业务 installer 均存在且无重复；UiBridge 先于依赖其分发能力的业务 Hook；
- [x] 14/14 worker 关键 `Update/BeginFrame` 阶段均存在，且页面叠加层→ConfirmDialog→Dialogue→Battle→InterfaceShell→Frontend 等真实消费偏序全部满足；
- [x] `dialogue_input.c/.h` 与 refactor24 字节级一致；
- [x] `pad_input.c/.h`、`cursor.c/.h`、`exploration.c/.h`、`frontend.c/.h`、`save_slot.c/.h`、`confirm_dialog.c/.h` 等底层稳定模块与 refactor24 字节级一致；
- [x] refactor24 后合法变化的 InputRouter、state2～6 页面、Runtime/UiBridge 逐项保留后续功能，不为追求 SHA 相同而回滚。

## refactor26b 历史实机项：state5 五内视觉（鼠标焦点已通过；动画未通过）

用户已明确：五内功能本身正常，但 refactor26a 仍有严重视觉 BUG。refactor26b 待验：

- [ ] LT 减一次：只允许当前“减”按钮瞬时响应一次，随后恢复静止；
- [ ] RT 加一次：只允许当前“加”按钮瞬时响应一次，随后恢复静止；
- [ ] 连续快速加减：不能有任何按钮进入持续闪烁/持续动画；
- [ ] X 绕五个节点多圈：历史节点不能重新出现残留动画；
- [ ] 手形焦点位于当前数值右上方约 60°，不再盖住数字；
- [ ] 右摇杆/实体鼠标接管后，真实鼠标点击五内必须仍是原版行为。

实现判据不是“看起来差不多”，而是必须与原版实体鼠标点击的按下/释放历史一致。

## refactor26a 历史实机项：state2 弃置/分发确认框（实机未通过）

refactor26 已确认存在输入路由遗漏，不能视为 state2 完整通过。refactor26a 待验：

1. 进入物品/法宝页，移动到原版允许“弃置”或“分发”的项目；
2. 按 X 打开页面自己的 Yes/No；
3. D-Pad ←/→ 必须只切 Yes/No；
4. A 必须执行当前选择，B 必须取消；
5. 弹窗显示期间 D-Pad ←/→ **不得翻物品页**；
6. 关闭弹窗后 D-Pad ←/→ 再恢复上一页/下一页；
7. 法宝 LT/RT 子类型、↑/↓列表、A、X 其它既有行为不得回归。

全源码机械审计未发现第二个同类型 Policy/Layer 漏接，但这不替代实机。

## 0. refactor27 当前唯一新增待验范围

此前用户已确认通过的模块继续冻结。当前**新增且尚未实机裁决**的只有：

1. **state7 天书**：共享 SaveSlot 槽位层、原版“存档/读档/取消”三项窗口、二次 Yes/No 的 modal 交接；
2. **state8 机能**：三行手形焦点、音乐/音效 LT/RT、空明流转 A、二次 Yes/No，以及实体鼠标/右摇杆接管。

普通剧情 A 两阶段推进、SDL3 拔插重连与热切换已由用户在 refactor26e 之后确认 PASS，不再列为当前问题。两个已验证 RPG.exe SHA 均属于支持白名单，也不再把第二 SHA 单独列 FAIL。

如果 state7/state8 实机失败，优先只修对应页面 Adapter/Context；除非证据证明公共 SaveSlot、ConfirmDialog、Cursor 或 InputRouter 有共性缺陷，否则不得为了新页修改已经 PASS 的旧页面。

## 1. 已实机 PASS / 冻结

- **state2 法宝/道具弃置/分发确认框确定/取消**：PASS。refactor26c 恢复 ConfirmDialog 第二类低层安装链后用户实机确认正常，冻结。
- **state5 五内整页**：PASS，冻结。X/LT/RT/Y、手形鼠标焦点与右上偏移、按钮瞬时动画均已用户实机确认。
- **Battle 不可交互项 A**：PASS。不可用内容按 A 无副作用，不再锁死菜单。
- **SDL3 热插拔**：PASS。`pad_input.c/.h` 继续整文件 SHA 防回归。
- **Battle Target**：PASS。用户接受恢复到新二维排序实验之前的旧成熟屏幕方向方案，后续不再尝试重排；`battle.c` 继续整文件 SHA 防回归。
- **state3 绝学/法术既有业务**：PASS。LT/RT 子类、↑/↓列表、A 与确认框均以此为业务基线；refactor26 新增 D-Pad 左右分页，因此新增输入仍需增量验收。
- **state4 及身既有业务**：PASS。X 栏位、↑/↓候选、A装备及 refactor25 的 LT/RT 翻页当前作为正常基线；refactor26 额外新增 D-Pad 左右分页，仍需增量验收。

## 2. refactor26 当前待验：统一主 Interface 操控

### 2.1 角色切换

- 角色切换不再使用 D-Pad。
- state1～5 中，左摇杆水平推量达到/超过约 50% 才产生一次上一/下一角色动作。
- 一次推杆只触发一次；必须回到中间死区后才能重新触发。
- state6 阵形禁止左摇杆换人。

### 2.2 D-Pad 左右分页

- state2 法宝：←上一页、→下一页；LT/RT 仍切子类型。
- state3 绝学：←上一页、→下一页；LT/RT 仍切子类型。
- state4 及身：←上一页、→下一页；LT上一页/RT下一页继续保留，两套入口必须调用同一原版分页事务。
- 各页第一页/末页的越界分页必须无副作用。

## 3. state5 五内：用户 PASS / 冻结

refactor26d 已由用户实机确认动画正常，因此 state5 不再是当前阻塞项：

- X：烈→神→魔→魂→迅循环；
- LT：当前五内减少；RT：当前五内增加；Y：蕴魂确认；
- 原版手形鼠标作为唯一焦点视觉，并使用已通过的右上偏移；
- LT/RT/Y 对应按钮只瞬时响应一次，随后恢复普通态；
- D-Pad 不加减、不换人；角色切换只走左摇杆 50% 单次方向沿。

后续除非用户明确要求，不再修改五内鼠标焦点、偏移或按钮动画生命周期。

## 4. state6 阵形

仍以 refactor24 实现为待验候选：D-Pad 四方向只移动阵位，绝不泄漏为角色切换；左摇杆角色切换也必须在 state6 禁止；A/B 继续走原版两阶段交换/取消。

## 5. 下一步

本轮先验收 **普通剧情对话 A 两阶段恢复**；其它已通过功能只做快速 BIG 回归确认。state6 阵形与主 Interface 后续页面继续按既定路线推进，不与本次 Dialogue 热修混改。

