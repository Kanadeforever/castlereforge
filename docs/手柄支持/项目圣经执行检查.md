> 当前权威版本：v0.3-refactor42（自动最近目标 + 确定/取消布局候选，2026-08-24）。实机稳定基线为 v0.3-refactor41：默认确定键按住/松开互动/取消键退出与 LT 兼容模式均已确认正常；R40目标轮盘及更早基线继续冻结。R42 新增两个默认开关：`Investigation.AutoFocusNearest=1` 在每次建立调查会话时复用R40距离第0项、肩键锁和resolver/25点probe自动尝试最近目标；`Controls.SwapConfirmCancel=0` 为南键确定/东键取消，设为1切换成东键（PS O）确定/南键（PS X）取消。确定/取消交换覆盖所有语义页面、调查激活和Back/RT鼠标左右键（确定=左键、取消=右键），但RB+ABXY战斗快捷键、X/Y专属功能等固定物理位置操作不变。用户补回的INI中文注释全部保留，新选项同样使用中文。PadInput、Cursor、RPG地址和业务UI冻结。下文更早“当前/候选”表述与本段冲突时按历史记录处理。

# refactor42 执行边界

动工前已检查并汇报：代码/ASI保持R41，仅源码与编译内容INI被用户同步修改为带完整中文注释的版本。R42必须保留这份用户内容，不能以旧英文模板覆盖。

执行要求：

1. AutoFocusNearest默认1，只在调查会话第一帧复用R40最近项；0关闭；
2. SwapConfirmCancel默认0，只交换Confirm/Cancel语义；1为PS O确定/X取消；
3. 确定/取消交换覆盖调查和鼠标左/右键；RB+ABXY/X/Y等固定物理功能不变；
4. PadInput/Cursor/battle.c/RPG地址冻结；输入映射只在Runtime/InputRouter/ControlModes/Investigation授权范围修改；
5. 用户INI全部中文注释保留；新Controls/AutoFocus说明也是中文；检查器锁关键句；
6. 新代码逐步解释一次性会话门、复用肩键算法、fixed/semantic双表、Raw读取和快捷键隔离；
7. 遍历14份文档，R41同步PASS，R42同步候选；构建后两套文档/工具镜像SHA一致；
8. 严格编译、双构建、源码包/编译内容包、Oracle全部通过后仍只标机器候选。

---

# refactor41 执行边界（历史，已实机验收通过）

本轮在动工前已按约定只读检查并汇报：R40源码/ASI哈希吻合，没有其它插件写入，没有临时文件。随后才收到明确“开始”并进入修改。

执行要求：

1. 默认0=A按住/松开互动/B取消；1=R40 LT模式。配置读取只在Runtime，按钮裁决只在ControlModes，目标确认只在Investigation；
2. B取消必须锁A到物理松开；A松开确认必须五门通过；RT覆盖/恢复必须同一语义状态机；
3. R40轮盘、PadInput、Cursor、RPG地址和全部业务Adapter冻结；
4. 修改文件不只写函数头摘要：每个关键赋值、分支、顺序和危险原因都以面向初学者的中文行内/块注释解释；检查器设置更高比例与关键解释词护栏；
5. 新增完整默认INI并显式 `ActivationMode=0`；构建失败时不得留下旧ASI冒充新产物；
6. 遍历全部14份现行文档，R40同步为用户PASS，R41同步为候选；GitHub构建说明只保留 `源码/readme.md` 英文例外，文档目录不放重复构建说明；
7. 源码包和编译内容包都携带完整中文文档；编译内容另带最新检查器和中文工具说明，镜像必须SHA一致；
8. 机器PASS、Oracle PASS和确定性构建不能冒充A/B/RT手感实机PASS。

完成后必须复核未授权冻结文件SHA，并在完整接档中记录成功/失败路线、踩坑、阻塞项和下一步。

---

# 项目圣经执行检查

> **v0.3-refactor36 当前候选（2026-08-22）：** refactor33 的统一商店主体已经用户实机确认；refactor35 实机反馈为各商店主体可用，但上下到列表边界不会连续翻页、移动项目会自动打开道具信息且 Y 无法关闭。本轮只修 Shop：上下越界分别调用原版上一页/下一页并落到新页末项/首项；左右显式翻页尽量保留当前行；两侧列手形相对 refactor35 统一向左 10 像素、向下 5 像素。
>
> **Y 道具信息裁决：** `0x417829` 与公共 `CALL_CMD0_EVENT` 是同一条原版 close Event CALL，必须复用全局 Hook，不得再次安装；`info+0x580` 只表示打开/关闭动画，稳态必须比较 `info+0x56C` 与原版关闭/打开目标 `+0x588/+0x58C`。方向/X/翻页只更新原版项目链并抑制最后开窗步骤；仅 Y 放行打开，已打开时 Y 点击 `info+0x598` 的真实关闭按钮。信息窗及动画期间其它商店输入保持模态阻断。
>
> **边界与验证：** 不按装备店、道具店、药店名称分支，继续复用 opcode `0x36 -> 0x413FA0 -> 0x89FCD4` 的同一 Shop Adapter；价格、库存、页、行、数量与成交结果均不直接写。天书偶发跳焦点继续低优先级，Tome/SaveSlot/Cursor 六文件逐字节保持 refactor33。27/27 C 单元严格编译通过，两次无时间戳 PE32/i386 链接字节一致；无 EXE **73 PASS / 0 FAIL**，未修改 `RPG.exe.org`（SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`）**93 PASS / 0 FAIL**。ASI **153,600 bytes**，SHA-256 `05e7ef5e96f32d6a55a819a5cb590ac2302adb9441564495a3df62882af2ef0c`。机器 PASS 不冒充实机 PASS。
>
> **交付裁决：** 从本版起不再内置散装 `证据/` 树；源码、文档与检查器构成接档材料。若以后确有新增证据，只合并维护单一 `文档/验证汇总.md`。
>
> **历史阅读规则：** refactor32 的存档点完整路由与客栈原位返回、refactor33 的地图十字键仅步行和统一商店主体均已验收并冻结；refactor34 的全局 Shop 形状硬门、refactor35 对共享 close Event 的重复 Hook 与对动画 mode 的稳态误判均已作废。当前修正以本文件顶部 refactor36 段落为准。

## v0.3-refactor36 执行情况

1. 接收用户的最新实机真值：r35 各类商店主体恢复，但上下连续翻页和 Y 道具说明没有实现正确；不以旧静态 PASS 覆盖实机反馈。
2. 复核 r33/r35 与原版地址，定位 `0x417829` 公共 Event 重复 Hook 和 `+0x580` 动画 mode 误读两项确定根因。
3. 复用公共 Event 的唯一所有权，只安装 Shop 两条 transition；信息失败仍不连坐商店主体。
4. 分页只点击原版按钮并观察原版状态，再请求真实行；没有直接写页、行或业务数据。
5. 上下边界分别落新页首/末；左右显式翻页保留行，延续既有同类菜单的连续操作规则。
6. 用户最终视觉数值采用两侧左10/下5；数量窗仍无手形。
7. 本轮修改限定 Shop、Runtime、UiBridge、地址/安装/检查器与文档；Tome/SaveSlot/Cursor 六文件继续逐字节等于 r33。
8. 27/27 严格编译、双链接一致、无 EXE 73/0、Oracle 93/0；明确仍待实机，不把机器 PASS 写成验收。
9. 遵守交付裁决：移除散装 `证据/`，以后如需证据只维护一个汇总文档。

## v0.3-refactor35 执行情况（历史）

1. 以用户实机结果覆盖静态猜测：r33 全店型 PASS、r34 全店型 FAIL，不为 r34 的静态 PASS 辩护；
2. 逐差分确认唯一阻断变化是 Shop 全局活动硬门与信息协议连坐，恢复 r33 `owner+active`，没有按店名堆补丁；
3. 重新闭合 opcode `0x36 -> 0x413FA0 -> 0x89FCD4` 与 `0x415912` 记录索引，证明装备/道具/药店同类；
4. 主体 4 锚点/11 Event 与信息 1 锚点/1 Event/2 transition 分开预检、分开安装，信息失败路径禁止关闭主体；
5. 保留用户要求的列手形下移、Y 独占、导航不开信息、数量窗无手形，不回滚已正确的 r34 反馈修正；
6. 天书/SaveSlot/Cursor 六文件逐字节等于 r33，偶发天书问题继续低优先级，本轮不扩大修改；
7. 27/27 独立 C 严格编译，两次链接字节一致；无 EXE 74/0、原版 EXE 94/0；
8. 文档明确标记 r34 主体失败与 r35 待实机，静态/Oracle PASS 不冒充用户验收。

## v0.3-refactor34 执行情况（历史；主体回归失败）

1. 以用户最新验收为真值：refactor33 地图和商店主体转为 PASS，本轮不重新设计已通过操作；
2. 从原版 `0x4145B0` 追到左右行 `code=1` 的完整后半段，确认信息窗是独立 transition，不再把它误称 hover；
3. Y 复用原版行 Event/信息 close Button，导航只拦最后开窗步骤；不写行、页、价格、库存或信息业务字段；
4. 数量窗沿用原版方框，使用已存在的隐藏命中 API，不修改公共 Cursor 模块；
5. 列手形只改用户要求的 Y，X 和动态 Button 几何规则不变；
6. 天书问题遵守用户“本版简单看”范围：用 refactor31/32/33 差分证明新入口在 Shop，六个天书/存档/Cursor 文件 SHA 冻结，只收紧 Shop 自己；
7. 当时把主类 vtable 与五个子对象设为共同硬门；用户实机证明该设计让所有商店失效，已由 refactor35 撤销；
8. 当时 27/27 严格编译及静态检查通过，但不能覆盖实机 FAIL；该版不得再作为部署候选。

## v0.3-refactor33 执行情况（历史，已验收）

1. 先以原版 EXE 证明各种商店共享 `0x413FA0`，再实现一套 Adapter，没有凭截图复制店铺逻辑；
2. 用户提供的四张参考图只用于视觉/交互规格，手形位置最终从真实 Button 几何动态计算；
3. 地图十字键复用已验收移动 Hook 和原版 `0x52`，没有注入键盘或改速度；
4. 商店价格、库存、金钱、页/行/类别、数量和成交结果零直接写，11 个动作全部回原版 ButtonEvent；
5. 新增 `shop.c/.h` 保持英文文件名、中文高密度注释和独立编译；
6. refactor32 已由用户验收并作为冻结 Oracle，本轮只修改必要的 Runtime/InputRouter/UiBridge/Plugin/Exploration 与新增 Shop；
7. 27/27 严格编译、可重复 PE 链接、73/0 无 EXE、92/0 原版 EXE 检查均通过；
8. 文档明确区分机器 PASS 与 refactor33 待实机，未把候选写成验收完成。

## v0.3-refactor32 执行情况（历史，已验收冻结）

- **先按用户反馈纠正需求方向：已执行。** 明确作废“固定回第一项”；以父 Inn 保存来源项实现诸态→诸态、炼化→炼化。
- **向前向后审计询问层：已执行。** 从前置 mode=3、wrapper、SaveSlot、`+0x5A4` 动作、`action+0x5B4` Yes/No 一直追到 wrapper 关闭，没有只修截图所在一层。
- **原始资料补缺但 EXE 裁决：已执行。** 补充固化包 JSON 用于定位线索；最终地址、短签名和调用边界由未修改 `RPG.exe.org` 复核，资料不进入交付。
- **共享能力按真实对象拆分：已执行。** SaveAction Hook 独立于 state7 安装；`save_point.c` 仍只管 owner；ConfirmDialog 仍只有一套；存档 I/O 仍归 RPG.exe。
- **输入所有权与脉冲闭合：已执行。** wrapper active 时仅放行深层 ConfirmDialog；SceneChoice 只维护已发 LEFTUP，所有旧剧情/底层新输入继续阻断。
- **删除错误跨模块耦合：已执行。** Synthesis 不 include/call Inn；来源历史由父 Inn 独占，鼠标/右摇杆接管路径保留，无 warp/Sprite 私写。
- **能力级 fail-closed：已执行。** wrapper、共享动作、天书继续各自预检，单能力失败不连坐其它 owner 或原版键鼠。
- **源码与构建：已执行。** 26/26 独立 C 严格编译，PE32/i386 无 CRT；无 EXE 71/0，`RPG.exe.org` 89/0，ASI 131,584 bytes。
- **全量接档与护栏同步：已执行。** 现行文档、证据、测试清单、产物标记和检查器均切到 refactor32；refactor31 两项错误只保留为历史反例。
- **静态与实机边界：已执行。** 当时机器检查未冒充实机；其后两条状态链已由用户验收通过并冻结。

## v0.3-refactor31 执行情况（历史且不完整）

- **先承认实机反证再重做 Oracle：已执行。** 没有为 refactor30 的文档结论辩护；重新枚举 `0x4245B0` 的三个直接 caller，并闭合地图事件、包装层发布、子 SaveSlot 绑定、Update 与销毁路径。
- **逆向资料只作证据：已执行。** 补充的 Godot 原始固化包用于交叉确认 opcode `0x3E`，最终 Hook/字段仍由未修改 `RPG.exe.org` 的机器码签名验证；资料没有混入源码或交付目录。
- **不复制已有能力：已执行。** 新 `save_point.c` 只做 owner 生命周期；`SaveSlot_Update()` 全项目仍只有一个定义，存档文件 I/O 继续全部归 RPG.exe。
- **最小修复炼化返回：已执行。** 不动已验收炼化导航/业务，只增加一次性 child→parent 焦点交接；复用 Inn 的既有 HitTest 视觉所有权。
- **键鼠优先：已执行。** 存档点按当前指针所有权决定是否立即显示焦点；炼化关闭途中实体鼠标/右摇杆接管会取消首项恢复；无鼠标瞬移和 Sprite 私写。
- **能力级 fail-closed：已执行。** 两条 ctor CALL 与三段 wrapper 签名独立预检；失败只关闭存档点 Adapter，不连坐天书、标题或原版键鼠。
- **自动护栏同步：已执行。** 删除错误的 state7 禁止模块规则，新增 owner 边界、唯一业务实现、调度顺序、焦点交接和 ASI 新鲜度检查。
- **源码与构建：已执行。** 26/26 独立 C 严格编译，PE32/i386 无 CRT 链接；无 EXE 71/0，`RPG.exe.org` 89/0。
- **全量中文接档：已执行。** 14 份现行文档全部遍历到 refactor31，明确把 refactor30 存档点结论标为历史错误。
- **静态不冒充实机：已执行。** 两项修复保持“机器 PASS、待用户实机”；用户已通过的旧能力继续冻结，不无故降级。

## v0.3-refactor30 执行情况（历史）

refactor30 正确修复并固化了 L/R 方向，但“存档点就是 state7、禁止 `save_point.c`”违反了 owner 生命周期证据，已由 refactor31 纠正。其余历史记录保留用于追溯。

## v0.3-refactor29 执行情况

- **先完整复核上一轮：已执行。** 修改前以 refactor28 + 完全未修改 `RPG.exe.org` 重新跑旧综合检查，并重新编译旧 22 单元；用户随后明确将 refactor28 整版实机裁决为 PASS。
- **需求先闭合原版协议再编码：已执行。** 客栈、炼化、剧情 mode=3 均先静态追出真实对象/按钮/调用链；没有凭截图硬猜对象类型。
- **禁止错误通用化：已执行。** 炼化/商店退出规则明确禁止 RMB；剧情“是/否”没有硬套菜单 ConfirmDialog，而是独立 SceneChoice Adapter。
- **业务结果归 RPG.exe：已执行。** 客栈不 new Interface/炼化、不写 HPMP；炼化不写类别/页/行/资格/结果；SceneChoice 不写剧情变量/结果；视觉焦点使用 RPG.exe 自带选择框，不显示第二套手形。
- **源码详细中文注释：已执行。** 新增 `inn.c/.h`、`synthesis.c/.h`、`scene_choice.c/.h` 均逐段说明字段、控制流、为什么这样做和禁止路径。
- **英文代码文件名：已执行。** 所有 `.c/.h/build.bat` 继续 ASCII 英文名。
- **严格编译：已执行。** 25/25 独立 C 单元以 x86 `clang-cl /W4 /WX /utf-8` PASS，并链接 PE32/i386 ASI。
- **自制工具同步：已执行。** `refactor_check.py` 增加三块新协议和禁止路径机械护栏，最终 86 PASS / 0 FAIL。
- **全量文档遍历：已执行。** 文档目录全部现行文件同步到 refactor29；旧版本状态只保留历史语境。
- **静态不冒充实机：已执行。** refactor29 三块新增能力继续标“实机候选”。


## v0.3-refactor28 历史执行情况

- **开发前完整复核：已执行。** 在写新功能前先用未修改 `RPG.exe.org` 对 refactor27 跑综合检查，得到 80 PASS / 0 FAIL，并对 22 个单元重新编译，确认上一版不是带病基线。
- **用户实机裁决同步：已执行。** refactor27 state7/state8 业务改为 PASS/冻结；state8 仅保留用户明确提出的视觉坐标微调。
- **先闭合原版协议再写代码：已执行。** 从 `0x43C160` Update 追到 `+0x768`、`0x43C920`、五个角色 Button、`0x43C933` 左键选择和 `0x43C1F7` 右键取消；没有靠截图猜点击区域。
- **业务结果归原版：已执行。** 插件不写目标角色 ID、治疗结算或 `+0x58C/+0x588/+0x788/+0x78C`；只在真实 ButtonEvent CALL 注入 code。
- **模块边界：已执行。** 治疗目标作为 state3 页面 modal 留在 `interface_skills.c`；没有复制 Battle Target，也没有新造通用 Target 系统。
- **键鼠兼容：已执行。** 无 pending 时 UiBridge 调回原函数；实体鼠标/右摇杆接管只撤私有焦点，不取消原版 `+0x768`。
- **输入所有权：已执行。** `InterfaceSkills_TargetSelectionActive()` 阻断 Shell；modal 自己消费与目标无关的菜单键。
- **详细中文源码注释：已执行。** 新增字段、每条协议、为什么不能写业务数据、为什么要让路键鼠均写入源码内行间注释。
- **构建复核：已执行。** 22/22 x86 C `/W4 /WX /utf-8` PASS；PE32/i386 链接 PASS。
- **自制工具同步：已执行。** 工具加入治疗目标协议、A/B code、modal、真实 Button 几何、禁止业务字段直写、state8 坐标微调护栏。
- **全量文档同步：已执行。** 14 份现行文档全部转为 refactor28 权威状态；旧“state7/state8 待验”等内容移入明确历史语境。
- **静态不冒充实机：已执行。** refactor28 治疗目标继续标“实机候选”；只有 refactor27 已由用户验收的 state7/state8 业务可以写 PASS。

### refactor28 交付门槛

- 22/22 严格编译 PASS；
- `RPG.exe.org` 综合静态检查 0 FAIL；
- 编译内容仅 ASI + INI；
- 无 OBJ/PYC/EXP/缓存；
- 全文件 SHA 清单、ZIP CRC、ZIP SHA-256 完成；
- 包顶层目录保持简短 `幽城DLL_v0.3-refactor28`。

---

## 历史累计记录（只作因果证据）

> 以下内容保留各历史版本当时的设计、失败方案、地址和测试记录。里面出现的“当前/待验/下一步”不再代表 refactor28 现行状态。

## v0.3-refactor27 执行情况

- **先复核上一轮：已执行。** 以 refactor26e 源码、完整接档和用户在本次聊天记录后续给出的 PASS 裁决为基线，确认 state6、剧情 A、SDL3 热切换已经通过后才进入 state7/state8。
- **只做手柄模块职责：已执行。** 天书只控制原版 Save UI；没有实现存档系统。机能只控制原版 ButtonEvent；没有实现音量/配置系统。
- **共享 SaveSlot：已执行。** state7 直接复用 `save_slot.c`，没有复制标题读档导航。新增 SaveAction 与历史五 Hook 独立安装，降低对已 PASS 标题读档的影响面。
- **页面专属 Adapter：已执行。** state8 独立 `interface_options.c/.h`，不为了抽象去改 state5 五内或其它冻结页面。
- **原版协议优先：已执行。** state7 三项窗口、state8 五个动作均只注入原版 ButtonEvent；不直接写游戏业务值。
- **输入所有权：已执行。** 新增 SaveAction / Options Context，modal 与 Shell 优先级明确，业务层不读 SDL 数字键。
- **详细中文行内注释：已执行。** 新增/修改源码逐步说明字段、输入为什么消费/放行、为什么不能直接写业务值、焦点位置如何计算。
- **双原版 SHA 裁决同步：已执行。** 历史精确目标与 `RPG.exe.org` 现在都作为支持 PASS 样本，并继续逐协议 fail-closed。
- **自制工具更新：已执行。** `refactor_check.py` 扩展 state7/state8、22编译单元、双 SHA、SaveSlot 稳定 Hook/新增 Hook 隔离等机械护栏。
- **全量文档遍历：已执行。** 14 份现行文档全部同步 refactor27；旧“剧情待验/SDL热切换暂停/第二SHA单独FAIL”改为历史语境。
- **静态不冒充实机：已执行。** 80/0 与构建 PASS 只标机器验证；state7/state8 明确保留“实机候选”。

### refactor27 交付门槛

- 22/22 独立 C 严格编译 PASS；
- PE32/i386 链接 PASS；
- 当前 `RPG.exe.org` 综合检查 80 PASS / 0 FAIL；
- state7/state8 新协议全匹配；
- `编译内容/` 仅 ASI + INI；
- 所有源码文件名 ASCII；代码中文详细注释护栏通过；
- 文档/证据文件中文命名；
- 最终 ZIP 前生成全文件 SHA-256 清单并做 CRC 完整性测试。

## v0.3-refactor26e 历史执行情况

- **先完整复核上一轮与更早稳定版：已执行。** 没有只比较 refactor26c→26d；按用户要求纵向对照 refactor21/refactor22/refactor24→refactor25→refactor26d。
- **剧情 A 根因闭合：已执行。** `dialogue_input.c/.h` 与 refactor24 字节一致、worker `DialogueInput_Update()` 一直存在；唯一缺失是 refactor25 起漏装 `DialogueInput_InstallHook()`。
- **与 refactor24 恢复完整性对照：已执行。** 14/14 业务 Hook installer 能力与 14/14 worker 关键阶段均完整；机器护栏只锁真实依赖/输入消费偏序，不再机械要求互不重叠模块与 r24 文本顺序完全一致。
- **安装能力与真实依赖恢复：已执行。** 当前 14/14 业务 Hook installer 全部存在且无重复；UiBridge 等真实依赖满足。互不重叠 installer 保留清晰模块化顺序，不把 r24 文本排列当成永久 ABI。
- **用户 PASS 裁决同步：已执行。** 五内整页正式 PASS/冻结；法宝确定取消继续 PASS/冻结；Battle Target、Battle禁用A、SDL3拔插重连继续冻结。
- **【refactor26e 历史】SDL 热切换当时暂停：已执行。** 后续用户已经实机确认热切换 PASS；refactor27 只保留这条历史因果，不再把它列为待查。
- **详细中文行内注释：已执行。** `plugin.c` 逐步解释为什么算法本体没坏、installer 漏装为何会导致 A 完全失效、为何恢复 r24 相对顺序。
- **自制工具更新：已执行。** `refactor_check.py` 新增 Dialogue installer、14项 r24 installer 能力完整性/真实依赖、14项 worker 关键阶段/输入消费偏序三道机械护栏。
- **【refactor26e 历史】全量文档遍历：已执行。** 当时同步五内 PASS、Dialogue 候选和 SDL 暂停；这些状态现已被 refactor27 的后续用户裁决覆盖。
- **静态不冒充实机：已执行。** Dialogue A 仍标待验；五内不再错误写成候选。

### refactor26e 交付门槛

- 20/20 独立 C 严格编译必须 PASS；
- 无 EXE 静态检查必须 0 FAIL；
- r24 14/14 installer 能力完整 + 14/14 worker 关键阶段/真实依赖对照必须 PASS；
- 用户原版第二样本除正式 SHA 边界外所有协议必须 PASS；
- `编译内容/` 只保留 ASI + INI；
- ZIP 前重新生成完整文件 SHA 清单并验证 CRC。

## v0.3-refactor26d 历史执行情况



- **先复核上一轮：已执行。** 以 refactor26c 实机日志为证据，先确认 Button `+0x28/+0x2C` 已释放，再继续反汇编 Sprite 层，不重复修已排除的根因。
- **用户 PASS 裁决同步：已执行。** 法宝确定/取消正式转 PASS/冻结；五内手形鼠标视觉继续 PASS/冻结。
- **动画根因闭合：已执行。** 证明 `0x43E6A0(0)` 进入 Sprite state5 循环而非 idle；证明 Button 构造 `0x4312C0` 使用 `0x43E6F0(0)` 建立普通态。
- **不直接写动画内部字段：已执行。** synthetic release 后只记录 Button，下一 state5 游戏线程扫描调用原版 `0x43E6F0(0)`；不写 Sprite `+0x1C`/帧号。
- **已 PASS 模块防回归：已执行。** `battle.c`、`pad_input.c/.h`、`interface_items.c`、`confirm_dialog.c/.h` 与 refactor26c 字节级一致。
- **五内鼠标视觉冻结：已执行。** `inner_focus_anchor()+inner_show_node_focus()` 固定片段 SHA 继续通过。
- **源码详细中文注释：已执行。** 新增 idle_reset 状态、恢复时序、为何必须在游戏线程执行均有逐步中文说明。
- **全量文档遍历：已执行。** 13 份现行 Markdown + 1 份放置说明同步 refactor26d；历史版本章节保留但明确为历史。
- **自制工具随包：已更新。** 新增 `0x43E6F0` EXE 签名、release→idle 链、禁止直接 Sprite 状态写的机械护栏。
- **静态不冒充实机：已执行。** 五内按钮动画仍标候选。

### refactor26d 交付门槛

- 20/20 独立 C 严格编译 PASS；PE32/i386 链接 PASS；
- ASI 128,512 bytes，SHA-256 `25158c7bb6524d72e9ec77084393693d518a32325b2d1b03a49bbdf4b48f9b94`；
- 无 EXE 静态检查 **60 PASS / 0 FAIL**；
- 用户完全未修改原版第二样本 **71 PASS / 1 FAIL**，唯一 FAIL 为正式精确 SHA；
- state5 新增 Button Sprite idle 入口 **1/1 PASS**；
- `编译内容/` 只保留 ASI + INI，无 OBJ/LIB/EXP/PYC/缓存。



> **refactor26e 历史状态（当时）：** refactor26d 的 state5“五内”已由用户实机确认**整页 PASS**：X/LT/RT/Y 功能、原版手形鼠标焦点与右上偏移、加减/蕴魂按钮瞬时动画均冻结；refactor26c 的法宝/道具弃置/分发确定取消继续 **PASS/冻结**；Battle Target、Battle 不可交互项 A、SDL3 拔插重连等已通过项继续冻结。当前明确待验只剩**普通剧情对话 A 推进恢复**。

> **refactor26e 剧情 A 根因与 r24 能力完整审查：** 纵向对照 refactor21/refactor22/refactor24→refactor25 后确认，`dialogue_input.c/.h` 与 worker 的 `DialogueInput_Update()` 一直存在，但 refactor25 大重构把 `DialogueInput_InstallHook()` 从总安装链漏掉；refactor26c 只恢复了 ConfirmDialog，没有恢复 Dialogue。refactor26e 仅恢复缺失的 Dialogue installer，并把 refactor24 作为“已验证能力与用户效果 Oracle”做完整对照：14/14 业务 Hook installer 必须全部存在且无重复，UiBridge 必须先于依赖它的业务 Hook；worker 的 14 个关键阶段必须全部存在并满足真实输入消费/Context 偏序。互不重叠的 installer 不再为了形式而强制逐项复刻旧版顺序。【历史】refactor26e 时 SDL“热切换”曾被暂时当作可能误判并暂停；其后用户已实机确认热切换 PASS，现与拔插重连一起冻结。

> **当前机器验证：** 无 EXE 静态检查 **64 PASS / 0 FAIL**；用户提供的完全未修改原版第二样本 **75 PASS / 1 FAIL**，唯一 FAIL 仍是历史正式精确目标 SHA 不同；r24 对照采用“能力完整 + 真实依赖”判据：**14/14 业务 Hook installer 全部存在且无重复，14/14 worker 关键阶段全部存在，所有 UiBridge/输入消费/Context 偏序均 PASS**。最终 ASI 为 PE32/i386，130,560 bytes，SHA-256 `64df5023224183937edfb1f5380e6d9893667b3e759275e6966befddc26c0389`。正式精确目标 SHA-256 继续为 `b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf`；第二样本 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f` 在 refactor26e 当时只作为可信 Oracle；refactor27 起已按用户裁决纳入协议兼容 PASS 白名单。

## v0.3-refactor26c 历史执行情况

- **先完整复核上一轮：已执行。** 先复核 refactor26b 当前源码、refactor21 的启动安装链与旧沟通记录，再改代码；没有凭 refactor26a 的“路由层推测”继续补丁叠补丁。
- **法宝确定/取消修根因：已执行。** 证明 refactor21 有 `ConfirmDialog_InstallHooks()`、refactor25～26b 丢失；恢复该安装步骤，让第二类 `0x4276F0` 的 2 HitTest + 2 Event CALL 真正接入 UiBridge。
- **页面业务边界不倒退：已执行。** 恢复低层 Hook 不等于恢复全局万能 Yes/No；state2/state3 popup 继续优先，ConfirmDialog 在页面弹窗活动时让路。
- **五内鼠标视觉用户 PASS 冻结：已执行。** `inner_focus_anchor()+inner_show_node_focus()` 完整源码片段 SHA 固定为 refactor26b 通过版本；本轮只改动画事务。
- **五内动画修事务而非动画字段：已执行。** 不直接写 Sprite 动画；把已经开始的 pulse 从鼠标焦点所有权中解耦，指针接管不能取消 press→release；watchdog 对已按下事务只推进 release。
- **已 PASS/冻结模块不碰：已执行。** Battle Target、SDL3 热插拔等 SHA 护栏继续通过。
- **详细中文行内注释：已执行。** 新增代码逐步解释为什么第二类 popup 以前无消费者、为什么按下动画必须保证 release。
- **全量文档遍历：已执行。** 13 份 Markdown + 1 份放置说明全部同步 refactor26c；旧 refactor26/26a/26b 结论只保留为历史。
- **自制工具随包：已更新。** 新增 ConfirmDialog 安装链护栏、五内鼠标视觉片段 SHA 冻结、pulse 必完成/指针接管/watchdog 检查。
- **静态不冒充实机：已执行。** state2 确定/取消与五内按钮动画仍标候选；五内鼠标视觉单独标用户 PASS。

### refactor26c 交付门槛

- 20/20 独立 C 严格编译 PASS；PE32/i386 链接 PASS；
- refactor26c 当时 ASI：128,000 bytes，SHA-256 `d31d1bb2770bd4ea903adf152ab3eb911ba7a1315b489f8229c8905631a56c72`；
- 无 EXE静态检查 **59 PASS / 0 FAIL**；
- 用户完全未修改原版第二样本 **70 PASS / 1 FAIL**，唯一 FAIL 为正式精确 SHA；
- ConfirmDialog 第二类协议在第二样本为 2/2 vtable + 4/4 CALL PASS；
- `编译内容/` 最终只保留 ASI + INI；
- 无 OBJ/LIB/EXP/PYC/缓存；
- 最终文件校验清单在所有内容冻结后重新生成。

## v0.3-refactor26b 历史执行情况

- **先复核再修：已执行。** 在改五内前重新反汇编 `0x440610 / 0x431400 / 0x431380 / 0x43E6F0 / 0x43E6A0`，确认真实鼠标两帧状态后才修改。
- **修根因而不是清动画字段：已执行。** 没有直接写 Sprite 动画状态；改为同步原版 Button+0x2C/+0x28 鼠标历史。
- **悬空指针安全：已执行。** pulse 中断/超时只在重新证明 Button 仍属于当前 state5 后清历史。
- **已 PASS/冻结模块不碰：已执行。** Battle Target、PadInput 热插拔、state2 popup 热修等业务源码 SHA 保持。
- **详细中文行内注释：已执行。** state5 每个历史字段、两帧顺序、视觉偏移和失败边界均在代码内逐步说明。
- **全量文档遍历：已执行。** 14 份现行文档全部同步 refactor26b 当前结论；历史 refactor26/26a 仍保留为历史。
- **自制工具最新稳定版随包：已执行。** 新增五内鼠标历史 + 焦点偏移机械护栏。
- **静态不冒充实机：已执行。** 五内视觉继续标候选。

### refactor26b 交付门槛

- 20/20 独立 C 严格编译 PASS；PE32/i386 链接 PASS；
- ASI：125,952 bytes，SHA-256 `2fc8c9c1ea4619b844c1537fd983feaf96aaec6b97561fa183e3ba290d6a90d7`；
- 无 EXE：58 PASS / 0 FAIL；
- 第二原版样本：69 PASS / 1 FAIL，唯一 FAIL 为正式精确 SHA；
- 编译内容只保留 ASI + INI；无构建垃圾；
- 文件校验清单最后生成。

### refactor26a 历史执行情况

- **先复核再修：已执行。** 完整对比 refactor25→refactor26 源码文件 SHA，并逐函数审计 r26 发生变化的输入模块后才修改。
- **根因而非补丁猜测：已执行。** 明确证明 state2 LEFT/RIGHT Policy 从 PASS→OVERRIDE，而 popup 仍读 COMMON；修复为 OVERLAY，不改原版 popup 协议。
- **大修遗漏系统性检查：已执行。** 全源码交叉扫描所有 `InputRouter_PressedOn/DownOn/ReleasedOn` 与 Context Policy；修复前只有 state2 popup 左/右两条不一致，未发现第二个同类错配。
- **静态工具防复发：已执行。** `refactor_check.py` 新增通用 Policy/Layer 检查。
- **已 PASS/冻结模块不碰：已执行。** Battle/Target、PadInput 热插拔、Frontend/SaveSlot、Dialogue、Exploration、Movie、Cursor、Runtime 等未因热修重写。
- **详细中文行内注释：已执行。** `interface_items.c` 写明 r25→r26 Layer 变更、为何 popup 必须读 OVERLAY、为何不能穿透分页。
- **全量文档遍历：已执行。** 14 份现行文档/说明全部检查并同步当时 refactor26a 与历史 refactor26 边界。
- **自制工具最新稳定版随包：已执行。** 工具说明同步新增 Policy/Layer 机械护栏。
- **静态不冒充实机：已执行。** popup 热修继续标候选。

## v0.3-refactor26 执行情况

- **先完整复核上一轮：已执行。** 修改前先运行 r25 `refactor_check.py`，55 PASS / 0 FAIL，并重新阅读 InputRouter、Shell、state2～state5、UiBridge/PadInput 关键链。
- **用户最新操控规格优先：已执行。** 角色切换迁到左摇杆50%；D-Pad左右统一给法宝/绝学/及身翻页；LT/RT、X 页面原义保持。
- **已 PASS 模块防回归：已执行。** Battle Target 与 SDL3 热插拔继续整文件 SHA 冻结；Battle 不可交互项 A 的事务护栏继续检查。
- **合法增强不被错误SHA冻结：已执行。** state3 原业务已 PASS，但用户明确新增左右翻页，因此取消 `interface_skills.c` 整文件 SHA 门槛，改用协议/无直写/确认框安全护栏。
- **五内动画按原版协议修：已执行。** 重新反汇编 `0x440610`，按 `0x431400` 按下 → 下一游戏帧 `0x431380` 释放/确认重放，不手改 Sprite 动画状态。
- **页面翻页只复用现成事务：已执行。** state2/3/4 D-Pad 左右分别调用各自已有 request_page，不复制页码状态机。
- **详细中文行内注释：已执行。** 左摇杆迟滞、两阶段脉冲、500ms保险丝、输入所有权均有逐步注释。
- **代码文件英文、文档文件简体中文：已执行。**
- **全量文档遍历：已执行。** 14 份现行 Markdown 与放置说明统一到 refactor26 当前操控规则；历史 r25 只保留为因果记录。
- **自制工具随包：已更新。** `refactor_check.py` 新增左摇杆50%、三页左右翻页、五内两阶段 pulse 护栏。
- **静态不冒充实机：已执行。** 新操控与五内动画修复仍标候选。

## v0.3-refactor25 历史执行情况

- **先复核上一轮：已执行。** 先核对 r24 源码与用户最新实机反馈，再改键位。
- **用户裁决优先：已执行。** Battle Target 按用户接受的旧成熟方案正式 PASS，不再继续“优化排序”。
- **已通过底层冻结：当时已执行。** r25 曾对 `battle.c`、`pad_input.c/.h`、state3 技能页做 SHA 护栏；refactor26 因用户明确给 state3 增加 D-Pad 分页，已合法取消 state3 整文件 SHA，仅继续冻结 Battle/PadInput。
- **state4 只做明确增量：已执行。** LT/RT 复用现成 `equipment_request_page()`，不复制分页、不写页码。
- **state5 分层：已执行。** 页面只管 X/Y/LT/RT；左右 PASS 给 Shell，Shell 统一角色切换。
- **原版业务优先：已执行。** state4 分页与 state5 加减均点击真实原版 ButtonEvent。
- **详细中文行内注释：已执行。** 新按键为什么属于页面/Shell、为何不直接写字段、分页等待保险丝均在代码内解释。
- **源码文件名英文、非代码文档简体中文：已执行。**
- **中文日志：继续保持。**
- **全量文档遍历：已执行。** 14 份现行文档全部检查并同步 Target/热插拔 PASS、state4/state5 新键位、state6 待验。
- **完整接档：已累计更新。** r24 历史保留，r25 当前权威增量单独追加。
- **自制工具随包：已更新。** `refactor_check.py` 新增 Target/热插拔 SHA 冻结、state4 Trigger 分页、state5 Trigger/左右路由、Shell state5 角色范围护栏。
- **静态不冒充实机：已执行。** state4 新 Trigger、state5 当前视觉/新键位、state6 仍写待验。

## refactor26 历史交付门槛

- 20/20 独立 C：`/W4 /WX /utf-8` 等价严格编译通过；
- PE32/i386 链接通过；ASI SHA-256 `02f108bb50ec36eb25342b594291052e283d1dcb5adf7c529366c758e066c6c8`，大小 125,440 bytes；
- `refactor_check.py` 无 EXE **56 PASS / 0 FAIL**；
- 原版第二样本 **67 PASS / 1 FAIL**，唯一 FAIL 为正式 SHA；
- ZIP CRC PASS；
- `编译内容/` 只含 ASI + INI；
- 无 `.obj/.lib/.exp/.pyc/__pycache__`；
- 文件校验清单最后生成。
