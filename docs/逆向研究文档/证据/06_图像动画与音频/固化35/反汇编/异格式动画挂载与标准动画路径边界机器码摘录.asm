# 固化35：异格式动画挂载、动态路径、标准SF2 Reader与RLE调用边界机器码摘录
# 来源：用户本轮 RPG.exe.org（SHA256 8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f）
# 说明：这是便于人工复核的反汇编证据摘录，不代表给所有函数强行命名。

# ---------------------------------------------------------------------------
# 1. ItfDir.DAT 在启动资源注册表中被挂载，别名为 "itf"。
#    这证明 SAF 所在容器进入了资源系统；它不等于证明 ITF000.SF2 被请求。
# ---------------------------------------------------------------------------
0043A1F0  68 74 B9 46 00        push 0x46B974        ; "itf"
0043A1F5  68 54 B9 46 00        push 0x46B954        ; "..\\MultiMedia\\Fight\\ItfDir.DAT"
0043A1FA  E8 31 FB 00 00        call 0x449D30        ; 资源容器注册/挂载链中的调用

# ---------------------------------------------------------------------------
# 2. 两处已知代码会使用 FIGHT\\ 前缀与 "%s%s\\%s.SF2" 动态拼接路径。
#    因此“EXE里没有完整 ITF000.SF2 字面串”不能再当作全局不可达证明。
# ---------------------------------------------------------------------------
0041F7F9  68 78 9F 46 00        push 0x469F78        ; "FIGHT\\"
0041F7FE  68 6C 9F 46 00        push 0x469F6C        ; "%s%s\\%s.SF2"
0041F803  56                    push esi
0041F804  E8 6A 22 03 00        call 0x451A73

0043DD15  68 78 9F 46 00        push 0x469F78        ; "FIGHT\\"
0043DD1A  68 6C 9F 46 00        push 0x469F6C        ; "%s%s\\%s.SF2"
0043DD1F  57                    push edi
0043DD20  E8 4E 3D 01 00        call 0x451A73

# ---------------------------------------------------------------------------
# 3. 0x4070D0 对象初始化直接读取“标准SF2”的描述表位置：
#    count 在 +0x14/+0x1E/+0x28/+0x32；offset 在 +0x16/+0x20/+0x2A/+0x34。
#    SAF 的四段描述表从 +0x0C 开始，因此此函数不能直接消费 SAF 原始逻辑布局。
# ---------------------------------------------------------------------------
004070FF  66 8B 48 14           mov cx, word ptr [eax+0x14]
00407128  8B 50 16              mov edx, dword ptr [eax+0x16]
00407133  8B 48 20              mov ecx, dword ptr [eax+0x20]
0040713B  8B 50 2A              mov edx, dword ptr [eax+0x2A]
00407143  8B 48 34              mov ecx, dword ptr [eax+0x34]
00407165  66 39 7A 1E           cmp word ptr [edx+0x1E], di
0040717F  66 39 78 32           cmp word ptr [eax+0x32], di

# ---------------------------------------------------------------------------
# 4. 该标准SF2对象链会调用 canonical RGB16 RLE decoder 0x44CD7B。
# ---------------------------------------------------------------------------
004071CC  66 8B 48 09           mov cx, word ptr [eax+0x09]   ; tile height
004071D4  66 8B 50 07           mov dx, word ptr [eax+0x07]   ; tile width
004071D9  E8 9D 5B 04 00        call 0x44CD7B

# ---------------------------------------------------------------------------
# 5. 严格SF2构造器先比较前三字节 "SF2"；不相等立即走拒绝分支。
#    因此 SAF 不会因为也使用 0x6F/zlib 包装就自然进入标准SF2构造流程。
# ---------------------------------------------------------------------------
00428C21  6A 03                 push 3
00428C23  68 08 A6 46 00        push 0x46A608        ; "SF2"
00428C28  56                    push esi
00428C29  E8 92 41 03 00        call 0x45CDC0        ; 三字节比较
00428C31  85 C0                 test eax, eax
00428C33  75 22                 jne 0x428C57         ; 非SF2 -> 拒绝
00428CCB  8A 45 0B              mov al, byte ptr [ebp+0x0B]
00428CCE  3C 6F                 cmp al, 0x6F         ; 只有魔数通过后才看压缩标志
00428CD2  8B 85 BC 42 00 00     mov eax, dword ptr [ebp+0x42BC]
00428CD8  8B 8D C0 42 00 00     mov ecx, dword ptr [ebp+0x42C0]
00428CF6  81 C5 C4 42 00 00     add ebp, 0x42C4     ; 标准SF2 0x6F尾部起点

# ---------------------------------------------------------------------------
# 6. canonical RGB16 RLE decoder 的另一个直接 E8 caller 仍属于标准SF2对象链。
#    固化35全 .text 扫描得到该 decoder 只有 0x4071D9 与 0x428E48 两个直接caller。
# ---------------------------------------------------------------------------
00428E1F  66 8B 58 07           mov bx, word ptr [eax+0x07]
00428E25  66 8B 68 09           mov bp, word ptr [eax+0x09]
00428E2B  66 8B 78 28           mov di, word ptr [eax+0x28]
00428E48  E8 2E 3F 02 00        call 0x44CD7B

# 固化边界：
# - SAF磁盘Section2已经证明与该RLE算法bit-exact兼容；
# - 但当前EXE中尚未定位到“SAF原始布局 -> canonical RLE decoder”的已证直接Reader链；
# - 不能由此证明Reader绝对不存在，函数指针、内联实现、其他模块/版本仍在证据边界之外。
