; Castle PadSupport 公共 API v1 导出表。  
; 这里写真实的 .asi 文件名，避免链接器按默认 .dll 名称生成冲突的 /OUT 指令。  
; x86 内部 C 符号带前导下划线，但导出表使用未修饰名称，外部插件才能稳定查询。  
LIBRARY Castle_PadSupport.asi
EXPORTS
    CastlePad_GetApi
    CastlePlugin_Query
    InitializeASI
    CastleRuntimeClient_NotifyLoaderReady
