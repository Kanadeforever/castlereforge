> 当前权威版本：v0.3-refactor42（自动最近目标 + 确定/取消布局候选，2026-08-24）。实机稳定基线为 v0.3-refactor41：默认确定键按住/松开互动/取消键退出与 LT 兼容模式均已确认正常；R40目标轮盘及更早基线继续冻结。R42 新增两个默认开关：`Investigation.AutoFocusNearest=1` 在每次建立调查会话时复用R40距离第0项、肩键锁和resolver/25点probe自动尝试最近目标；`Controls.SwapConfirmCancel=0` 为南键确定/东键取消，设为1切换成东键（PS O）确定/南键（PS X）取消。确定/取消交换覆盖所有语义页面、调查激活和Back/RT鼠标左右键（确定=左键、取消=右键），但RB+ABXY战斗快捷键、X/Y专属功能等固定物理位置操作不变。用户补回的INI中文注释全部保留，新选项同样使用中文。PadInput、Cursor、RPG地址和业务UI冻结。下文更早“当前/候选”表述与本段冲突时按历史记录处理。

# refactor42 检查器补充

检查器继承R41全部护栏，并新增：

- INI公开键增为20；必须 `SwapConfirmCancel=0`、`ActivationMode=0`、`AutoFocusNearest=1`；
- 验证用户中文INI关键句全部存在，拒绝英文hold-A模板回退；
- 自动聚焦必须只在session_started第一帧、右杆处理前调用既有肩键+1；分支不得直接点击/震动/读取普通hover；
- Runtime必须默认1读取AutoFocus；
- InputRouter必须区分fixed物理表和可交换Confirm/Cancel表，并提供Raw语义接口；
- ControlModes/Investigation禁止直读PAD_SOUTH/EAST；调查激活、取消、确认必须跟随Swap；
- 鼠标模式必须确定Raw→左键、取消Raw→右键；
- Chord接口必须使用fixed表，battle.c保持旧调用和冻结SHA；
- X/Y物理映射不变；
- 修改的input_router/runtime/investigation文件纳入初学者注释比例和关键解释门；
- ASI编译标记必须包含自动最近目标、两种布局、鼠标语义和物理快捷不变。

最终机器计数与哈希在完整构建后写回。静态PASS不冒充实机PASS。

---

# 工具详细说明

> **v0.3-refactor36 当前候选（2026-08-22）：** refactor33 的统一商店主体已经用户实机确认；refactor35 实机反馈为各商店主体可用，但上下到列表边界不会连续翻页、移动项目会自动打开道具信息且 Y 无法关闭。本轮只修 Shop：上下越界分别调用原版上一页/下一页并落到新页末项/首项；左右显式翻页尽量保留当前行；两侧列手形相对 refactor35 统一向左 10 像素、向下 5 像素。
>
> **Y 道具信息裁决：** `0x417829` 与公共 `CALL_CMD0_EVENT` 是同一条原版 close Event CALL，必须复用全局 Hook，不得再次安装；`info+0x580` 只表示打开/关闭动画，稳态必须比较 `info+0x56C` 与原版关闭/打开目标 `+0x588/+0x58C`。方向/X/翻页只更新原版项目链并抑制最后开窗步骤；仅 Y 放行打开，已打开时 Y 点击 `info+0x598` 的真实关闭按钮。信息窗及动画期间其它商店输入保持模态阻断。
>
> **边界与验证：** 不按装备店、道具店、药店名称分支，继续复用 opcode `0x36 -> 0x413FA0 -> 0x89FCD4` 的同一 Shop Adapter；价格、库存、页、行、数量与成交结果均不直接写。天书偶发跳焦点继续低优先级，Tome/SaveSlot/Cursor 六文件逐字节保持 refactor33。27/27 C 单元严格编译通过，两次无时间戳 PE32/i386 链接字节一致；无 EXE **73 PASS / 0 FAIL**，未修改 `RPG.exe.org`（SHA-256 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`）**93 PASS / 0 FAIL**。ASI **153,600 bytes**，SHA-256 `05e7ef5e96f32d6a55a819a5cb590ac2302adb9441564495a3df62882af2ef0c`。机器 PASS 不冒充实机 PASS。
>
> **交付裁决：** 从本版起不再内置散装 `证据/` 树；源码、文档与检查器构成接档材料。若以后确有新增证据，只合并维护单一 `文档/验证汇总.md`。
>
> **历史阅读规则：** refactor32 的存档点完整路由与客栈原位返回、refactor33 的地图十字键仅步行和统一商店主体均已验收并冻结；refactor34 的全局 Shop 形状硬门、refactor35 对共享 close Event 的重复 Hook 与对动画 mode 的稳态误判均已作废。当前修正以本文件顶部 refactor36 段落为准。

## refactor36 自制检查工具增量

`工具/refactor_check.py` 新增或改写以下护栏：

- `0x417829` 必须与 `CALL_CMD0_EVENT` 共址且不允许 `UiBridge_InstallShopItemInfoHooks` 重复安装；Shop 信息能力只安装两条 transition。
- 稳态判断必须引用 `SHOP_ITEM_INFO_CURRENT_POSITION/CLOSED_POSITION/OPEN_POSITION`，禁止把动画 mode 当永久开关。
- 上下边界必须分别声明 first/last landing；左右翻页必须声明 preserve landing；分页只允许原版 ButtonEvent 路径。
- 列标记公式固定为左10/下5；数量窗仍禁止可见手形。
- 交付根不得存在 `证据/`；若存在验证材料只允许单一 `文档/验证汇总.md`。
- ASI 新鲜度标记升级为 refactor36；结果为无 EXE 73/0、Oracle 93/0。工具只能验证结构和样本协议，不能替代实机。

## refactor35 自制检查工具增量（历史）

`工具/refactor_check.py` 把 refactor34 错误的全绑门改成可机械验证的两级能力：

- 主体必须保持 refactor33 的 4 个锚点/11 个 Event，`shop_active_owner()` 只能使用有效 owner 与根 active，禁止 vtable 或子对象链成为全局入口条件；
- 信息窗单独校验 1 个 Update 锚点、1 个 close Event 和 2 个 transition，并要求安装失败路径中不存在 `g_shop.enabled=0`；
- 检查 Y 在 Shop Root 策略为 OVERRIDE，打开走当前行 `SHOP_ROW_INTENT_ITEM_INFO`，关闭走 `info+0x598` 真实 Button；
- 检查方向/X 使用 `SHOP_ROW_INTENT_NAVIGATION`，并在信息 transition wrapper 中只抑制打开步骤；
- 检查数量窗使用 `Cursor_MoveHiddenSelectionAt` 且旧 `shop_show_popup_marker` 已删除；列手形 Y 必须为 `top-height`；
- 保留 `marker_visible` 对公共手形 Hide 的所有权限制，并锁定 `interface_tome`、`save_slot`、`cursor` 六文件逐字节等于 refactor33；
- ASI 新鲜度标记升级为 refactor35 的“恢复全商店入口 / Y 信息独立 / 其它模块冻结”。

最终结果：不带 EXE **74 PASS / 0 FAIL**；带未修改 `RPG.exe.org` **94 PASS / 0 FAIL**。

## refactor33 自制检查工具增量（历史）

`工具/refactor_check.py` 新增：

- 原版商店主界面/数量窗 4 个短签名和 11/11 ButtonEvent CALL 精确核验；
- `shop.c/.h`、两个 Shop Context、owner 生命周期、pointer takeover、列手形、类别反向 native index、行 `code=1` / 点击 `code=2`、数量窗 LT/RT 与真实退出图标结构检查；
- 禁止 Shop 使用 RMB 或直接写 owner/list/popup 商店业务字段；
- 地图 D-Pad 四语义、`0x21..0x28` 八方向、十字键优先、`force_walk` 与左摇杆恢复结构检查；
- Windows 构建单元数更新为 27，ASI 必须包含 refactor33 地图/商店新鲜度标记。

最终结果：不带 EXE 73 PASS / 0 FAIL；带未修改 `RPG.exe.org` 92 PASS / 0 FAIL。工具只证明静态结构和样本协议，不代替真实游戏验收。

## refactor32 自制检查工具增量（历史）

`工具/refactor_check.py` 在 refactor31 wrapper 护栏基础上增加以下机械验证：

- **存档点原版协议组**：验证 `0x40DB2D -> 0x413B00`、`0x413C3F -> 0x4245B0` 两条 CALL，以及 `0x40DB32 / 0x413C48 / 0x413D89` 三段发布、绑定、Update 短签名。
- **包装层边界**：要求 `save_point.c` 只读 `0x89FCD0`、`wrapper+0x580/+0x584`，并调用共享 `SaveSlot_Begin/Update/End`；禁止复制槽位、动作、确认或文件 I/O。
- **共享动作独立能力**：验证 `0x4262C0`、`0x426365`、`0x426387`；要求 `SaveSlot_InstallActionHooks()` 全局独立安装，禁止继续塞在 `interface_tome.c` 或依赖 state7 ctor。
- **完整深层模态**：wrapper active 分支必须继续调用 `ConfirmDialog_Update()`，并只调用无新输入的 `SceneChoice_MaintainPulse()`；旧 SceneChoice 和底层 Context 不得读取新按键。
- **唯一业务实现**：全源码只能有一个 `SaveSlot_Update()` 定义；`interface_tome.c` 不得读取存档点全局，`save_point.c` 不得依赖 `INTERFACE_PAGE_7`。
- **客栈来源记忆**：Inn 必须保存 index0/1 的 `return_focus` 并验证同一 owner inactive→active；Synthesis 禁止 include/call Inn，禁止旧 `Inn_RequestRootFocusRestoreFromChild` 与固定首项锁存。
- **视觉安全**：客栈恢复禁止 `SetCursorPos`、菜单手形瞬移或直接写 Button Sprite；实体鼠标/右摇杆仍能接管。
- **编译产物新鲜度**：ASI 必须包含 refactor32、独立 wrapper、客栈原位返回和存档点深层询问标记，旧 refactor31 二进制不能冒充新产物。

最终结果：无 EXE **71 PASS / 0 FAIL**；对完全未修改且 SHA-256 为 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f` 的 `RPG.exe.org` 为 **89 PASS / 0 FAIL**。当前只拿到这一份不同 SHA 的原版样本，未把同内容副本冒充第二样本。

## refactor31 历史工具增量

refactor31 删除了 refactor30 错误的“禁止 `save_point.c`”规则并建立 wrapper owner 护栏，这部分继续有效；其固定首项交接护栏和“wrapper+SaveSlot 即完整”的产物标记已由 refactor32 删除。

## refactor30 历史工具增量

refactor30 建立的全局 L/R 方向护栏继续有效；当时“存档点等于 state7、拒绝 `save_point.c/.h`”的检查已删除并由上述独立包装层协议替代。

## refactor29 自制检查工具增量

`工具/refactor_check.py` 已同步到 refactor29，当前会额外机械验证：

- 25 个独立 C 编译单元全部出现在 `build.bat`；
- 客栈 `0x413850` Update、Hit/Event 2/2；
- 炼化 ctor/Update 与 8/8 ButtonEvent CALL；
- 公共剧情 `mode=3 -> 0x404600` 分发、原版两条结果提交 CALL；
- 客栈 B 不得请求业务 Event；
- 炼化根层 B 必须使用退出图标、第二层 B 必须使用专属取消 Button，源码不得包含 RMB 模拟；
- 炼化不可自行复制“能否炼化”资格逻辑；
- SceneChoice 只能移动隐藏命中坐标、驱动原版选择框 + 左键脉冲，不得显示第二套手形或直接调用剧情结果函数；
- 现有 Battle/Title/Save/Interface/剧情 A/SDL3 热切换等旧护栏全部继续执行。

当前 `RPG.exe.org` 综合结果：**86 PASS / 0 FAIL**。


## refactor28 `refactor_check.py` 历史职责（当时现行）

本包自制工具继续使用 Python 标准库、只读源码/ASI/RPG.exe，不修改游戏文件。本版新增的机械护栏包括：

- state3 原版治疗目标 `+0x768` 短签名必须匹配；
- state3 ButtonEvent 从既有 4 处扩展为 **6/6**：新增 `0x43C933` 角色目标与 `0x43C1F7` 取消；
- 源码必须通过 `UiBridge_GetButtonScreenCenter()` 取得目标头像中心，不允许写死角色头像屏幕坐标；
- A 必须走 `code=2`、B 必须走 `code=1`；
- `InterfaceSkills_TargetSelectionActive()` 必须把这一层声明为 InterfaceShell modal；
- 禁止 `interface_skills.c` 直接写 `+0x768` 或 `+0x58C/+0x588/+0x788/+0x78C` 目标业务字段；
- state8 必须仍只走五个原版 Event，且焦点保留按钮矩形外的正 X 间距与单独 Y 微调；
- build.bat 仍必须是 22 个独立 C、x86、`/W4 /WX /utf-8`、无 CRT；
- 文档、证据、编译内容、缓存/中间文件边界继续检查。

【refactor28 历史】当时工具只能证明结构/协议没有明显破坏；治疗法术目标选择的视觉、手感、实际治疗对象和 B 取消当时仍待实机裁决。后续用户已将 refactor28 整版验收 PASS。

---

## 历史累计记录（只作因果证据）

> 以下内容保留各历史版本当时的设计、失败方案、地址和测试记录。里面出现的“当前/待验/下一步”不再代表 refactor28 现行状态。

包内自制工具：`工具/refactor_check.py`。只使用 Python 标准库；默认只读源码/文档/PE，传 `--exe` 时也只读 RPG.exe，不修改任何目标文件。

## 0. refactor27 `refactor_check.py` 历史职责（当时现行）

当前工具已经随源码更新，不是 refactor26d/e 的旧检查器。主要新增：

- 两个原版 SHA 都作为支持样本；
- state7：检查 `Interface+0x654` 必须复用共享 SaveSlot，既有五个稳定 Hook 不得被重写，新增三项窗口 Hook 必须由独立 installer 安装；
- state7：禁止在 `interface_tome.c` 中实现 Save 文件 I/O 或直接写槽位/游戏状态；
- state8：检查五个原版 ButtonEvent 地址、三行焦点与右侧手形定位逻辑，禁止直接写音乐/音效数值；
- 输入策略增加 SaveAction 与 InterfaceOptions Context；
- 构建单元从 20 增至 22；
- 继续检查业务层不得直接读取 SDL 数字键、PASS/MERGE/OVERRIDE/CONSUME、UiBridge owner、冻结模块结构、中文文档和交付目录清洁度。

执行：

```bat
python 工具\refactor_check.py --root . --exe RPG.exe
```

本次对 `RPG.exe.org` 的最终结果为 **80 PASS / 0 FAIL**。工具只做机械/静态验证，不可把这个结果写成 state7/state8 的实机 PASS。

## 1. refactor26d 机械护栏

- 42 个要求源码/头文件/构建文件齐全；源码文件名保持英文/ASCII。
- 业务层不能直接读取 SDL 数字键；InputRouter 必须维持 Context + PASS/MERGE/OVERRIDE/CONSUME。
- **Battle Target**：`battle.c` 必须与用户已接受的成熟 Target 版本 SHA 完全一致。
- **SDL3 热插拔**：`pad_input.c/.h` 必须与用户 PASS 版本 SHA 完全一致；每 tick 先 UpdateGamepads，再断开/重枚举/打开/采样，重连首帧抑制虚假边沿。
- **Battle 不可交互 A**：建立前拒绝、Event 后 watchdog、自动解锁和 B 清 owner 等已通过结构必须仍存在。
- **左摇杆角色切换**：必须存在 50% 阈值 `16384`、一次推杆 latch、回中死区后 re-arm；Shell 不得再读取 D-Pad 左右进行角色切换。
- **state2/3/4 分页**：D-Pad 左右必须各自调用现成的原版分页 request 函数；禁止复制页码状态机或直接写 page 字段。
- **state3 既有业务保护**：由于本轮合法新增 D-Pad 分页，不再使用整文件 SHA 冻结，而检查真实 ButtonEvent、无直接业务字段写入、确认框安全链等业务不变量。
- **state4**：D-Pad 左右与保留的 LT/RT 必须共用 `equipment_request_page()` 原版分页事务。
- **state5 五内**：X 顺序仍为烈→神→魔→魂→迅；LT/RT/Y 保留 `0x431400` press → `0x431380` release；release 后必须记住同一 Button，并在下一次 state5 游戏线程扫描调用原版 `0x43E6F0(0)` 恢复 Button idle；禁止直接写 Sprite 动画字段。
- **state5 鼠标视觉**：用户已 PASS/封版。工具对 `inner_focus_anchor()+inner_show_node_focus()` 整段源码做固定 SHA，且要求 `(+21,-36)` 偏移；按钮动画修复不得再改焦点位置。
- **state2 第二类确认框安装链**：`plugin.c` 必须实际调用 `ConfirmDialog_InstallHooks()`；UiBridge 必须先于它安装，保证 `0x4276F0` 的 4 个 CALL 真正接入分发层。它与 Exploration 不存在已证实的先后依赖，因此不再做形式化顺序限制。
- **state6 阵形**：D-Pad 四方向硬独占、左摇杆角色切换禁止；A/B 仍走原版 ButtonEvent，不直接写阵形业务字段。
- build.bat 必须编译 20 个独立 C，x86 `/W4 /WX /utf-8 /nodefaultlib`；BAT 为 UTF-8 BOM + CRLF。
- `编译内容/` 只允许 ASI + INI；禁止 OBJ/LIB/EXP/PYC/缓存。
- 14 份现行 Markdown 必须集中在 `文档/`，更新版本时全量遍历同步。

## 2. 使用

```text
python 工具/refactor_check.py --root <包目录>
python 工具/refactor_check.py --root <包目录> --exe <RPG.exe>
```

最终 PASS/FAIL 数字以本包 `证据/当前重构验证/第二十六次重构戊静态检查.txt` 与 `第二十六次重构戊用户原版程序协议对照.txt` 为准；剧情安装链与 r24 能力裁决见 `第二十六次重构戊剧情输入安装链恢复与旧稳定版能力对照.txt`。`丁/丙/乙/甲/无后缀`分别保留为 refactor26d/refactor26c/refactor26b/refactor26a/refactor26 历史证据。

## 3. EXE 检查边界

正式精确目标：`b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf`。

第二原版样本 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f` 会如实得到 **SHA 1 FAIL**；协议一致不代表自动扩展正式支持范围。不要为了让数字全绿删除 SHA 门槛。

## 4. 工具不能替代实机

机械护栏只能证明结构、地址、冻结文件和静态不变量仍存在，不能替代用户实机裁决。五内整页、法宝确定/取消、Battle Target、Battle 不可交互项 A、SDL3 拔插重连等已经由用户 PASS 的项目继续冻结；refactor26e 当前新增待验重点只剩普通剧情 A 两阶段恢复。
## 5. refactor26 历史机器结果

- 无 EXE：**56 PASS / 0 FAIL**。
- 用户完全未修改原版第二样本：**67 PASS / 1 FAIL**，唯一 FAIL 为正式目标 SHA 不同，其余协议全部 PASS。
- 最终 ASI：PE32/i386，125,440 bytes，SHA-256 `02f108bb50ec36eb25342b594291052e283d1dcb5adf7c529366c758e066c6c8`。

该小节是 refactor26 历史机器结果；对应历史原始输出仍保留在 `证据/当前重构验证/第二十六次重构静态检查.txt` 与 `第二十六次重构用户原版EXE协议对照.txt`。当前 refactor26e 结果请以“第二十六次重构戊静态检查.txt / 第二十六次重构戊用户原版程序协议对照.txt / 第二十六次重构戊剧情输入安装链恢复与旧稳定版能力对照.txt”为准。

## refactor26a 新增：Context 策略 / 读取 Layer 一致性检查

`refactor_check.py` 会解析 `InputAction`、每个 `g_policy_*` 与 Context→Policy 映射，再扫描所有 C 文件中的 `InputRouter_PressedOn` / `InputRouter_DownOn` / `InputRouter_ReleasedOn`。

检查规则：

- `PASS`：业务应从 `COMMON` 读取；
- `OVERRIDE`：业务应从 `OVERLAY` 读取；
- `MERGE`：两层均可；
- `CONSUME`：不应存在业务读取。

这项检查正是为了机械阻止本轮“state2 已改 OVERRIDE、popup 仍读 COMMON”再次发生。refactor26b 无 EXE 最终结果：**58 PASS / 0 FAIL**。第二原版样本：**69 PASS / 1 FAIL**，唯一 FAIL 为正式精确 SHA 门槛。

### refactor26b 新增：五内鼠标历史与焦点偏移护栏

工具单独验证：

- `INNER_BUTTON_PRESS_HISTORY = 0x2C`；
- `INNER_BUTTON_RELEASE_HISTORY = 0x28`；
- 按下/松开两帧同时存在 `2 -> 0` 写入；
- 异常中断使用 `inner_clear_button_mouse_history_if_owned()`，不能无条件写缓存裸指针；
- 焦点显示偏移必须固定为 `+21/-36`。

这项护栏只能证明源码仍保留真实鼠标历史同步，不能替代游戏内肉眼确认动画是否只闪一次。

### refactor26c 新增：安装链与五内“事务必完成”机械护栏

工具本轮新增三条直接针对真实回归的检查：

1. `plugin.c` 必须存在 `ConfirmDialog_InstallHooks()`，并验证 UiBridge 已先安装；不再对与其无真实依赖的 Exploration 做顺序绑定；
2. 五内 active pulse 必须在 `controller_focus_visible/Cursor owner` 判断之前处理；`OnPointerTakeover()` 不得再清 `pulse_button/pulse_phase`；watchdog 对 phase2/3 必须转入 release；
3. 用户已通过的五内焦点视觉源码片段 SHA 必须保持 `e886d71a2cd2aade5287a88e900a31eb25279aff21dcc542664622f306dd755a`，避免以后修动画又改坏鼠标位置。

历史结果：refactor26c 为无 EXE **59/0**、第二样本 **70/1**；refactor26d 为 **60/0**、**71/1**；refactor26e 当时为 **64/0**、**75/1**，这些数字只保留作历史。refactor27 已按用户裁决把第二样本纳入支持白名单，**refactor27 当时最终结果为 80 PASS / 0 FAIL**。
